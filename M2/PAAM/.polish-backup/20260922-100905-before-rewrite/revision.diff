--- before/01-threads_zh.md
+++ after/01-threads_zh.md
@@ -12,13 +12,13 @@
 
 ## 一个程序运行起来后，系统管理什么（第 1–2 页）
 
-程序文件记录指令和静态数据；**进程**是程序运行时的实例。同一个编辑器可以启动两次，得到各自的运行状态。不同程序也能同时运行。操作系统负责创建、终止、调度进程，并提供信号、管道、套接字等通信机制。
+启动两次同一个编辑器，磁盘上的程序文件可以相同，两次运行却各有自己的状态。这样的运行实例就是**进程**。操作系统负责让它们开始、运行和结束，并提供信号、管道、套接字等通信方式。
 
-课件将进程近似比作“一台完整机器的虚拟化”：进程有自己的地址空间与资源视图，普通情况下不能随意读写另一个进程的内存。这是理解隔离的比喻，并不意味着每个进程都有独占的物理 CPU 或内存。
+课件把进程近似比作一台完整机器的虚拟化，着重说明它拥有自己的地址空间和资源视图。进程通常不能直接访问另一个进程的内存。底层 CPU 和物理内存仍由系统管理、在进程之间分配，并非每个进程各占一台真实机器。
 
 ## 从函数调用理解线程（第 3–5 页）
 
-假设 `main` 调用 `add(1, 2)`。调用过程中，需要保存参数、局部变量以及返回到调用者所需的信息。课件用调用帧表示这组信息，用栈表示嵌套调用的关系。
+先看一次普通函数调用。`main` 调用 `add(1, 2)` 时，执行过程需要记住传入的参数、计算中用到的局部变量，以及返回后该从哪里继续。课件将这些信息画成调用帧；嵌套调用把多个帧组织成栈。
 
 ```c
 /* 原课件片段 */
@@ -30,15 +30,19 @@
 
 ![调用帧和下一条指令](Images/01-threads/pdf_p004.png)
 
-**图解：** `add` 的帧在 `main` 的帧之上；图中 `x=1`、`y=2`、`z=3`，箭头指向接下来执行的 `return z`。返回后继续 `main` 中的输出操作。图内覆盖正文的标注造成了 OCR 断词，不能把那些残片理解成额外代码。
+读图时从右侧的栈开始：`add` 的帧位于 `main` 之上，保存了 `x=1`、`y=2`、`z=3`。再看左侧箭头，它指向下一步要执行的 `return z`；返回以后，程序回到 `main` 继续输出。图上的大标注遮住了一部分底层文字，OCR 因此产生断词，不能将这些残片拼入代码。
 
-一个线程不仅有“正在执行哪个函数”，还要有“执行到函数的哪里”。课件由此引入线程：可以被 CPU 执行、被系统调度的执行上下文，包括调用栈和下一条指令的位置。实际实现还保存寄存器等状态。优化编译器可能把变量放在寄存器、内联函数，所以这张图是调用过程的教学模型，不是每次编译后的物理内存布局保证。
+如果系统此时暂停执行，稍后恢复，它既要找回这组调用信息，也要知道下一条指令在哪里。**线程就是系统可以调度执行的上下文**：课件用调用栈和下一条指令的位置来说明它，实际实现还保存寄存器等状态。
+
+这幅图适合用来追踪调用和返回。至于编译后的具体布局，变量可能被放进寄存器，函数也可能被内联，不一定逐项对应图中的栈格。
 
 ## 一个进程如何容纳多个线程（第 6–7 页）
 
-同一进程的线程共享代码、全局数据和堆等地址空间，但各有自己的栈和执行位置。于是两个线程既能处理不同任务，又能通过共享对象合作。共享地址空间也意味着：一个线程拿到另一个线程栈中对象的有效地址后，可以访问它；“各有自己的栈”不等于地址被隔离。
+一个进程可以包含多个这样的执行上下文。线程各有调用栈和执行位置，同时共享进程的代码、全局数据和堆，因此可以一边处理不同任务，一边访问共同的数据。
 
-课件从单线程启动的常见过程讲起：程序启动后创建其他线程，系统把它们调度到 CPU 上。并发表示执行过程可以交错；并行表示它们确实在多个执行单元上同时进行。只有一个 CPU 时也可能存在并发错误。
+各有栈，并不意味着栈地址相互隔离。只要获得一个仍然有效的对象地址，线程也可以访问另一个线程栈上的对象。决定访问是否安全的，仍是对象的生命期以及访问之间的同步。
+
+课件从常见的单线程启动过程讲起：初始线程创建其他线程，操作系统再把它们安排到 CPU 上。几个线程轮流运行、执行步骤相互交错，就已经构成并发；如果它们在多个执行单元上同时运行，才发生了并行。后面分析错误时，只需要一次不利的交错，因此单 CPU 也可能出现并发错误。
 
 `pthread_t` 用于表示线程标识，`pthread_self()` 返回当前线程的标识。不要把 `pthread_t` 无条件当成普通整数，也不要将它与 Linux 内核线程号混为一谈。
 
@@ -46,28 +50,36 @@
 
 ## 创建、结束与回收（第 8–11 页）
 
-课件中的创建接口为：
+接下来用 pthread 接口把执行上下文创建出来。先读参数：调用者提供入口函数和参数，系统通过 `tid` 写回新线程的标识。
 
 ```c
 int pthread_create(pthread_t *tid, const pthread_attr_t *attr,
                    void *(*start_routine)(void *), void *arg);
 ```
 
-**接口校对：** 课件省略了 `attr` 的 `const`；这里按 POSIX 接口补齐。`tid` 是写回新线程标识的地址；`attr` 描述属性，传 `NULL` 使用默认属性；`start_routine` 是入口函数；`arg` 是传给入口的参数值。传的是一个指针值，并不会自动深拷贝它所指向的整个对象。
+入口函数 `start_routine` 接收一个 `void *` 参数，因此可以通过 `arg` 传入对象的地址。`pthread_create` 传递这个指针值，**不会替你复制指针所指的对象**；对象由谁持有、能存活多久，需要调用者安排。
 
-创建成功后，新线程和创建者的先后执行顺序不确定。第 8 页的例子在创建之后直接从 `main` 返回，因此不能保证工作线程的输出一定发生。这是课件紧接着引入等待终止的原因。
+其余两个参数负责线程信息：`tid` 指向接收新标识的位置；`attr` 描述线程属性，传 `NULL` 使用默认属性。课件省略了 `attr` 的 `const`，上面的声明按 POSIX 接口补齐。
 
-线程可以从入口函数返回，也可以调用 `pthread_exit(retval)` 结束。课件第 9 页的 `start_rountine` 是拼写问题，含义是 `start_routine`。从 `main` 返回或某个线程调用 `exit` 会结束整个进程，不能与结束当前线程混淆。因此第 6 页“最后一个线程结束时进程结束”不是全部终止情形。
+创建成功以后，入口函数何时开始执行取决于调度，创建者也会继续向下运行。第 8 页直接从 `main` 返回，工作线程可能还没来得及输出，整个进程就结束了。因此，创建线程之后通常还要明确：程序由谁等待它完成。
+
+线程从入口函数返回，或调用 `pthread_exit(retval)`，都可以结束自身的执行。与此区别的是进程退出：从 `main` 返回，或者任何线程调用 `exit`，会使整个进程结束。
+
+读第 6 页“最后一个线程结束时进程结束”时，要同时记住第 9 页补充的这些情形。第 9 页中的 `start_rountine` 是拼写错误，指的就是入口 `start_routine`。
 
 ```c
 int pthread_join(pthread_t thread, void **pretval);
 ```
 
-`pthread_join` 等待指定线程结束，并可把它的返回值写到 `*pretval`。二级指针来自“要把一个指针结果写回给调用者”。如果不要结果，可以传 `NULL`。第 10 页的 `(void *)0x42` 只是演示返回一个指针值，不代表地址 `0x42` 上有可访问对象，更不能直接解引用它。
+调用 `pthread_join` 后，调用者等待指定线程结束，还可以取回它的返回值。返回值本身是一个指针，接口又需要把它写到调用者提供的位置，所以参数使用二级指针，写回目标是 `*pretval`。不需要结果时，可以传 `NULL`。
 
-默认创建的线程是 joinable：终止后仍需由合适的调用者 `join` 以回收相应资源。detached 线程结束时由系统回收这类线程资源，不能再用 `join` 获取结果。线程退出并不自动释放它分配的全部堆对象；共享对象的生命周期仍须由程序设计。
+第 10 页用 `(void *)0x42` 演示传回一个指针值。这不说明地址 `0x42` 上存在可用对象；取回结果和解引用结果是两回事。
 
-**学习检查：** 把函数局部变量的地址传给线程后，创建者马上返回是否安全？只有在该对象确实活到线程最后一次使用之后才安全；让线程 detached 并不能延长局部变量的寿命。
+默认创建的线程处于 joinable 状态。执行结束后，它还保留供另一个线程 `join` 的状态和相应资源。detached 则把这类资源的回收交给系统，结束后不能再用 `join` 获取返回值。
+
+这里回收的是线程自身的相关资源。线程曾经分配的堆对象可能还要由其他线程使用，是否释放、何时释放，都仍由程序管理。
+
+把前面的参数传递与退出规则放在一起看：若参数来自创建者的局部变量，创建者返回以后，该对象就可能失效，而新线程仍准备使用它。将线程设成 detached 只改变回收方式，不能延长参数对象的生命期。
 
 <a id="lost-update"></a>
 
@@ -91,7 +103,7 @@
 
 ## 互斥保护的是一段操作（第 13–15 页）
 
-临界区是需要与其他相关代码段互斥执行的区域。这里要保护的是从读取余额到写回余额的完整过程。如果只在写回时加锁，两个线程仍可能在锁外读到同一个旧值。
+余额错误发生在读取旧值到写回新值之间。因此需要把这整个更新过程放进临界区，让另一个相关操作等它完成后再开始。只保护最后一次写回，仍会允许两个线程事先读到同一个旧余额。
 
 互斥锁有空闲和占用两种抽象状态。获取操作等到锁可获得，并使当前线程成为持有者；释放操作允许其他等待者继续。它们的原子效果由实现提供，下一讲将解释这种实现。
 
@@ -104,17 +116,17 @@
 pthread_mutex_unlock(&m);
 ```
 
-这里的关键不变量是：任何一个要参与该保护协议的线程，在访问共享余额的相关状态时都持有 `m`。仅给写入者加锁、读取者绕过锁，或者每个线程使用不同的锁，都不能建立这个保证。
+**所有参与余额更新的线程，都要遵守同一把锁的保护规则。** 上面操作中的 `m` 保护从读到写的全过程。若读取者绕过锁，或者存款和扣款各自使用不同的锁，原来的交错仍可能发生。
 
-课件说临界区“原子地执行”，是指遵守同一同步协议的竞争操作看不到中间状态，不是说整个临界区变成了一条 CPU 指令，也不是说线程不能在临界区内被抢占。
+遵守这个规则的竞争操作只能依次观察完整更新，这就是课件所说的临界区原子效果。临界区内部仍有多条指令，持锁线程也可能被抢占；其他线程在它恢复并释放锁之前继续等待。
 
 <a id="condition"></a>
 
 ## 条件变量：等数据到达时，让出锁（第 16–17 页）
 
-接收者需要等待 `msg != NULL`。只用互斥锁还不够：若它一直持锁检查 `msg`，发送者就无法拿到锁更新消息。若它先解锁、再自行休眠，消息可能恰好在两步之间到达，从而错过通知。
+有了互斥锁，线程可以安全地检查消息状态，但还要解决消息尚未到达时怎么办。接收者若一直持锁等待 `msg != NULL`，发送者就拿不到锁，也无法设置消息。若先解锁再自行休眠，通知又可能恰好落在这两步之间，接收者睡下以后无人再叫醒它。
 
-条件变量把“等待某个状态变化”与互斥锁配合起来。课件示意的接收方为：
+条件变量与互斥锁配合，协调检查条件、释放锁和等待通知这几个动作。先看接收者的写法，注意 `pthread_cond_wait` 前已经持有锁，并且它位于 `while` 循环内：
 
 ```c
 pthread_mutex_lock(&m);
@@ -124,19 +136,21 @@
 pthread_mutex_unlock(&m);
 ```
 
-`pthread_cond_wait` 在调用者持有互斥锁时进入，把释放锁与进入等待协调起来；返回前重新获取该锁。返回之后，线程必须再次检查自己的条件。原因既包括可能的虚假唤醒，也包括其他线程在它重新获取锁之前又改变了状态。POSIX 明确要求围绕共享状态谓词使用这种检查逻辑。[POSIX 条件等待](https://pubs.opengroup.org/onlinepubs/9799919799/functions/pthread_cond_clockwait.html)
+`pthread_cond_wait` 将释放互斥锁与进入等待协调起来，使发送者有机会拿锁修改状态；等待结束时，它会重新取得这把锁，然后才返回。
+
+**返回以后仍要检查条件。** 一次唤醒不保证消息状态满足要求：可能发生虚假唤醒，也可能在接收者重新拿到锁之前，其他线程又改变了状态。这就是代码使用 `while` 的原因。POSIX 对条件等待也要求依据共享状态谓词重新判断。[POSIX 条件等待](https://pubs.opengroup.org/onlinepubs/9799919799/functions/pthread_cond_clockwait.html)
 
 发送方在同一把锁下设置 `msg`，调用 `pthread_cond_signal`，再释放锁。唤醒不会把锁立即转交给接收者。`signal` 至少唤醒一个等待者（如果存在），`broadcast` 唤醒所有等待者；被唤醒者仍需竞争锁并检查谓词。
 
 ![消息发送和条件等待](Images/01-threads/pdf_p016.png)
 
-**图解：** 左侧修改共享状态并通知，右侧用 `while` 检查。真正记住“消息已经到达”的是 `msg`，条件变量本身不储存一张可供以后消费的通知票。若消息在接收者开始等待之前就已设置，接收者检查到非空即可直接继续。
+图中左侧负责写消息并通知，右侧负责持锁检查。保存消息是否到达这一事实的是 `msg`，条件变量只参与等待和唤醒。如果发送者先设置好消息，接收者稍后拿锁检查到非空，就可以直接继续，不需要补收一次过去的通知。
 
 这只是一次消息传递的教学例子。反复发送时，还需要明确消费后如何清空状态、多条消息如何排队、字符串何时失效，不能把这段代码直接当作完整消息队列。
 
 ## 把生命周期与同步分开思考（第 18 页）
 
-`create`、`exit`、`join` 管理执行的开始、结束和回收；mutex 保护共享状态更新；条件变量允许线程在状态尚不满足时等待。`join` 不是用于每次数据到达的通用通知机制，条件变量也不代替线程资源回收。
+到这里，可以分别画出线程的生命周期和共享对象的访问过程。`create`、`exit`、`join` 说明线程从创建到结束、回收的关系；mutex 规定哪些状态更新必须互斥；条件变量处理条件尚未满足时的等待。每次数据到达通常不需要结束一个线程再 `join`，而线程最终结束时，也不能靠条件通知代替资源回收。
 
 对应实验：[线程与同步](../Labs/tp-threads/tp-threads.html)。建议先能画出自己的线程何时创建、何时退出、谁等待谁，再为共享对象标明保护它的锁和等待条件。
 
--- before/01-welcome_zh.md
+++ after/01-welcome_zh.md
@@ -12,15 +12,17 @@
 
 ## 多个核为什么没有自动让程序变快（第 1–2 页）
 
-这门课研究如何在底层编写多核程序。课件举出数据中心、桌面和手机上的多核处理器，并展示一台有 4 个插槽、每个插槽 12 个核的机器，总计 48 个核。这里的核数是该机器的例子，不是当前处理器的规格上限。
+多核机器让多个任务有机会同时执行。要把这种机会变成程序的加速，需要决定怎样分工、怎样共享数据，以及发生冲突时谁先修改。这门课就从 C 程序和底层同步机制出发，逐步讨论这些问题。
+
+课件展示的机器有 4 个插槽，每个插槽 12 个核，共 48 个核。数据中心、桌面和手机都可以采用多核架构；图中的核数只是这台机器的配置。
 
 ![课件中的多核机器](Images/01-welcome/pdf_p002.png)
 
-**图解：** 四个插槽意味着处理器资源分布在多个物理位置。一个插槽中的核与另一个插槽中的核可以同时执行，但共享数据的访问与传递有成本。后面的锁、NUMA 和缓存一致性都在处理这种合作成本。
+图中四个插槽对应不同的物理位置。不同插槽中的核可以同时执行，但彼此交换共享数据需要时间。后面讨论锁的争用、缓存一致性和 NUMA 时，都会回到这一点：增加执行者的同时，也增加了协调和传递数据的工作。
 
-设想两个线程同时往银行账户存钱。增加线程给了程序同时处理请求的机会，却也引入了“先读余额、再计算、最后写回”的交错。如果两次修改互相覆盖，速度再快也没有意义。使用锁可以恢复正确性，但所有请求都排在一把锁后面，又可能使多个核大部分时间只是在等待。
+以账户存款为例，两个线程都要读取余额、加上存款额，再写回结果。若它们读到同一个旧余额，后写回的结果可能覆盖先前的修改。加锁可以让更新依次完成；不过，如果所有请求都依赖同一把锁，其他核就可能一直等待，无法发挥并行能力。
 
-因此学习主线是：先明确哪些操作必须表现为一个整体，再选择实现方法，最后分析通信、等待和内存访问成本。正确性是性能比较的前提。
+学习时可以始终围绕同一个程序追问：哪些状态由多个线程共享，一次更新怎样才算完成，等待和数据传递又花了多少时间。先把这些关系讲清楚，再比较实现的速度，才知道性能改善有没有牺牲正确性。
 
 <a id="route"></a>
 
@@ -37,7 +39,7 @@
 
 课件给出的组织方式是六周，每周一个主题，含 1–2 小时理论、4–5 小时实验及平均 1 小时课后工作。这是原材料的安排说明，实际课表以教师通知为准。
 
-学习时以这篇讲义和对应实验为主。遇到指针、内存管理或系统调用的具体困难，再回查基础材料，不必先把全部补充课程读完。
+阅读顺序与实验顺序配合起来即可。先用线程和 mutex 写清一个共享状态的更新过程，再追问 mutex 怎样实现。遇到指针、内存管理或系统调用的具体障碍时，回查相应基础材料。这样可以把先修知识用在眼前的问题上，不必等到所有补充课件读完才开始。
 
 <a id="prerequisites"></a>
 
@@ -45,7 +47,7 @@
 
 课件要求较好的 C 编程能力，尤其是指针和内存管理，以及进程、进程间通信等系统知识。
 
-一个直接的准备检查是：你能否说明一个指针指向谁、该对象由谁创建、在什么时候失效？线程开始执行的时间不由创建者精确控制，因此“把局部变量地址传给另一个线程”是否安全，取决于该变量在对方使用期间是否仍然存在。这类对象生命周期问题会贯穿后续各讲。
+读并发代码前，先练习追踪对象的生命期：指针指向哪个对象，由谁创建，谁还会使用它，何时才能销毁。比如，把函数局部变量的地址传给新线程以后，创建者可能先返回，而新线程尚未读取参数。这时关键就在于对象是否仍然存在。后面的链表删除、事务日志和内存回收，都会再次遇到这个问题。
 
 <a id="self-check"></a>
 
--- before/02-locks_zh.md
+++ after/02-locks_zh.md
@@ -12,15 +12,17 @@
 
 ## “看一眼空闲，再写成忙碌”为什么不够（第 1–4 页）
 
-假设线程甲读到锁空闲，尚未写入忙碌状态就被暂停；线程乙也读到空闲，随后进入临界区；甲恢复后同样进入。单次读取和写入各自原子，并不等于“检查后占有”这个组合原子。
-
-课件把原子操作非正式地解释为“看起来在一个瞬间完成”。它强调的是其他线程能观察到的效果，而不是执行时间真的为零。实现锁时不能用同一把尚未实现的锁保护自己，因此要从更低层的原子读写及硬件原语开始。
+上一讲把互斥交给了 pthread mutex。这一讲从它内部需要完成的动作出发：检查锁是否空闲，然后取得它。
+
+设线程甲刚读到空闲便被暂停，线程乙也读到空闲，并将锁设为忙碌、进入临界区。甲恢复以后若照旧设置状态，也会进入。同一个空闲状态被两人接受，问题就发生在检查与设置之间。即使单次读写各自原子，组合起来仍需要额外保证。
+
+原子操作使这类动作对其他线程表现为一个不可分割的效果。课件用“看起来在一个瞬间完成”描述它，关心的是观察结果；实现仍然需要时间。为了实现 mutex，我们要找到足以完成这种不可分割更新的底层机制，不能再假设已经有一把 mutex 可以保护它。
 
 **原课件勘误，第 2 页：** 图中确实写了 `while (mutex->state == FREE) { }`。若含义是等待锁空闲，判断应当针对 `BUSY`。但仅改条件仍不能构成正确的锁，因为检查和设置之间仍有竞争窗口。同页 `#define ... = { FREE };` 也不是应当照抄的初始化宏写法。这些都是 PDF 原文问题，不是 OCR 把字符识别错了。
 
 ## 必须先说清机器允许什么（第 5–8 页）
 
-课件先假设：一个机器字的读写不可撕裂，并且线程指令按指定顺序观察。于是读取从 `0` 改成 `0x101` 的变量，只会读到完整旧值或新值。
+讨论算法前，先固定用于推理的机器模型。课件暂时假设机器字的读写不可撕裂，指令也按指定顺序被观察。第一个条件表示：变量从 `0` 写成 `0x101` 时，读取者得到完整旧值或完整新值，不会得到由两者拼出的部分结果。
 
 第二个例子先写 `x=0x101`，后写 `y=0x202`；另一个线程先读 `y` 再读 `x`。在所设模型中，如果读到新 `y`，接着就应读到新 `x`。第 6、8 页用覆盖式警告说明这些假设不能不加条件地推广到所有硬件。
 
@@ -30,7 +32,9 @@
 
 ## Bakery：怎样不用原子加一也能排队（第 9–13 页）
 
-把取锁想成面包店取号。每个线程扫描目前的号码，取最大值加一。同一时刻可能有两个线程取到相同号码，因此比较的是二元组 **(号码，线程编号)**：号码小的先走；号码相同时，线程编号小的先走。号码 0 表示不参与竞争。
+Bakery 算法用排队取号决定谁先进入临界区。每个线程扫描现有号码，取最大值加一，再与竞争者比较。由于扫描和取号并非一个整体，两个线程可能同时选中相同号码，所以还需要线程编号来区分先后。
+
+比较规则是按 **(号码，线程编号)** 排序：先比较号码，号码相同再比较编号，较小者优先。号码 0 单独表示该线程没有参与竞争。
 
 `num[i]` 是线程 i 的号码；`entering[i]` 表示它还在取号过程中。一个线程的典型步骤是：
 
@@ -38,45 +42,55 @@
 2. 对每个竞争者，等它完成取号；若它持有优先于自己的非零号码，就等待。
 3. 所有更优先的竞争者退出后进入临界区；离开时把自己的号码改成 0。
 
-为什么必须有 `entering`？线程 2 算出了最大值加一，却在写回前停住。线程 3 看到旧数组，也取到同一个号并进入。线程 2 恢复写入后，发现自己编号更小，也可能进入。`entering[2]` 使线程 3 知道“它还没取完号”，不能提前比较。
+`entering` 解决取号尚未完成的窗口。设线程 2 已计算出最大值加一，却还没写回；线程 3 此时仍看到旧数组，也取得同一个号并进入临界区。线程 2 恢复后写入号码，又会因为自己的编号更小而认为应当先走。
+
+因此，比较号码之前必须先等对方完成取号。线程 3 若看到 `entering[2]` 为真，就知道还不能依据当前号码作决定。
 
 ![Bakery 原课件代码](Images/02-locks/pdf_p011.png)
 
 **原课件勘误：** 第 11 页 `if (entering[i]) { }` 是空条件语句，不会等待，和旁边注释及第 12 页的论证不符。教学解释应为“持续等到 `entering[i]` 变假”，例如 `while (entering[i]) { }`；原文保留该差异。第 13 页解释中写“maximum number”也不能理解为最大号先进入，算法比较的是较小的非零二元组优先。
 
-互斥理由可以从反证理解：两个已经完成取号的竞争者若同时宣称自己优先，那么它们的二元组都要小于对方，严格全序不允许这种情况。完整证明还要考虑取号与扫描交错；`entering` 正是为此服务。这里讲的是课件的抽象机器版，实际 C 实现需要原子对象与适当顺序，不能让普通 `int` 数组跨线程竞争读写。[Lamport 原始论文](https://lamport.azurewebsites.net/pubs/bakery.pdf)
-
-代价是每次取锁要扫描多个线程的状态，访问次数随线程数 N 增长，数量级为 O(N)。原课件的“至少四次”是特定计数说明，不是所有实现和所有等待时长的准确常数界。
+先只看已经完成取号的竞争者，二元组给出了唯一的先后顺序。若两个线程都能越过对方进入，就需要各自的二元组都小于对方，这与严格全序矛盾。完整证明还要覆盖取号与扫描相互交错的过程；前面的 `entering` 正是在处理这部分。
+
+这个推理使用的是课件设定的抽象机器。写成 C 时，相关状态需要合适的原子对象与内存顺序，不能让普通 `int` 数组发生跨线程竞争读写。[Lamport 原始论文](https://lamport.azurewebsites.net/pubs/bakery.pdf)
+
+取号和比较都要扫描竞争者的状态，因此单轮访问量随线程数 N 增长，数量级为 O(N)。一旦需要等待，扫描还可能重复。原课件的“至少四次”是在说明特定步骤的访问数量，并没有给出包含全部等待过程的固定成本。
 
 <a id="cache"></a>
 
 ## 缓存一致性为什么影响锁的速度（第 14–20 页）
 
-CPU 通常按缓存行管理数据，课件以常见的 64 字节行为例。多个核可以拥有同一行的共享副本；某个核要写时，需要取得相应写权限，并使其他不再有效的副本失效。课件用“多个读者或一个写者”的简化模型解释一致性，不是说硬件逐行调用了软件读写锁。
+Bakery 已经给出了一种互斥办法，接下来要理解它为什么可能慢。读写共享状态会触及 CPU 缓存，而缓存通常以整行为单位维护数据，课件以常见的 64 字节行为例。
+
+同一行可以在多个核中保留共享副本。一个核需要写入时，要先取得对应写权限，使其他不再有效的副本失效。课件用多个读者或一个写者来概括这种协调；这是缓存一致性的简化模型，硬件并未逐行调用软件读写锁。
 
 ![两个核写同一缓存行](Images/02-locks/pdf_p017.png)
 
 **图解：** 两个线程分别写 `tab[0]` 和 `tab[1]`，逻辑上没有写同一元素，却可能在同一缓存行上竞争。图中各自持有旧副本、各改一部分的画法用于解释为什么必须有一致性协议，不是说正常硬件任意丢写。
 
-第 18 页列出读缺失、写缺失和共享行转写三种情况。理解它们时抓住一个问题：下一次操作是否需要从别的核取得数据或写权限？不同核反复写同一个锁字，会使所属缓存行来回转移。即使临界区很短，这种交接也可能限制吞吐量。
+第 18 页的读缺失、写缺失和共享行转写，可以从同一个角度理解：这个核是否已经拥有所需的数据和权限，还是要与别的核通信。多个核反复写同一锁字时，所属缓存行便可能在它们之间来回转移。临界区很短，也可能因为频繁交接而无法继续提高吞吐量。
 
 <a id="ordering"></a>
 
 ## 原子性与顺序是两件事（第 21–29 页）
 
-原子读写首先保证对象访问不被拆成可观察的部分。C 中可以使用 `_Atomic` 对象以及 `atomic_load`、`atomic_store`。内存顺序则回答：观察到某个更新之后，可以推断哪些其他更新已经发生？
+缓存协议之后，还要分清原子性与内存顺序。对 `_Atomic` 对象使用 `atomic_load`、`atomic_store`，首先是为了保证对象访问的原子性。一次读取没有被撕裂，并不足以说明它与其他变量的更新有什么先后关系。
+
+例如接收者看到了有消息标志，还需要知道发送者先前写入的消息内容是否已经可见。内存顺序就是用来建立这类推断的。
 
 课件第 23 页列举读读、写写、写后读、读后写等顺序关系；第 24–26 页比较 TSO、较弱模型和屏障。这里的机器举例及 Java 说法均为教学概括，不能直接用来证明一段有 C 数据竞争的程序正确。具体指令、内存类型及对齐限制应以处理器手册为准。[Intel 架构手册](https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html)
 
 以“先写消息，再设置有消息标志”为例，即使目标硬件保存相关顺序，编译器和语言层也仍需正确的同步关系。第 24 页标为“Correct code”的普通 C 风格片段不能作为可移植的 C 并发实现；第 25 页“Java 中段错误”也只是提示发布错误风险，不能按 C 的故障形式解释 Java 语言行为。
 
-`relaxed` 原子访问保留该原子对象的原子性及修改顺序，但不靠自己发布其他对象。若两个原子变量初值均为 0，线程 1 依次写 `x`、`y`，线程 2 依次读 `y`、`x`，仅使用 relaxed 时，读到新 `y`、旧 `x` 并不矛盾。第 29 页警告层在 OCR 中混进了代码；它应作为解释单列。“普通非原子访问可以读到拼接值”也不能穷尽 C 数据竞争的后果，因为语言语义已经是未定义行为。
+`relaxed` 保留原子对象自身的原子性与修改顺序，但单靠它不能发布其他对象。设两个原子变量初值为 0：线程 1 先写 `x` 再写 `y`，线程 2 先读 `y` 再读 `x`。如果只用 relaxed，读到新 `y`、旧 `x` 仍是允许的，因为这里没有建立从一个对象的观察到另一个对象的同步关系。
+
+第 29 页的警告标注曾被 OCR 混进代码，应单独阅读。还要注意，普通非原子对象上的 C 数据竞争会导致未定义行为，不能将其后果仅概括成可能读到拼接值。
 
 课件列出的各个 `memory_order` 并非可以随意用于每一种操作。例如普通 atomic load 不能指定 release，普通 atomic store 不能指定 acquire；acq_rel 用于适合的读改写等操作。更弱顺序需要证明，不能只凭一次测试没出错就采用。
 
 ## Release–acquire：用一个原子对象发布一组数据（第 30 页）
 
-设发送者先写 `z=1`、`y=17`，再对原子 `x` 做 release 写入 42；接收者对 `x` 做 acquire 读取。如果这次读取确实读到该发布所对应的值，在没有其他冲突写入的前提下，发布前的 `y`、`z` 写入就通过同步关系对后续读取可见。
+现在为消息发布补上同步关系。发送者先完成 `z=1`、`y=17`，再对原子 `x` 做 release 写入 42；接收者用 acquire 读取 `x`。只有这次读取确实取得相应发布的值，才能通过这条同步关系使用发布前写好的 `y`、`z`。这里还假设没有其他冲突写入。
 
 ```c
 /* 教学片段：x 是原子变量；y、z 仅由发布者在发布前写入。 */
@@ -91,7 +105,7 @@
 }
 ```
 
-这可以写成因果链：数据初始化 → release 发布 → 读到该发布的 acquire → 使用数据。仅仅在两个线程里各写了 release/acquire 并不够，它们必须建立对应关系。
+数据初始化 → release 发布 → 读到该发布的 acquire → 使用数据，这几步组成了读者所依赖的顺序。**关键是 acquire 确实读到了相应发布，而不仅是两边都写了内存顺序参数。** 上面的条件分支将使用数据放在确认发布之后，便于看清这一前提。
 
 **原课件勘误：** PDF 第 30 页两个 atomic 接口的 `x` 前均缺少 `&`，且无条件读取普通 `y`、`z`。若 acquire 没有读到发布值，这些读取就没有获得所需同步。上面增加了条件，是明确标注的教学修正版，不是声称原图已经这样写了。
 
@@ -112,9 +126,11 @@
 线程4读 y→1
 ```
 
-因此课件列出的结果并没有违反共同全序。这是原例子的逻辑问题，不是 OCR 问题。若要构造标准的相反写序观察例，需要让两个读线程按相反的变量顺序读取，并重新指定结果；这里不偷偷替换原题。
-
-第 33 页十分重要：后续课件为了可读性，常省略显式原子调用，默认使用顺序一致的原子读写。阅读后面的锁与非阻塞伪代码时必须带着这个约定；搬到实验的 C 文件里时再落实类型和接口。
+逐行检查这个交错，两位读者都保持了先读 x 再读 y 的顺序，所有操作也能放在同一个全序中，所以它足以反驳原页的不可能结论。问题来自原例子的逻辑，OCR 并未造成这一点。
+
+通常用来展示相反写序观察的例子，会让两个读线程按相反的变量顺序读取，并相应设置结果条件。那是另一个例子，不能直接用它替换原页后声称原图正确。
+
+**后续课件默认使用顺序一致的原子读写。** 第 33 页说明，为了简化表达，很多显式原子调用会从伪代码中省略。阅读下面的锁和下一讲的数据结构时，要一直保留这个约定；写实验的 C 实现时，再把原子类型与接口明确写出。
 
 <a id="locks"></a>
 
@@ -129,13 +145,17 @@
 atomic_store(lock, FREE);
 ```
 
-失败者仍在反复做读改写，争夺同一缓存行的写权限。所以这种锁在低争用时很直接，高争用时却会造成大量通信。课件第 38 页强调写失效和排序成本，但“原子性本身没有任何性能成本”过于绝对；实际延迟、吞吐和实现资源同样要考虑，不应当作为通用硬件定律。
+拿不到锁的线程会继续执行 exchange。即使锁已经是 BUSY，这仍然是一次读改写，会争夺同一缓存行的写权限。低争用时，获取路径很短；高争用时，大量失败尝试却可能把时间耗在缓存通信上。
+
+第 38 页用写失效和排序开销解释这一现象，但“原子性本身没有任何性能成本”的说法过于绝对。真实硬件还需考虑指令延迟、吞吐和实现资源，不能从这页推出普遍的零成本结论。
 
 对于恰当实现的这一简单锁，成功获取使用 acquire、释放使用 release 可以提供临界区之间的可见性。这个结论不能自动推广为“所有算法都可以统一把 seq_cst 换成 acquire/release”。
 
 ## Ticket lock：写一次取号，随后只读等待（第 41–46 页）
 
-`atomic_fetch_add` 把变量加上增量，返回加之前的值。共享的 `ticket` 发放不同的号，`screen` 指示当前轮到谁。
+Ticket lock 让每个线程先取得自己的序号，再等待轮到自己。它用到 `atomic_fetch_add`：将共享变量加上增量，同时返回加之前的值。这样，竞争者能获得不同的号。
+
+锁里分别保存 `ticket` 和 `screen`：前者发号，后者表示当前可以进入的号码。
 
 线程原子地取号，然后等待屏幕显示自己的号；离开时使屏幕进入下一号。课件用 `< my` 检查等待，在号码单调且不回绕的教学假设下可读懂。工程实现常用无符号计数和相等性比较，并明确回绕及同时在队列中的线程数限制，不能忽视有限整数范围。
 
@@ -149,15 +169,15 @@
 
 先理解 CAS：给定地址、期望值和新值，只有当前值匹配期望时才写入。C 的 `atomic_compare_exchange_strong` 返回真假；失败时还会把观察到的当前值写回 `expected`。第 48 页函数体中误拼为 `..._string`，标题中的 `..._strong` 才是实际名称。
 
-MCS 为等待线程建立队列，每个线程拥有自己的节点，含 `next` 和 `isFree`。课件用 `_Thread_local` 表示每线程各有一份节点。全局 `lock` 变量在结构上是队尾指针，不是始终指向持有者。
+Ticket lock 的等待者虽然主要在读，仍然盯着同一个 screen。MCS 把等待位置分散到每个线程自己的节点中，节点包含 `next` 和 `isFree`。课件使用 `_Thread_local`，让每个线程各有一份节点；全局 `lock` 则记录队尾，用来把新到的线程接进队列。
 
 获取过程：初始化节点，将队尾原子交换成自己。如果原队尾为空，可以直接获得锁；否则把自己接到前驱的 `next`，然后反复读取自己的 `isFree`。释放者只需向直接后继写一次许可，而不必让所有等待者争抢同一个锁字。
 
 ![MCS 获取](Images/02-locks/pdf_p051.png)
 
-**图解：** 新等待者先改全局队尾，再补上前驱到自己的链。这两步之间可能被暂停，所以“前驱还没有看到 `next`”并不能证明没有后继。
-
-释放过程必须处理这个窗口：
+图中入队包含两步：新等待者先通过交换取得队尾位置，再把前驱的 `next` 连到自己。线程可能在两步之间被暂停。这时，前驱看见的后继链接还是空的，但全局队尾已经指向后来者。
+
+释放锁时必须区分这种情况与队列确实没有后来者的情况，具体步骤如下：
 
 1. 若 `my.next` 为空，尝试 CAS 把队尾从自己改为空。成功说明没有后来者取得队尾，可以结束。
 2. 若 CAS 失败，后来者已改队尾，但可能还没把链接写入自己的 `next`，因此等待该链接出现。
@@ -167,7 +187,9 @@
 
 **校对说明：** 原图旁注说“head of the queue”，但这次 CAS 实际比较的是全局队尾。教程按代码的作用解释。简化版每线程只有一个节点，不能直接用它同时嵌套获取多个 MCS 锁；节点的存活期也必须覆盖排队与交接。
 
-MCS 的公平性是队列中的交接顺序，不是等待时长的硬上界。如果下一个继任者被调度器暂停，其他人可能形成 convoy（队列拖延）。本地自旋降低共享写热点，但不消除调度和通信成本；节点若挤在同一缓存行，还可能出现伪共享。
+MCS 按队列顺序交接锁，因此其公平性体现为先后次序。如果下一位等待者被调度器暂停，排在后面的人仍可能一起等待，形成 convoy（队列拖延）；FIFO 并没有给出固定的等待时长。
+
+把自旋分散到各自节点能减轻共享写热点，但入队和交接依然需要通信。节点布局也影响效果：多个节点挤在同一缓存行时，仍可能出现伪共享。
 
 ## 将三种锁放在一起（第 53 页）
 
--- before/03-non-blocking_zh.md
+++ after/03-non-blocking_zh.md
@@ -8,13 +8,13 @@
 
 阅读路线：[性能动机与进展保证](#progress) → [栈](#stack) → [队列](#queue) → [链表](#list) → [回收边界](#reclamation) → [自检](#self-check)。
 
-沿用上一讲第 33 页的约定：共享访问按顺序一致原子操作理解。课件的 `CAS(address, old, new)` 返回交换前观察到的旧值；这不同于 C `atomic_compare_exchange_strong` 的布尔返回值和 `expected` 指针接口，不能直接替换函数名后照搬。
+读这一讲的代码前，先记住两项约定。上一讲第 33 页规定，共享访问按顺序一致的原子操作理解；这里的 `CAS(address, old, new)` 则**返回交换前观察到的旧值**。因此，代码常通过比较返回值与期望值来判断成功。C 的 `atomic_compare_exchange_strong` 使用布尔返回值和 `expected` 指针参数，接口不同，不能只替换函数名。
 
 <a id="progress"></a>
 
 ## 先问瓶颈在哪里，再问能否不用锁（第 1–3 页）
 
-若单线程时间为 T，其中比例 p 可以完全并行，理想 n 线程时间为
+增加线程以前，先看程序有多少工作仍须串行完成。将单线程总时间记为 T，可完全并行的比例记为 p：串行部分不会随线程数减少，理想的并行部分则由 n 个线程均分，于是得到
 
 $$
 T_n=T\left((1-p)+\frac pn\right),\qquad
@@ -23,7 +23,7 @@
 
 当 n 增大，串行部分留下的加速上限为 $1/(1-p)$。p=0.75 时上限只有 4；p=0.95 时上限为 20。按式代入，32 核时分别约 3.66 和 12.55，后一情形在 128 核时约 17.41。课件的 3.7、17.42 是近似数。这里 p 是基准执行时间中的比例，不应简单数源代码行数；模型也未计入并行新增的通信和同步开销。
 
-锁可能让一个暂停的持有者阻挡所有等待者。非阻塞算法尝试提供不同的进展保证：
+缩短串行部分是性能动机，减少对某个线程的依赖则是另一个问题。假如持锁线程被暂停，等待者即使一直获得 CPU，也可能全部停在获取锁的地方。非阻塞算法讨论的进展保证，就是要说明在不同干扰条件下，哪些操作仍能完成。
 
 | 保证 | 关心谁能完成 | 不能据此推出什么 |
 |---|---|---|
@@ -31,17 +31,23 @@
 | lock-free | 系统持续执行时，总有操作持续完成 | 不能保证某个指定线程不饿死 |
 | obstruction-free | 某个操作从当前状态独自执行足够久时能完成 | 多线程一直冲突时不一定有人完成 |
 
-强弱关系为 wait-free ⇒ lock-free ⇒ obstruction-free。假设一个线程停在持锁状态，另一个线程即使独自运行也拿不到锁，这说明普通阻塞锁为什么不满足最弱一项。反过来，“源代码里没有 mutex”不构成 lock-free 的证明。
+这三个保证依次减弱：wait-free ⇒ lock-free ⇒ obstruction-free。判断它们时，可以先问指定线程能否完成，再问系统中是否总有人完成，最后问一个线程不再受干扰时能否完成。
+
+普通阻塞锁连最后一项也可能做不到：某线程拿锁后停住，另一个线程即使独自执行，也无法替它释放锁。另一方面，删除 mutex 调用本身并不能证明进展，仍要分析重试循环会因什么结束。
 
 <a id="stack"></a>
 
 ## 栈：把头指针当作一次操作的提交点（第 4–11 页）
 
-栈遵守后进先出。每个节点有元素和 `next`；共享 `head` 指向栈顶。带锁版本先完成节点链接，再更新头，整个过程位于临界区。非阻塞版保留同样的状态变化，但用 CAS 检查“我用来计算新状态的旧头还有效吗”。
+从结构最简单的栈开始。共享 `head` 指向栈顶，每个节点保存元素和 `next`，沿 next 可以找到更早压入的节点。
 
-以压入 D 为例：读到旧头 C，构造 `D.next=C`，尝试把头从 C 改成 D。如果期间别的线程已经把头改成 X，CAS 失败，必须重新读取头并重新准备链接。不能在失败后继续使用旧的 `D.next=C`，否则可能把 X 丢掉。
+带锁版本先构造新链接，再更新 head，并把整个过程包在临界区内。非阻塞版本仍做这些事，只是在更新 head 时增加一次 CAS 验证：准备新链接期间，别人是否已经改过头指针。
 
-弹出时读旧头 n，若为空则返回空或报错；否则尝试把头从 n 改成 `n.next`。成功 CAS 是非空弹出的**线性化点**：可以把并发操作看成在这个瞬间按某个顺序发生。
+假设当前栈顶是 C，我们准备压入 D。先令 `D.next=C`，再尝试把头从 C 改为 D。如果另一个线程抢先把 X 放到了栈顶，这次 CAS 就会失败。
+
+下一轮需要重新读取头，并让 D 连向新的栈顶。若仍沿用旧的 `D.next=C`，即使后来成功发布 D，也可能把 X 从链中丢掉。**重试必须重新准备依赖共享状态的那部分结果。**
+
+弹出的过程反过来：读取旧头 n，若为空就返回空或报错；否则尝试把头从 n 改为 `n.next`。成功的 CAS 确定了这次非空弹出在抽象栈中的先后位置，称为**线性化点**。有了它，就可以把交错执行的操作与某个合法的串行栈操作顺序对应起来。
 
 ```text
 教学伪代码：CAS 返回旧值；暂不考虑节点回收
@@ -65,25 +71,31 @@
 
 ![栈的 CAS 与进展说明](Images/03-non-blocking/pdf_p011.png)
 
-若自己的强 CAS 因头变化而失败，说明别的更新已经取得进展。这个论证依赖 CAS 及内存管理等底层步骤满足相应假设。它不保证自己最终成功：别人可能总在自己之前改变头。因此该结构的更新核心是 lock-free，而不是 wait-free。
+这里的重试还有一个有用性质：强 CAS 因头指针发生变化而失败时，别的线程已经成功更新过头。只要相关底层步骤满足假设，持续的竞争就伴随着成功更新，这给出了 lock-free 的论证方向。
+
+不过，成功者不一定轮到自己。一个线程可能每次都比别人晚一步，始终重试，所以这个更新核心没有 wait-free 的个体步数界。分配与回收是否满足所需进展假设，还要单独检查。
 
 <a id="queue"></a>
 
 ## 队列为什么比栈多一个困难（第 12–19 页）
 
-队列先进先出，入队改尾部，出队改头部。带锁版本可以把“原来为空时同时改 head 和 tail”包在临界区内，但单字 CAS 不能一次修改两个独立指针。
+栈的压入和弹出都围绕同一个 head。队列则要求先进先出，入队在尾部接节点，出队从头部取元素。队列原来为空时，一次入队还可能同时影响 head 和 tail。
 
-课件保留一个哨兵节点（fake/dummy），使 `head` 始终指向当前哨兵，`head.next` 才是第一个有效元素。`tail` 只是帮助定位末端的指针，允许滞后。三个关键不变量是：head 的后继是第一个有效元素；从 head 和 tail 出发的链至少相交；最终沿 `next` 到达的是已入队链的末端。
+带锁版本能够保护这组修改，但单字 CAS 只能更新一个位置。非阻塞队列因此需要重新安排结构，让操作先完成一个关键修改，剩下的状态再逐步维护。
+
+课件引入一个哨兵节点（fake/dummy）：`head` 指向哨兵，`head.next` 才是第一个有效元素。这样，空队列也有节点可作为连接起点。`tail` 用来帮助找到末端，允许暂时落后。
+
+读后面的动画时，始终检查三个不变量：head 的后继是第一个有效元素；从 head 与 tail 出发沿链接前进，最终会相交；两条路径的末端都指向最后一个已入队节点。它们允许辅助指针不够新，却要求链的结构始终可用。
 
 ![允许尾指针滞后](Images/03-non-blocking/pdf_p018.png)
 
-**图解：** 第 18 页甚至允许 tail 落后于当前 head。别把这份教学算法的 tail 不变量替换成“tail 永远是最后一个节点”。这也意味着旧节点仍可能用于向后追赶，不能在出队后随意释放。
+第 18 页特意把 tail 画到了当前 head 之前。只要旧节点及后继链接仍有效，入队者就还能沿它们追赶到末端。因此这份教学算法允许这样的滞后，也由此依赖旧节点的生命期：出队后不能立即任意释放它们。
 
 ## 单个入队如何完成（第 20–23 页）
 
 初始时 head 和 tail 都指向同一个哨兵。构造元素 A 的新节点，其 `next` 为空。入队者从 tail 出发，若发现 `next` 非空，就帮助 tail 向后追赶；找到末端后，CAS 把该末端的 `next` 从空变成新节点。
 
-把空的 `next` 改成 A 的那次成功 CAS，才是入队的线性化点。后面尝试将 tail 移到 A 只是维护辅助指针。即使这个 tail CAS 失败，也不需要撤销已经成功的入队，因为其他人可能已经帮忙移动了它。
+**将末端空的 `next` 成功改为 A，就是这次入队的线性化点。** 此时，从队列链上已经能找到 A。随后移动 tail 只是让下一位入队者更快找到末端；即使这次辅助 CAS 失败，也无需撤销入队，因为 tail 可能已由别人推进。
 
 ## 两个入队者如何互相帮助（第 24–36 页）
 
@@ -98,7 +110,7 @@
 
 ![C 帮助 B 推进尾指针](Images/03-non-blocking/pdf_p031.png)
 
-**图解：** 图上的法语旁注指 C 替 B 完成 tail 的推进。这是帮助机制：操作不要求最先发起修改的线程亲自完成所有后续维护。紫色链表示已经连接的节点，单独浮着的新节点尚未入队。
+看图中的两种位置：B 已经接在紫色链上，而 C 仍由自己的局部变量指向，尚未入队。法语旁注说明，C 的执行者正在替 B 推进 tail。借助这种帮助机制，已经完成的链接不必等待原线程恢复，后来者也能补上维护步骤。
 
 ## 出队为什么移动的是哨兵（第 37–40 页）
 
@@ -108,7 +120,9 @@
 
 **图解：** 返回 A 的值，不等于把 A 的节点立即删掉。新的 head 正指向 A，下一次出队从 A 的后继取值。第 37–39 页使用 `value`，前面的结构定义写 `element`，这是原课件字段命名不一致，教程统一称元素值。
 
-进展论证要分别看两类 CAS：连接新节点或推进 head 直接完成一个操作；推进 tail 是帮助，可能只是中间进展。不能把“任何 CAS 失败”一概解释为“别人已经返回一个完整操作”。需要结合有限链追赶和成功更新分析，才能得出总体 lock-free 结论。
+这里要分清抽象操作已经生效与某个函数已经返回。成功连接新节点或推进 head，分别使入队、出队产生抽象效果；推进 tail 则属于帮助维护，未必完成一个新的队列操作。
+
+所以分析失败重试时，需要先确定失败的是哪一次 CAS，再结合有限链的追赶过程判断进展。只说 CAS 失败了，所以别人一定已经返回，并不足以证明这个队列 lock-free。
 
 <a id="list"></a>
 
@@ -116,9 +130,9 @@
 
 考虑 `A → B → C`。线程甲想删除 B，计划把 A 的后继改成 C；线程乙想在 B 后插入 X。如果甲先读了旧后继 C，而乙已经把 X 接到 B 后面，甲随后直接绕过 B，就可能一起丢掉 X。
 
-课件采用 Harris 式思路：先把 B **逻辑删除**，再由任意遍历者将 B **物理摘链**。节点有白/灰两种状态，灰色表示逻辑上已删除。
+Harris 式算法给删除增加一个中间状态：先把 B 标为已删除，使后续操作不再把它当作有效节点，再尝试从链上绕过它。这两步分别叫**逻辑删除**和**物理摘链**。课件用白色表示有效、灰色表示已逻辑删除；后续任何遍历者都可以帮助完成摘链。
 
-标记必须与 B 的 `next` 放在同一个可 CAS 的值中。否则插入者只检查 next 是否没变，无法知道 B 已经被删除。课件用指针最低位作标志：
+插入者需要同时确认 B 尚未删除、B 的后继也没有改变。若颜色和 B 的 `next` 分开存放，只比较 next 的 CAS 就可能漏掉颜色变化。课件因此将两项状态编码在同一个可 CAS 的值中，并用指针最低位记录删除标志：
 
 ```text
 pointer(v) = v 去掉最低位
@@ -129,9 +143,13 @@
 
 ## 遍历与帮助摘链（第 49–59 页）
 
-`pred` 不只是前驱节点，它是“指向当前节点的那个链接字段的地址”。所以开头可等于根指针的地址，往后则变成 `&cur->next`。这个间接层让删除头节点与删除中间节点使用同一种操作。
+读这段代码最容易混淆的是 `pred` 的间接层。它保存的是指向当前节点的**链接字段地址**：当前节点在表头时，这个地址是根指针的位置；走过一个节点后，它成为 `&cur->next`。
 
-遍历到灰色节点 cur 时，CAS 尝试把 `*pred` 从 cur 改成 cur 的真实后继。成功后继续检查同一个 pred 所指的新节点；失败则重新寻找，原因可能是前驱已被标记，或者中间已经插入新节点。这里 `continue` 不应误读为跳过一个后继，否则会漏查。
+因此，修改 pred 所指的内容，就能改变谁连向当前节点。无论删除的是表头还是中间节点，都可以沿用同一套链接更新方法。
+
+遍历遇到灰色节点 cur，就尝试用 CAS 将 `*pred` 从 cur 改为它的真实后继。成功以后，pred 仍指着同一个链接字段，只是这个字段已经指向下一个节点，所以代码在这里 `continue`，重新检查它。
+
+若 CAS 失败，说明这处连接关系已经变化，需要重新寻找。可能是前驱被标记，也可能是两者之间插入了新节点。把成功后的 continue 误读为继续向后移动 pred，会跳过本应检查的节点。
 
 ## 删除 17：操作成功与摘链成功不是一回事（第 60–67 页）
 
@@ -157,11 +175,11 @@
 
 ## 从教学图走到真实程序，还差内存回收（第 76 页及补充）
 
-最后强调进展等级、数据结构不变量和线性化点。学习时应能为每个成功操作指出提交效果发生在哪里，也能说明失败重试是否代表其他线程取得进展。
+三个结构都可以沿同一条思路复查：先画出共享状态和需要保持的不变量，随后指出操作在哪次更新中生效，最后分析失败者继续重试时，系统是否仍取得进展。线性化点帮助判断操作结果，进展等级则描述操作能否完成，二者需要分别论证。
 
 **补充，非原课件完整展开内容：** 逻辑删除、物理摘链、释放内存是三件事。一个节点已经不在链上，另一个线程仍可能持有旧指针。立即 `free` 会造成释放后使用；地址被回收再复用，还可能使 CAS 看到同一地址值而误以为状态没变，这就是常见的 ABA 风险。仅给指针加版本号，并不能单独解决旧指针解引用的安全性。
 
-因此这里的论证先假设节点在使用期间有效，并且不会发生破坏假设的地址复用。真实系统需要垃圾回收、hazard pointers、epoch 等适当机制，并检查内存分配及回收是否改变整体进展保证。这里点明缺口，不假装已经实现完整回收算法。[Michael 的 hazard pointers 论文](https://research.ibm.com/publications/hazard-pointers-safe-memory-reclamation-for-lock-free-objects)
+前面的算法分析都依赖同一前提：节点在使用期间有效，地址也不会以破坏算法假设的方式被复用。真实实现要用垃圾回收、hazard pointers 或 epoch 等机制落实这一点，并重新检查分配和回收对进展保证的影响。安全回收算法超出本讲展开范围；这里明确其必要性，供进入实验时继续核对。[Michael 的 hazard pointers 论文](https://research.ibm.com/publications/hazard-pointers-safe-memory-reclamation-for-lock-free-objects)
 
 对应实验：[非阻塞算法](../Labs/tp-non-blocking/tp-non-blocking.html)。开始写代码前，先画出读指针、验证指针、使用节点与释放节点的关系。
 
--- before/04-stm_zh.md
+++ after/04-stm_zh.md
@@ -12,11 +12,15 @@
 
 ## 从一把大锁到事务接口（第 1–4 页）
 
-大锁便于理解，却把许多本可独立运行的操作串行化。细粒度锁要求维护锁顺序、对象之间的不变量以及失败清理；非阻塞结构也有复杂的重试和生命周期要求。尤其是把两个已经正确的对象操作组合起来时，它们未必仍构成一个不可分割的操作。
-
-事务内存提供一个高层 `atomic { ... }`：对受事务协议管理的观察者，该块的效果应像整体执行。它是编程模型语法，不是标准 C 的 `_Atomic` 变量修饰符，也不是一条硬件原子指令。
-
-课件的例子是一边把 x 加 1，另一边乘 2。假设 x 初值为 21，串行顺序有两种：先加后乘得到 44，先乘后加得到 43。第 3 页并未写出初值 21，这是由结果反推并在此明确补充的教学前提。图中序列 `[b,c,e]` 把右花括号 e 放进操作列表，实际写回是 d；应按读、计算、写回完整理解。
+前两讲已经展示了两种困难。用一把大锁保护完整操作，容易理解，却会让本可独立进行的工作排队；改用细粒度锁或非阻塞结构，又要维护锁顺序、不变量、重试和对象生命期。
+
+当程序想把两个各自正确的对象操作组合起来时，困难尤其明显：单独调用都正确，不代表组合过程对别人也是不可分割的。事务内存从这里出发，提供一组读写整体生效的接口。
+
+这个接口写成 `atomic { ... }`。对于受事务协议管理的观察者，块中的修改应表现为整体执行。它描述的是一段代码的事务语义；标准 C 的 `_Atomic` 修饰单个对象，硬件原子指令则提供更低层的操作，三者的作用范围不同。
+
+第 3 页让两个事务分别执行 x 加 1 与 x 乘 2。为对应图中的结果，先假设 x 初值为 21：加完再乘得到 44，乘完再加得到 43。两种结果都对应完整事务先后执行。读者应能依据这种串行顺序解释最终状态，而不能读到只完成了一部分的事务效果。
+
+这里的初值 21 是由结果反推后补出的前提，原页没有显式写出。原图操作序列 `[b,c,e]` 也有标签问题：e 是右花括号，实际写回位于 d，应按读、计算、写回来理解。
 
 把临界区改写成 atomic 可以隐藏锁的选择，但不是机械删除所有锁就能正确。事务外访问、I/O、不可重试操作及与其他同步机制的交互都需要单独定义。
 
@@ -24,34 +28,44 @@
 
 ## retry 等的是读过的状态发生变化（第 5–9 页）
 
-条件变量版本要显式写“持锁检查—等待—被通知—再检查”。课件采用的 STM 接口允许事务发现条件不满足时 `retry`：撤销本次试执行，等待本次读取集合中的相关状态变化，再重新运行。
-
-这里 `retry` 不是在原地不断读的忙等。运行时需要记住读集合，并正确衔接登记等待与状态更新，以免丢失唤醒。
-
-例子是把元素从 src 队列移到 dst 队列。如果先独立提交 `src.deq()`，再发现 dst 已满而等待，元素就已从源队列消失。把两者放在同一个外层事务中，任何内部 retry 都回到整个 move 的起点：没有出现“只完成一半”的已提交状态。
+线程与同步一讲中的条件等待，需要程序显式安排持锁检查、等待和再次检查。在课件采用的 STM 接口中，事务发现条件不满足时可以执行 `retry`。运行时放弃这次试执行，等它读过的相关状态发生变化后，再重新运行事务。
+
+运行时之所以知道等什么，是因为它记录了**读集合**。例如事务检查队列是否为空，就对队列状态建立了读取依赖。登记等待必须与更新通知正确衔接，否则也会像条件变量使用不当一样丢失唤醒。这样的 retry 是基于状态变化的等待，避免了在原地反复读取的忙等。
+
+再看组合操作：把元素从 src 队列移到 dst 队列。若先单独提交 `src.deq()`，随后才发现 dst 已满，源队列的变化已经公开，而目标队列还没有接收元素。
+
+把出队和入队包进同一个外层事务，就能共同决定它们是否生效。内部任何一步触发 retry，整个 move 都从外层起点重来，两边不会留下只提交了一半的状态。
 
 ![嵌套事务的 retry](Images/04-stm/pdf_p008.png)
 
 **图解：** 第 6 页把两个操作包入外层 atomic，第 8 页把重启箭头画到外层起点。这种扁平式嵌套语义是本课件的约定，不等于所有事务系统都支持相同的嵌套方式。
 
-第 7 页的队列片段有两个原文问题：`addLast()` 未传入元素 e；`addLast` 配 `removeLast` 表达后进先出，而非通常的 FIFO 队列。如果教学目标是队列，应明确使用尾插头取。不能把这些不一致归因于 OCR，也不在原文中偷偷更换接口。
-
-`orElse` 提供候选方案：先尝试左分支，若它 retry，再尝试右分支。例如“立即出队，若当前不能出队就返回空”。右分支并不是捕获任意异常；若两个分支都 retry，运行时还需依据相关读依赖等待后重新尝试。
+第 7 页的队列伪代码需留意两个原文问题：`addLast()` 缺少元素 e；`addLast` 与 `removeLast` 配对表现为后进先出。若按 FIFO 队列理解，应采用尾插头取。这些差异已在原文校对中保留，不影响此处借队列说明 retry 和组合，但不能照抄成完整队列实现。
+
+`orElse` 让事务有另一个候选动作。先尝试左分支；若它需要 retry，就尝试右分支。课件用立即出队说明：有元素便取走，当前不能出队则返回空。
+
+只有分支的 retry 触发这种选择，普通异常不能都按同样方式处理。若两个候选分支都 retry，运行时仍要根据相应的读取依赖等待，随后重新尝试。
 
 <a id="logging"></a>
 
 ## 冲突的本质：能否解释为事务整体执行（第 10–12 页）
 
-最直接的实现是全局锁：事务开始获取，结束释放。它实现串行执行，但限制并行。乐观实现允许先执行，发现与其他事务冲突后撤销或重试。
-
-如果事务 X 连续向 A 写 b、c，而 Y 读到只存在于 X 中间的 b，Y 看到了本不应暴露的内部状态。另一个例子是 Y 在同一事务里两次读 A，读到 X 提交前后的两个值；若没有特殊语义允许这种行为，就不能把 Y 看成一个瞬间执行。
+要让 atomic 块整体生效，最直接的方法仍是一把全局锁：开始时获取，结束时释放。代价是事务彼此串行。
+
+乐观实现尝试让事务先并发执行，到需要验证时判断能否接受结果。若这次执行无法与其他事务共同解释为合法的整体操作，就撤销或重试。下面的冲突例子说明验证要排除什么。
+
+设事务 X 在内部先向 A 写 b，再写 c。对外只能观察到 X 整体执行的效果，另一个事务 Y 若读到了仅在中途存在的 b，就看到了不应公开的状态。
+
+另一种问题发生在读者自身：Y 先后两次读取 A，中间 X 提交了修改，Y 因而取得两个版本。若系统没有专门允许这种语义，就无法把 Y 的读取解释成一个整体观察。两种情形分别促使我们考虑写入如何暂存、读到的状态如何验证。
 
 | 方案 | 试执行时写哪里 | 成功时 | 失败时 |
 |---|---|---|---|
 | 延迟更新（redo log） | 私有写日志 | 验证后将日志发布到共享内存 | 丢弃尚未发布的日志 |
 | 立即更新（undo log） | 共享内存，另存旧值 | 在合适保护下确认提交 | 按旧值恢复 |
 
-redo 避免在正常试执行期间公开中间写值，但提交时批量写回也必须有同步协议，不能把日志逐项随意写出。undo 需要在他人看到或依赖中间值之前进行冲突管理。两者都有各自的日志、验证和恢复成本，不能仅凭“提交多”或“失败多”就断言性能赢家。
+redo 先把修改放在私有日志，其他事务在试执行期间不会直接读到这些中间值。提交时仍需安全地发布整组修改，不能把日志中的位置逐个随意写出。undo 则已经动到了共享状态，必须配合冲突管理，避免别人在恢复前依赖了这些中间值。
+
+两者把工作放在不同阶段：日志记录、提交验证和失败恢复的成本都要计算。事务多数成功或多数失败，可以帮助分析代价，但不足以单独决定哪种方案更快。
 
 ## 两种日志的图中过程（第 13–14 页）
 
@@ -61,13 +75,17 @@
 
 ## 什么时候检测冲突（第 15–16 页）
 
-eager 在访问期间尽早发现冲突，通常需要对相关读写插桩；lazy 把更多检查推迟到提交阶段，但这可能让本应失败的事务继续用无效数据计算。
+检测时机是日志方案之外的另一项选择。eager 在访问期间尽早检查冲突，通常需要在相关读写处插入检查代码；lazy 将更多验证推迟到提交阶段。
+
+推迟检查可以减少某些执行中的工作，却留下一个问题：事务在到达提交点以前，可能已经根据无效的读值继续计算。后面的僵尸事务例子会具体展示这个风险。
 
 HTM 借助硬件维护读写集合及推测状态；STM 在软件中记录与验证；HyTM 尝试结合两者。课件对“HTM 常用 lazy”“只能放进 L1”“STM 可以任意大小”的说法是简化：检测时机、容量与回退机制依具体实现，软件也受内存和其他资源限制。这里不把历史概括写成今天所有处理器的统一规格。
 
 ## 第一版 STM：每个位置配一个版本（第 17–22 页）
 
-给共享位置配值与计数器。事务读时记录位置及版本，写时记录私有新值。提交时取得提交锁，检查读集合版本未变，成功则写回并更新版本。
+第一版 STM 给每个共享位置增加一个计数器，用它辨认数据有没有被修改。事务读取时记住位置和版本，写入时只记录自己的新值。到提交时取得提交锁，再检查此前读取的版本是否还有效；检查通过，才发布写入并更新版本。
+
+读集合用于记录计算依据，写集合用于记录准备公开的修改。两者可能重叠，却不必相同。
 
 课件例子初始值为 a=10、b=21、c=7、d=83、e=8。事务执行：
 
@@ -77,7 +95,9 @@
 b = c
 ```
 
-第一次算出 a=31，只记在写日志里；第二句必须读到自己刚写的 a=31，因此得到 c=23；最后 b=23。d 完全未使用，e 只读。
+按代码顺序跟踪本地状态。第一句得到 a=31，将它写进自己的日志；第二句使用的也应该是这个新 a，因此算出 c=23；第三句再把 b 记为 23。d 没有参与计算，e 只被读取。
+
+这个小例子说明，即使共享内存尚未变化，事务也必须能看到自己的写入，否则第二句就会错误地使用旧 a。
 
 ![本地日志与提交后的内存](Images/04-stm/pdf_p022.png)
 
@@ -87,21 +107,29 @@
 
 ## 只在最后检查，为什么可能来不及（第 23–35 页）
 
-第一个例子先检查 `x != null`，然后调用 `x.f()`。另一事务在中间提交 `x=null`。如果后续再次读取共享 x 而没有及时验证，调用时就可能拿到空引用。第 28 页的“does not read x again”应结合第 29 页理解为没有再次检查相应版本，不能据此说语法上的 `x.f()` 不需要求得接收对象。
-
-第二个例子更关键。全局不变量是 x≠y，原状态 `(4,5)`，另一个事务整体提交 `(217,4)`，两个状态都满足不变量。但读者先取得旧 x=4，之后取得新 y=4，就拼出从未作为有效整体存在的 `(4,4)`，计算 `1/(t1-t2)` 时可能出错。
+仅在提交时验证，未必来得及保护事务自身。第一个例子先检查 `x != null`，准备调用 `x.f()`；另一事务在中间提交了 `x=null`。如果调用时重新使用共享 x，却没有及时验证它是否改变，就可能拿到空引用。
+
+第 28 页的“does not read x again”要与第 29 页结合理解，指的是缺少再次版本检查；语法上的 `x.f()` 仍然需要取得接收对象。
+
+第二个例子说明，只保护重复读取同一变量还不够。设已提交状态总满足 x≠y，初始为 `(4,5)`，写者随后整体改为 `(217,4)`，修改前后都满足这个条件。
+
+但另一个事务可能先读到旧 x=4，随后读到新 y=4。它组合出了 `(4,4)`，接着计算 `1/(t1-t2)`，就可能在提交验证之前出错。问题出在把不同状态中的值拼在了一起。
 
 ![不同版本拼出的无效状态](Images/04-stm/pdf_p035.png)
 
 **图解：** 左侧保留先读到的 t1=4，右侧提交后把 y 变成 4，接下来读者只检查新 y 自己的版本仍可能接受它。问题不是每次读取是否原子，而是多次读取能否属于一致的状态。
 
-这种“已经注定不能成功，却仍在执行”的事务常被称为 zombie transaction。最终不提交并不能消除执行途中已经发生的异常、越界或不可撤销行为。课件接下来通过事务开始时的版本边界避免接受未来提交的状态。
+这种已经失去有效读取依据、却还在继续执行的事务，被称为 zombie transaction。最后拒绝提交，可以防止公开错误结果，却无法撤回此前已经发生的异常、越界或不可撤销操作。
+
+因此下一版算法需要更早判断：当前取得的版本，是否还属于本次事务可以接受的状态。课件用开始时保存的全局时钟来建立这个边界。
 
 <a id="clock"></a>
 
 ## 全局时钟定义这次事务能够读取什么（第 36–45 页）
 
-课件约定全局时钟 G 表示下一次提交可使用的版本。事务开始时保存 S=G。已经完成的旧提交版本小于 S；若某个位置的版本大于或等于 S，则它在事务开始之后被提交，不能读作本次快照的一部分。
+课件用 G 表示下一次提交可使用的版本号。事务开始时保存 S=G，把它作为本次读取的版本边界。此前已完成的提交版本小于 S；读到大于或等于 S 的版本，说明遇到了不能纳入本次快照的新修改，需要中止。
+
+除了检查这个边界，还要处理读到自己写入的情况，并在提交时复查读集合。整个流程如下：
 
 ```text
 开始：S = G，清空读集合与写集合
@@ -129,7 +157,9 @@
 
 ## 用不可变的值—版本对避免读到拼接状态（第 46–51 页）
 
-课件把每个共享位置做成指向不可变 `(value,counter)` 对象的指针。提交者创建新对象并原子替换指针，永远不原地分别改旧对象的两个字段。读者于是拿到匹配的一对值与版本，而不是新值加旧版本。
+版本检查本身也依赖一个前提：读者拿到的值和版本必须相互对应。如果把两个字段分别原地修改，读者可能在中间取得新值与旧版本，进而作出错误判断。
+
+课件把它们放进不可变的 `(value,counter)` 对象。提交者准备好新对象，再原子替换指针；旧对象的字段不再修改，读者便可以拿到匹配的一对值与版本。
 
 事务状态包括读集合、写集合和开始时钟。`read` 优先返回本事务写集合中的值，确保能读到自己的写入；否则读取共享值对并验证。`commit` 在一把全局提交锁下复查读集合、发布新对象，最后增加全局时钟。
 
@@ -141,11 +171,15 @@
 
 ## 退避、外部副作用与性能（第 52–54 页）
 
-失败的事务若总在同一时刻重试，可能反复冲突。随机指数退避让重试时间错开，但不会凭空提供每个线程都能成功的保证。
-
-**第 52 页勘误：** 原例在 `doTransaction()` 内重新设 `n=16`，失败后递归调用该函数。因此新一轮又从 16 开始，无法按所写代码持续扩大退避窗口，还会增长调用栈。正确表达该意图应把 n 放在外层循环的持续状态中，成功或放弃时退出，而不是每次递归重新初始化。这里只说明修正方向，不把它伪装成已验证运行库。
-
-外部 I/O 不一定能撤销。事务执行中发送了请求、写了日志文件，之后内存事务 abort，并不会使外部世界回到过去。课件用一个夸张的函数名说明这点；不可撤销模式需要在操作前协调提交能力，或者把外部动作放到提交后的明确协议中。
+事务失败之后还要决定何时重试。两个竞争者若总同时开始，可能反复发生相似冲突。随机指数退避将重试时间错开，并在连续失败时扩大等待范围，以降低持续冲突的概率。它是调节竞争的策略，并没有提供每个线程都能成功的保证。
+
+**第 52 页勘误：** 原例在 `doTransaction()` 里设置 `n=16`，失败后递归调用自己。新的调用又把 n 设回 16，所以窗口不能按代码的意图持续增大，调用栈还会增长。
+
+要保留退避状态，应让 n 在外层重试循环中持续存在，成功或决定放弃时再退出。这里说明的是原例需要怎样调整，并未将它实现为经过验证的运行库。
+
+日志能恢复受管理的内存，外部动作却可能已经发生。例如事务发送了请求或写了日志文件，之后再 abort，对方收到的请求和文件中的内容不会因此自动消失。
+
+课件用一个夸张的函数名强调这点。若要在事务中执行不可撤销动作，就需要在动作前协调提交能力；也可以把外部动作放在提交之后，并另行设计两者的衔接协议。
 
 第 54 页写“无死锁、无饥饿”，但这是过强的概括：运行时仍可能使用锁，冲突事务也可能持续重试，程序还可能等待永远不会成立的业务条件。接口减少了手工锁顺序负担，并不自动证明全部进展性质。
 
--- before/05-numa_zh.md
+++ after/05-numa_zh.md
@@ -12,21 +12,27 @@
 
 ## 核数增加后，问题转移到了哪里（第 1–11 页）
 
-大数据分析、大计算和大量客户端都需要计算能力。早期可以期待更高频率与单线程性能，但频率和功耗存在约束，更多晶体管也逐渐用于更多核。第 4–7 页是历史趋势图：晶体管数量、频率、单线程性能、功耗与核数是不同曲线，不能把它们都当作程序吞吐量。
+前几讲主要讨论线程怎样协调共享状态。这一讲继续追问：同步正确以后，为什么增加线程仍可能提不了速？大数据分析、大计算和大量客户端都需要更多算力，而更高频率受到功耗等条件约束，处理器逐渐把更多晶体管用于增加核数。
+
+第 4–7 页展示这一历史变化。读图时要分别看晶体管数量、频率、单线程性能、功耗与核数，每条曲线描述的量都不同；只有结合程序的工作方式，才能讨论它们对吞吐量的影响。
 
 第 8 页展示同为 2.2GHz 的双核笔记本和 48 核服务器，示例计时反而是 0.474 秒与 1.142 秒。它要提出的问题是“昂贵的多核机器为什么不会自动加速这个程序”。
 
 **代码审查：** 原图只写了单线程循环，并且 `malloc` 后未初始化数组就读 `a[i]`，结果也没有被外部消费；还可能涉及有符号整数溢出及优化器消去。因而这段代码不是可直接复现实验结论的严格基准，不能仅据它将时间差归因于 NUMA。合理实验应控制初始化、优化、输出依赖、线程绑定和访问模式，再测量。
 
-第 9–11 页逐层增加要求：不仅要并行化，还要让算法随核数扩展，并正确处理复杂内存架构。
+因此第 9–11 页逐步把问题推进到内存：程序首先要有可并行的工作，其次要能随核数扩展，最后还要让内存访问支持这种扩展。接下来先从硬件上看，一次访问的时间为什么会随位置变化。
 
 ## 同样读取内存，为什么能相差几十倍（第 12–20 页）
 
 ![访问核与分配核的延迟矩阵](Images/05-numa/pdf_p012.png)
 
-**图解：** 两个横向轴分别是访问数据的核与分配数据的核；高度和颜色表示周期数。近对角线的低谷对应较近或缓存中的数据，不同区域的平台反映拓扑差异。课件的 48 核、4 插槽 AMD、128GB 实验中，例子给出约 5、50、275、380 cycles，不能将 5 cycles 解释成一次完整 DRAM 访问。
-
-第 13 页另列缓存层次，L1、L2、L3 示例约为 5、15、50 cycles。未命中缓存后才进一步涉及本地或远端内存控制器。NUMA 的“非均匀”表示同一共享地址空间中的内存位置不同，访问代价可能不同，不是说远端地址不可访问。
+先固定访问核，再沿另一个方向看分配核的位置，就能读懂这张延迟矩阵。两个横向轴分别标出访问数据和分配数据的核，高度与颜色表示周期数；近对角线的低谷对应较近或缓存中的数据，其他区域的平台反映拓扑差异。
+
+这组实验使用 48 核、4 插槽、128GB 的 AMD 平台，页面给出的示例延迟约为 5、50、275、380 cycles。它们横跨缓存与远端访问情形，不能把 5 cycles 当作一次完整 DRAM 访问的成本。
+
+第 13 页将缓存层次单独画出，L1、L2、L3 的示例代价约为 5、15、50 cycles。若数据仍在近处的缓存里，程序未必需要访问内存；缓存未命中后，本地与远端内存控制器的位置才进一步影响代价。
+
+NUMA 的非均匀性就体现在这里：进程使用同一个共享地址空间，仍能访问远端地址，但访问不同物理位置的成本可能不同。
 
 第 15–16 页用四插槽主板说明复杂连接，标出 4×16 核及 128 个硬件线程。核与硬件线程不能等量视为独立计算能力。图中总内存写 256GB，局部箭头写 16×8GB；课件未充分解释该标注覆盖范围，所以不据此重新计算并断言完整装机容量。
 
@@ -34,17 +40,21 @@
 
 ![节点与跨节点路径](Images/05-numa/pdf_p018.png)
 
-第 19 页进一步让所有核访问同一个节点上的不同缓存行，示例延迟升到约 870 cycles。没有写同一缓存行，也会因内存控制器或互联拥塞变慢。因此优化要同时考虑局部性、内存节点负载和互联负载，不能只最小化远程访问次数。
+距离也不是访问成本的唯一来源。第 19 页让所有核访问同一节点上的不同缓存行，示例延迟升到约 870 cycles。这些访问没有写同一行，却集中使用了同一内存节点及相关路径。
+
+因此，放置数据时要同时看**访问是否本地、节点负载是否均衡、互联是否拥塞**。将每个线程的数据放近，可能改善局部性；若大量请求仍集中到同一处，还要考虑分散流量。
 
 <a id="placement"></a>
 
 ## 页表是内存放置的连接点（第 21–25 页）
 
-物理地址对应具体内存节点，进程使用虚拟地址，页表把虚拟页映射到物理页。因此相同的程序地址可以由不同 NUMA 节点上的物理页支撑；不必为了改变放置而修改每个 C 指针。
+程序使用的是虚拟地址，NUMA 节点承载的则是实际物理页。页表连接了两者：一个虚拟页可以映射到某个节点上的物理页，内存放置策略便通过选择这些物理页发挥作用。
+
+这样，应用可以继续使用原有的虚拟地址，系统通过页映射安排数据的位置，而不必让程序逐个修改 C 指针。
 
 第 23 页先用 `mmap` 保留虚拟地址区间，第 24 页用简化的 `mbind` 表达为范围选择节点。**课件中的 `mbind(x, sizeof(*x), 2)` 不是完整 Linux 接口签名**；实际参数还包括策略、节点掩码等。页策略影响哪些分配，既有页是否迁移，以及不同文件映射的行为，需依具体接口与映射类型判断。[Linux NUMA 内存策略](https://cdn.kernel.org/doc/html/latest/admin-guide/mm/numa_memory_policy.html)
 
-线程亲和性决定“谁在哪个核运行”，内存策略决定“数据在哪个节点”，两者是不同控制面。只设置一个，不能保证访问本地化。
+线程亲和性安排线程在哪个核运行，内存策略安排数据由哪些节点承载，这两部分必须配合。例如主线程先初始化了共享数组，随后再把工作线程绑定到其他节点，并不能据此保证已有数组也随之搬到了它们附近。是否分配、是否迁移，要结合具体策略判断。
 
 ## 缓存命中率如何放大或掩盖 NUMA 影响（第 26–27 页）
 
@@ -52,19 +62,23 @@
 
 **校对与解释：** 三层都写“70% accesses”，若将它们当作互斥的总体比例，相加达到 210%，显然不成立。合理的重建方式是把它理解为“到达每一级时的条件命中率”。这个条件含义没有在原页写全，因此这里明确作为解释性假设。
 
-令每级条件命中率为 h，则分别在 L1、L2、L3 结束的概率是 h、(1−h)h、(1−h)²h，落到内存的比例为 (1−h)³。用各级总访问代价的简化加权模型：
+按这个条件概率解释，把每级到达后的命中率记为 h。访问在 L1 结束的概率为 h；先错过 L1、再命中 L2 的概率为 (1−h)h；直到 L3 才命中的概率为 (1−h)²h。只有三级都未命中，才落到内存，这部分为 (1−h)³。
+
+下面的 L₁、L₂、L₃、L_M 分别表示在各层取得数据的简化总访问代价。用相应概率加权，就得到平均代价模型：
 
 $$
 E[L]=hL_1+(1-h)hL_2+(1-h)^2hL_3+(1-h)^3L_M.
 $$
 
-h=0.3 时，有 34.3% 访问落到内存；h=0.7 时只有 2.7%。因此把内存代价从本地变成拥塞远程访问，会对前一种情况产生更大影响。
-
-这不是精确的现代 CPU CPI 模型：它省略内存级并行、预取、乱序隐藏延迟等因素。直接用显示的取整值复算额外开销，会得到 387.5% 和约 142.9%，而非页面上的 385% 和 137%；不应伪造未提供的精确参数来声称完全复现。
+将 h=0.3 与 h=0.7 代入，最终落到内存的访问分别占 34.3% 和 2.7%。这解释了同一机器上的不同应用为什么对 NUMA 有不同反应：大量访问已经在缓存结束时，内存位置变化影响的只是较小一部分；频繁访问内存时，本地与拥塞远端之间的差距就更容易体现在程序时间里。
+
+这个模型帮助理解影响方向，距离精确预测现代 CPU 的 CPI 还有一步。内存级并行、预取以及乱序执行隐藏延迟等因素都被省略了。
+
+数值上也应保留原页的近似性：按显示的取整值计算，额外开销为 387.5% 和约 142.9%，与课件的 385% 和 137% 并不完全吻合。缺少更精细的参数时，只能说明近似结果和差异，不能声称已精确复现。
 
 ## 四类放置思路（第 28–40 页）
 
-第一组研究比较来自五组基准的 29 个应用，目标是了解 Linux 策略与应用反应。课件实验限定 4KiB 页，不能自动推广到大页。
+理论模型说明了内存位置可能重要，接下来需要在实际应用中比较策略。第一组研究选取五组基准中的 29 个应用，考察 Linux 的放置方式与应用访问行为怎样配合。实验使用 4KiB 页，以下结果也应放在这一页大小条件下理解。
 
 | 方法 | 怎么放 | 有利情形 | 代价或局限 |
 |---|---|---|---|
@@ -75,7 +89,9 @@
 
 第 31 页的 `setaffinity` 等也是概念写法。读代码时应区分 Linux `sched_setaffinity`、线程亲和性接口和 NUMA 节点掩码，不把核集合当成节点集合。
 
-第 34–37 页逐步画 first-touch：线程在节点 1 运行，访问尚未建立实际物理页的虚拟页，内核选取物理页并建立映射。教学实验通常通过并行写入初始化来触发私有页分配；单纯 `malloc` 或首次读取不总是等价于真正分配独立物理页。本地内存不足、显式策略、线程迁移等也会改变结果。[Linux NUMA 概念](https://docs.kernel.org/mm/numa.html)
+第 34–37 页把 first-touch 画成一个分配过程：节点 1 上的线程访问某个尚未获得实际物理页的虚拟页，内核选页并建立映射。若后续使用者与初始化者保持相同的分工，这种方式就可能得到较好的局部性。
+
+教学实验通常让各工作线程写入自己将使用的区域，以触发私有页分配。单纯 `malloc`，或第一次读取，并不总等价于已经分配独立物理页。显式策略、本地内存不足和线程迁移等因素，也都可能改变最后的位置。[Linux NUMA 概念](https://docs.kernel.org/mm/numa.html)
 
 第 38–39 页的 Carrefour 同时关心节点拥塞与互联拥塞：前者可能需要分散，后者可能需要把页面移近使用者。课件把 Dashti 等的工作标作 ASPLOS’15；对应 Carrefour 原论文发表于 ASPLOS 2013，这是文献年份勘误。[作者提供的原论文](https://people.ece.ubc.ca/sasha/papers/asplos284-dashti.pdf)
 
@@ -83,19 +99,23 @@
 
 ![四种策略相对 first-touch 的加速比](Images/05-numa/pdf_p041.png)
 
-**图解：** 每组是一个应用，纵轴是相对 Linux FT 的加速比，1 表示与基准相当；不同灰度是 FT、FT/Carrefour、4K interleave、4K/Carrefour。图中不同箭头分别指出四种方案胜出的应用，支持“需要根据访问行为选择”的结论。不要从低分辨率小字猜出每根柱的精确值。
+比较柱形图时，先以纵轴的 1 为参照：这是与 Linux FT 性能相当的位置。每组代表一个应用，四种灰度分别对应 FT、FT/Carrefour、4K interleave、4K/Carrefour，柱高表示相对 FT 的加速比。
+
+图中的箭头分别指出四种方案胜出的情形，所以这组实验给出的主要结论是，选择要取决于应用的访问行为。原图的小字分辨率有限，适合比较趋势，不宜从中估计每根柱的精确数值。
 
 <a id="prediction"></a>
 
 ## 用节点访问不均衡预测策略（第 43–52 页）
 
-第二组研究先在 first-touch 下测各节点的访问量，再用相对标准差描述不均衡。若八个节点访问量相等，指标为 0；若访问集中在一个节点，指标显著增加。
+逐个运行所有策略再选最好的一种，需要付出测量成本。第二组研究尝试先观察 first-touch 下的访问分布，再据此预测合适策略。
+
+它使用各节点访问量的相对标准差描述不均衡。八个节点访问量相等时，指标为 0；访问越集中，指标越大。这里统计的是访问发生了多少次，数据分配得均匀并不保证流量也均匀。
 
 ![不均衡程度与使用节点数](Images/05-numa/pdf_p044.png)
 
 **图解：** 上行是 0%、40%、62%、83%、107%、138%、185%、283%，下行是 8 到 1 个节点。该表按“被使用的节点平均分担访问，其余为 0”理解，并使用**样本标准差**，可以复现数字。若用总体标准差，单节点集中会得到约 264.6%，不是 283%。原文只写相对标准差，分母约定是这里根据数值核对得到的说明。
 
-设八个节点访问量为 $a_i$，均值 $\bar a>0$，则该表符合：
+为看清这个指标，设八个节点的访问量为 $a_i$，均值为 $\bar a>0$。先计算各节点偏离均值的程度，再用均值归一化，得到与原表数值相符的相对标准差：
 
 $$
 \mathrm{imbalance}=\frac{\sqrt{\frac1{7}\sum_{i=1}^{8}(a_i-\bar a)^2}}{\bar a}.
@@ -113,30 +133,34 @@
 
 第三组研究使用 Spark 在 friendster 图上运行 PageRank，堆为 40GB，采用 Parallel Scavenge（PS）垃圾回收器。虽然用户写的是较高层的数据分析程序，运行时仍在分配、扫描和搬移大量对象。
 
-第 54 页横轴是核数/线程数，纵轴是以完成时间衡量的加速比，实际曲线在约 12 核后明显变平。第 55 页拆分总时间：应用计算时间下降，而 GC 时间下降不够。第 56 页把 GC 单独画成 GB collected per second，同样出现扩展瓶颈。
+先看整体应用：第 54 页横轴是核数/线程数，纵轴是由完成时间计算的加速比，曲线在约 12 核后明显变平。单凭这条曲线还不知道瓶颈在哪里，所以第 55 页将时间拆成应用计算与 GC 两部分。
+
+计算部分继续缩短，GC 时间却下降得不够快。第 56 页再单独查看 GC 的 GB collected per second，也看到吞吐难以随核数增长。这样，后续优化才有了较明确的对象。
 
 ![区分应用计算与 GC 时间](Images/05-numa/pdf_p055.png)
 
 **图解：** 上部实色区域是应用时间，下部网纹区域是 GC 时间，纵轴为秒。不能把它误读成“核数越多，GC 收集了越多垃圾”；这里比较的是同一工作负载的耗时构成。
 
-第 57–58 页先移除不必要同步、优化锁与工作窃取队列，性能提高却仍未解决扩展性。这说明同步与内存位置是不同的瓶颈，优化了锁，不保证 NUMA 问题已经消失。
+第 57–58 页先从同步入手：移除不必要的同步，优化锁和工作窃取队列。吞吐有所提高，扩展性问题却仍在。这一步说明优化同步已经有效，也说明还需要继续寻找它之外的限制，接下来就转向 GC 的跨节点访问。
 
 ## NUMAGiC：先把工作送到数据附近，再处理负载不均（第 59–74 页）
 
-第 59–61 页让节点 0 的 GC 线程遍历对象图，遇到节点 1 的对象便反复远程访问。第 62–68 页改为按节点组织 GC：本地线程发现远端对象时，把处理任务发送给远端工作者，由它在自己的节点上继续访问。
+GC 会沿对象之间的引用继续寻找要处理的数据。第 59–61 页让节点 0 的线程沿对象图走到节点 1，随后的遍历便产生远程访问。
+
+第 62–68 页尝试调整工作的执行位置：本地线程发现远端对象后，将处理任务交给那个节点的工作者，让后续访问在数据附近完成。任务跨节点传递，对象处理则尽量留在本地。
 
 ![把远程访问改成任务消息](Images/05-numa/pdf_p063.png)
 
-**图解：** 节点内对象访问留在本地，跨节点边转化为消息。但消息有入队、通知或批量交换等成本，不能假定比单次远程 load 更便宜。
+图中跨节点引用对应的工作通过消息交给对方，节点内的遍历继续由本地线程完成。消息仍要付出入队、通知或批量交换的成本。如果每次远程引用都生成一条昂贵消息，节省的 load 开销可能抵不过传递任务的开销，这正是后面几页要处理的问题。
 
 第 69–72 页指出直接转换会产生过多消息。实验观察到，Spark 中不同分配线程产生的对象之间的引用约占 1%，因此提出让对象保持在分配节点的启发式，减少跨节点引用。这是该负载的观察，不能写成所有 Java 程序的普遍比例。
 
-对象聚集又带来另一个问题：某节点还有大量对象要扫描，另一节点却空闲。第 73–74 页采用自适应工作方式：
+减少跨节点引用后，还要检查工作是否均衡。对象集中在某个节点时，它的 GC 线程可能仍在忙，另一个节点却已无事可做。第 73–74 页因而在两种工作方式间自适应切换：
 
 - 有本地工作时使用 local 模式，把远端工作发给对应节点。
 - 本地空闲时使用 thief 模式，窃取并处理远端对象，以远程访问换取更好的并行利用率。
 
-这体现了贯穿这一讲的取舍：尽可能本地化，但不为了零远程访问而让大量核空闲。
+两种模式配合，让线程在有本地任务时保留局部性，在本地无事可做时接受远程访问成本。优化目标是缩短整个收集过程；把远程访问压到最低，却让许多核空闲，也未必更快。
 
 ## 吞吐提升如何传导到应用完成时间（第 75–77 页）
 
@@ -158,13 +182,17 @@
 
 **图解：** 两张图的纵轴不同，上方接近 10 的柱是归一化开销，不是 10 cycles；下方才是 cycles per access。图示与研究诊断相符，但单看一张相关图不是排除所有其他原因的独立因果实验。
 
-客户机虚拟地址到客户机物理页，再到宿主机实际物理页，中间任何一层不了解 NUMA，都可能破坏应用原本的放置意图。XenNUMA 把通用策略引入 Xen，并增加 Linux 与 Xen 的接口，让管理层知道某进程有哪些页、由哪里触发访问。
+虚拟化使放置关系多了一层：客户机虚拟地址先映射到客户机物理页，后者又要由宿主机实际物理页承载。客户机以为数据位于某处，并不自动意味着宿主机也按这个意图安排了它。
+
+XenNUMA 将通用 NUMA 策略引入 Xen，并增加 Linux 与 Xen 之间的接口，使管理层能够识别一个进程使用的页面及其访问来源。只有贯通这些信息，才能在底层选择相应的放置方式。
 
 第 82 页固定 48 个 vCPU 到 48 个 pCPU，比较所用默认 Xen 与选择最佳策略的 XenNUMA；报告最高性能改善 700%，虚拟化额外成本低于 50% 的应用由 12/29 增至 23/29。这里“最佳策略”包含选择优势，不能直接理解成无调参部署的收益，也不重解释百分比为未注明的绝对耗时。
 
 ## 暴露一次拓扑还不够（第 83–88 页）
 
-如果只在 hypervisor 内选择通用策略，客户机运行库看不到拓扑，就难以实施 NumaGiC 或特定分配器的策略。若 vNUMA 只在启动时暴露拓扑，而 hypervisor 之后迁移 vCPU，客户机可能继续按过期的“本地/远端”关系工作。
+不过，hypervisor 里的通用策略还不能替代应用专用知识。若客户机运行库看不到拓扑，NumaGiC 或分配器就无法依据真实位置安排工作。
+
+即使 vNUMA 在启动时提供了拓扑，信息也可能过期：hypervisor 后来迁移 vCPU，原来本地的数据就可能变远。客户机继续按旧的距离关系运行，仍会做出不合适的放置选择。
 
 XPV 的思路是既暴露初始拓扑，也在拓扑变化时通知客户机 OS 和运行库。第 87 页研究固定拓扑，第 88 页研究三个相同 VM 在 48 vCPU / 42 pCPU 设置下的迁移情形，原页分别报告最高 130%、127% 的改善。图例中还区分 OS-only XPV 与运行库也参与的 XPV，不应将它们合成同一条结果。
 
@@ -172,7 +200,9 @@
 
 ## 实际学习时如何使用这条主线（第 89 页）
 
-先确认访问成本是否真的落在内存，而非大部分命中缓存；再观察线程放置、物理页分布及节点流量；尝试通用策略后，才判断是否需要运行时专用机制。高远程访问比例、带宽失衡和同步争用是不同证据，不能仅凭程序“慢”就断定 NUMA 是原因。
+回到自己的程序，可以按课件的研究过程逐步排查。先确认瓶颈是否涉及内存访问，再看线程在哪运行、页面落在哪些节点、访问流量如何分布。比较通用策略的效果后，才判断是否需要针对运行时调整算法。
+
+远程访问多、节点带宽失衡和同步争用，分别指向不同问题。记录这些证据，才能解释程序为什么变快或变慢，也才能决定下一步该调整位置、分工还是同步方式。
 
 对应实验：[NUMA](../Labs/tp-numa/tp-numa.html)。实验记录应同时写清机器拓扑、核/线程数、页大小、初始化方式、绑定策略和测量指标，才便于解释结果。
 
--- before/06-nvmm_zh.md
+++ after/06-nvmm_zh.md
@@ -12,9 +12,11 @@
 
 ## 把持久性带到内存访问路径（第 1–3 页）
 
-普通易失内存断电后不保留数据；传统文件或块存储可以保留数据，但接口和访问成本不同。课件介绍的 NVMM 结合了按字节寻址、普通 load/store 访问和非易失性。图中的“DDR4 + flash”是特性类比，不是所有 NVMM 的硬件制造配方。
+前面几讲默认程序运行期间数据仍在内存里。这一讲加入崩溃和掉电：一次修改完成后，恢复程序究竟还能看到什么？
 
-第 3 页用 DDR4、Optane DC 与 NVMe 给出历史性能对比。以下保留原表单位，不擅自把 Gbps 改成 GB/s：
+普通易失内存断电后不保留数据，传统文件或块存储则通过不同的接口保存数据。课件介绍的 NVMM 同时提供按字节寻址、普通 load/store 访问和非易失性。图中的“DDR4 + flash”是在并列这些特性，并未给出所有 NVMM 都遵循的制造方案。
+
+第 3 页将 DDR4、Optane DC 与 NVMe 放在一起比较。读表时先区分单次访问的延迟与连续传输的带宽，再区分顺序访问和随机访问。下面沿用原表的 Gbps，未将它换成含义不同的 GB/s：
 
 | 介质 | 顺序读 | 顺序写 | 顺序带宽 | 随机读 | 随机写 | 随机带宽 |
 |---|---|---|---|---|---|---|
@@ -26,11 +28,13 @@
 
 ## DAX 去掉的是哪一层缓存（第 4–6 页）
 
-普通文件映射常由内核 page cache 承载，修改随后写入存储设备。DAX 使适合的存储介质直接映射给进程，省去这份页缓存中转。绕过内核 page cache，并不等于绕过 CPU cache。后面的持久化问题恰恰可能发生在 CPU 缓存及写入队列中。[Linux DAX 文档](https://www.kernel.org/doc/html/v6.9/filesystems/dax.html)
+从程序到介质之间，通常还隔着缓存。普通文件映射常由内核 page cache 承载，修改随后传到存储设备。DAX 将适合的介质直接映射给进程，省去这份内核页缓存中转。
+
+**CPU cache 仍然需要单独考虑。** 一次 store 经过 DAX 访问持久内存，也可能先停在 CPU 缓存或写入队列中；能直接访问，并不足以说明修改已经进入掉电安全的区域。[Linux DAX 文档](https://www.kernel.org/doc/html/v6.9/filesystems/dax.html)
 
 ![DAX 与普通文件访问路径](Images/06-nvmm/pdf_p006.png)
 
-课件示意先 `open`、`ftruncate`，再 `mmap`，通过 `addr[0]='a'` 修改映射。系统调用用于建立访问关系，而随后访问数据看起来像普通内存写入。
+图中的代码先通过 `open` 取得文件，使用 `ftruncate` 设置长度，再以 `mmap` 建立映射。之后，程序就能用 `addr[0]='a'` 这样的内存写入访问该区域。要理解接下来的崩溃问题，需继续追踪这次写入去了哪里、何时才算持久。
 
 **原代码需要修正的地方：** `open(..., O_CREAT)` 需要适当的 mode 参数；每个系统调用必须检查失败；`MAP_SYNC` 不能当作普通 SSD 文件也普遍支持的标志。不支持所需 DAX 同步映射时会失败，不能忽略错误继续使用地址。`MAP_SYNC` 也不替代应用为修改执行必要的持久化动作。[Linux mmap 手册](https://www.man7.org/linux/man-pages/man2/mmap.2.html)
 
@@ -38,9 +42,11 @@
 
 ## 数据没有消失，不等于状态正确（第 7–9 页）
 
-向持久化对象的 `name[256]` 写入字符串时，`strcpy` 可能执行多次写入。若在中间崩溃，恢复后数组可能只完成一部分，既不是旧值也不是完整新值。
+先看最简单的字符串更新。向持久对象的 `name[256]` 调用 `strcpy`，内部可能包含多次写入。如果写到一半崩溃，恢复时这些字节未必全部回到旧值，也未必都成为新值。介质保留了数据，保留下来的却可能是一次更新的中间状态。
 
-课件加入 `committed` 标志：恢复时为假就重做初始化，为真就接受数据。但如果标志先持久化、字符串后持久化，崩溃后仍会得到“已提交但内容不完整”。
+恢复程序需要一个判断依据。课件为对象加上 `committed`：标志为假就重做初始化，为真便接受当前数据。
+
+这个标志必须可靠地代表数据状态。如果它比字符串更早持久化，崩溃以后，恢复程序就会相信一份尚未写完整的字符串。因此要先明确标志需要保证的含义：
 
 需要建立的不变量是：
 
@@ -50,15 +56,17 @@
 \text{所承诺的数据已完整持久化}.
 $$
 
-**原文勘误：** 结构字段叫 `committed`，条件里却写 `isCommitted`，这是课件本身的不一致。教程统一讲 `committed`。本例还假设持久化区有正确初始化的假标志、单次初始化操作可安全重做，并无并发写入者；它不是能处理任意多次更新的通用事务库。
+**原文勘误：** 结构中声明的是 `committed`，判断条件却写成 `isCommitted`，教程统一按 `committed` 解释。
 
-普通内存屏障主要用于约束相应内存操作的观察顺序。它不能不加平台条件地保证所有脏缓存行都已进入掉电安全区域，尤其不能只根据 C 源码先后就推断持久化顺序。
+以下只讨论一次可重做的初始化：持久区中的假标志已正确建立，没有并发写入者，重复写入这份数据也安全。多次更新或多个写者需要进一步设计，不能直接沿用这个最小例子的全部步骤。
+
+仅让源码按数据在前、标志在后的顺序书写，还不足以建立这个保证。普通内存屏障约束相应操作的观察顺序，而修改可能仍留在易失缓存中。要判断崩溃后留下哪些字节，还需要平台提供的写回与持久化保证。
 
 <a id="ordering"></a>
 
 ## 先持久化数据，再持久化提交标志（第 10–11 页）
 
-课件定义 `pwb(addr)` 为请求写回包含 addr 的缓存行，`pfence()` 为约束前后 store/write-back 的顺序。先按这个抽象模型理解：
+课件接着引入两个抽象操作：`pwb(addr)` 请求写回包含 addr 的缓存行，`pfence()` 约束前后 store/write-back 的顺序。用它们表达先写回数据、再写回标志的协议：
 
 ```text
 教学伪代码：单次初始化，初始 committed=false 已可靠建立
@@ -69,13 +77,15 @@
 pwb(&committed)
 ```
 
-关键不是“代码中加了某个 fence 就安全”，而是恢复者不可能观察到标志已提交、对应数据却还缺失的持久化状态。
+检查这段协议时，回到前面的恢复不变量：只要提交标志已经持久化，字符串所需的写入就必须先到达。屏障和写回操作的作用，都要通过这条先后关系来解释。
 
 ![数据、屏障与标志的顺序](Images/06-nvmm/pdf_p011.png)
 
-**图解：** 原图箭头依次连接字符串写入、数据写回、屏障、标志写入和标志写回。这里只保证抽象模型规定的先后关系；第 12 页会进一步区分“请求有序”和“已经完成”。
+沿原图箭头读过去，顺序是字符串写入、数据写回、屏障、标志写入、标志写回。这个抽象模型首先约束它们的先后；是否已经等到写回完成，是第 12 页接着讨论的另一项要求。
 
-**范围修正：** 原图只调用一次 `pwb(id->name)`。对于恰好位于一条缓存行内的短字符串写入可能足够，但一般的数组修改必须覆盖所有涉及的行。即便字符串很短，地址若靠近缓存行末尾也可能跨行，不能只看 `name` 长度或字符串字符数就决定刷新次数。
+**写回范围也要核对。** 原图调用一次 `pwb(id->name)`，它只针对包含这个地址的一条缓存行。如果本次修改全部落在该行内，可能已经足够；若修改跨行，就必须覆盖其他涉及的行。
+
+因此要看地址和修改范围，不能只看 `name` 数组长度或字符串有多短。即便短字符串从一行末尾开始，也可能写到下一行。
 
 还要考虑缓存行可能被硬件主动写回，数据与标志是否同处一行、标志写入的掉电原子粒度，以及已有标志如何在下一轮更新前安全复位。这些是扩大到一般持久化数据结构时需要补充的条件。
 
@@ -87,15 +97,17 @@
 | 数据已持久化，标志未持久化 | 数据完整，但仍显示未提交 | 重做必须是安全的，不能把“重复执行无害”当作默认 |
 | committed 已可靠为真 | 按协议应已有完整数据 | 接受已提交状态 |
 
-这个例子用固定字符串展示可重做初始化。若操作是“账户加 100”，盲目重做就可能重复增加，必须使用日志、版本或其他恢复设计。
+这里能够重做，是因为例子写入的是同一个固定字符串。恢复时再次完成相同写入，可以得到所需结果。若操作改为账户加 100，直接重做就可能多加一次；需要日志、版本或其他恢复设计来判断该操作究竟进行到了哪一步。
 
 <a id="completion"></a>
 
 ## 让另一个线程知道“现在已经持久”（第 12–13 页）
 
-有时仅保证“如果标志持久，那么数据先持久”还不够。程序可能要在返回成功、通知别人或继续外部动作之前，确认前面的写回已经完成。课件为此引入 `psync()`：除顺序作用外，还等待之前请求的持久化完成。
+前面的协议约束数据和标志的持久化先后。程序若还要向调用者返回成功，或通知另一个线程继续，就可能需要确认写回此刻已经完成。
 
-课件把 `visible` 放在易失内存中，并使用原子变量：
+课件为这项要求引入 `psync()`：它既提供顺序作用，也等待此前请求的持久化完成。接下来才适合发布已完成的消息。
+
+第 13 页增加原子变量 `visible`，让它负责当前运行中的线程通信，并将其放在易失内存中。生产者先完成持久化，再设置这个标志，读者按下面的顺序等待：
 
 ```text
 线程 1：写数据 → pwb 数据 → pfence
@@ -107,19 +119,23 @@
 
 ![先完成持久化，再发布可见标志](Images/06-nvmm/pdf_p013.png)
 
-**图解：** `committed` 为恢复协议服务，`visible` 为本次运行中的线程通信服务。`psync` 在发布 visible 之前，读者才可以把读到该发布值作为“生产者已等到持久化完成”的依据；它还需要正确的原子同步语义。恢复后易失的 visible 必须重新初始化，并从持久状态决定后续行为。
+这张图里有两个服务对象不同的标志。恢复程序依据持久的 `committed` 判断数据，运行中的读者依据 `visible` 判断生产者是否已经完成准备。
 
-第 12 页把 durable linearizability 简述为写入可见与持久之间的联系，不能作为该一致性条件的完整定义。严格讨论还需规定操作历史、返回、崩溃以及恢复后的抽象状态。这里将这幅图限定为“持久后发布”的示例，而不声称已证明任意数据结构满足耐久线性化。
+生产者先执行 `psync`，再以正确的原子同步语义发布 visible。读者读到该发布值，才可以据此推断生产者已经等到持久化完成。崩溃恢复之后，易失的 visible 需要重新初始化，再由持久状态决定后续行为。
+
+第 12 页借 durable linearizability 说明可见性与持久性的联系，但这里的图只展示了持久后发布。要完整定义并证明耐久线性化，还须规定操作历史、返回、崩溃和恢复后的抽象状态，不能仅由这一段代码推出任意数据结构都满足该条件。
 
 <a id="platform"></a>
 
 ## 为什么两个抽象屏障可能对应同一条指令（第 14–15 页）
 
-课件给出 x86 风格映射：pwb 用 `clwb`，pfence 和 psync 都用 `sfence`。抽象接口区分所需语义，而某个平台的一条指令可以同时提供更强的顺序/等待效果，因此不矛盾。
+最后将抽象操作对应到具体平台。课件的 x86 风格映射为：pwb 使用 `clwb`，pfence 和 psync 都使用 `sfence`。抽象接口分别说明算法需要什么保证；具体指令若同时提供了足够强的顺序与等待效果，就可以承担两个接口的作用。
 
-但“Pentium 有残余能量，所以能把数据刷到 NVMM”不能当作所有处理器的保证。必须知道**持久域**：哪些 CPU 缓存、控制器队列或其他缓冲区得到掉电保护。ADR 与 eADR 的覆盖不同，所需写回方式也可能不同；支持 TSO 不代表支持 CLWB，更不代表整机已配置持久内存保护。[PMDK 持久粒度说明](https://pmem.io/blog/2022/06/introduction-to-libpmem2-part-1/)
+判断这种映射是否足够，必须确认平台的**持久域**，也就是哪些缓存、控制器队列或其他缓冲区受掉电保护。课件关于 Pentium 残余能量的说法，不能直接当作所有处理器和整机的保证。
 
-因此这里保留课件的概念映射，不在当前机器上执行持久内存指令或声称做过掉电验证。实际实现应使用确认平台能力的库和文档，并用故障注入及恢复测试检查协议。
+ADR 与 eADR 覆盖的范围不同，所需写回方式也可能不同。TSO 只说明相关内存顺序，不能据此推断 CPU 支持 CLWB，或整机已经具备所需的持久内存保护。[PMDK 持久粒度说明](https://pmem.io/blog/2022/06/introduction-to-libpmem2-part-1/)
+
+进入具体实现时，应先按平台文档和支持库确认能力，再通过故障注入与恢复测试检查协议。本篇完成的是课件抽象模型的讲解，没有在当前机器上执行持久内存指令或进行真实掉电实验。
 
 对应实验：[非易失性内存](../Labs/tp-nvmm/tp-nvmm.html)。先标明哪些变量易失、哪些变量持久，再为每条持久写标出依赖顺序；这个步骤比先背指令名更重要。
 
--- before/README.md
+++ after/README.md
@@ -1,6 +1,8 @@
 # PAAM 课程讲义
 
-从线程与同步开始，依次理解锁、非阻塞算法、事务内存、NUMA 和持久内存。每讲都有原始 PDF、按页校对的原文和中文详解；中文稿沿课件顺序解释问题、代码与图示，并提供自检题。
+这组讲义沿着一个问题展开：多个线程怎样共同完成工作，并得到正确、可靠的结果？先从线程与同步学起，再进入锁的实现、非阻塞数据结构和事务内存；最后把视线转向数据的位置，以及崩溃后留下的状态。
+
+初读可以直接进入中文教程，按例子和执行步骤理解机制。需要核对术语、代码或原图时，再打开同一行的原文与 PDF。三种材料按课件页码对应。
 
 | 顺序 | 这一篇回答的问题 | 中文教程 | 对照材料 |
 |---|---|---|---|
@@ -14,13 +16,15 @@
 
 ## 怎样阅读
 
-第一次读，先跟着每篇开头的路线建立主线。遇到代码，先找共享状态和一次操作的完成位置，再分析线程交错；遇到性能图，先确认机器、横纵轴、基准与单位。正文中的补充解释与原课件勘误均已标注。
+读代码时，先画出几个线程共同访问的状态，再跟踪一次操作从准备到生效的过程。能说清成功路径后，试着在中间暂停一个线程，看看别人继续运行会发生什么。教程中的余额、队列和事务例子都可以用这种方式检查。
+
+读性能图时，先找到横纵轴、单位和比较基准，再看曲线变化。增加核数后总时间是否下降，与某个局部操作是否更快，可能是两个不同的问题。图旁说明保留了原实验的机器与工作负载条件。
 
 锁讲义的内存模型是后续阅读的关键：课件在后面的伪代码中省略很多显式原子操作，却假设顺序一致语义。不能把它们按普通 C 变量照抄。事务内存的并发原子性与最后一讲的持久性也是不同要求，学习时要分别判断。
 
-所有正文图片已在本地，原文稿还可按页展开原图，核对箭头、颜色与逐步动画。中文稿合并重复的展示页，但注明完整页码范围；完整原页没有删除。长篇 NUMA 可以分为硬件与放置、策略预测、GC、虚拟化四次阅读。
+每篇开头有阅读路线，可以按概念单元分次读。NUMA 尤其适合分成硬件与放置、策略预测、GC、虚拟化四段。合并讲解的动画标有完整页码范围，需要追踪某一步时，可在原文稿中逐页展开原图。所有正文图片都已保存在本地。
 
-每篇末尾链接对应实验。自检题用于检查概念，不等同于原课件题目或考试范围；本轮没有代写全部实验答案。
+每篇末尾有自检题及判断要点，先尝试独立解释，再对照答案补上遗漏的条件。随后可进入对应实验，把共享状态、同步关系或内存放置落实到代码中。这些自检题是学习辅助，不代表原课件题目或考试范围；实验入口链接到教师提供的材料。
 
 ## 校对说明
 
