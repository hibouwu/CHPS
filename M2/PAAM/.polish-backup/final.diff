只在 Official/Lectures 中存在：01-threads.md
只在 Official/Lectures 中存在：01-threads.pdf
只在 Official/Lectures 中存在：01-welcome.md
只在 Official/Lectures 中存在：01-welcome.pdf
diff -u -r .polish-backup/zh/01-welcome_zh.md Official/Lectures/01-welcome_zh.md
--- .polish-backup/zh/01-welcome_zh.md	2026-09-22 09:47:56.452102577 +0200
+++ Official/Lectures/01-welcome_zh.md	2026-09-22 09:48:00.512434209 +0200
@@ -37,7 +37,7 @@
 
 课件给出的组织方式是六周，每周一个主题，含 1–2 小时理论、4–5 小时实验及平均 1 小时课后工作。这是原材料的安排说明，实际课表以教师通知为准。
 
-学习时以本讲义和对应实验为主。遇到指针、内存管理或系统调用的具体困难，再回查基础材料，不必先把全部补充课程读完。
+学习时以这篇讲义和对应实验为主。遇到指针、内存管理或系统调用的具体困难，再回查基础材料，不必先把全部补充课程读完。
 
 <a id="prerequisites"></a>
 
只在 Official/Lectures 中存在：02-locks.md
只在 Official/Lectures 中存在：02-locks.pdf
diff -u -r .polish-backup/zh/02-locks_zh.md Official/Lectures/02-locks_zh.md
--- .polish-backup/zh/02-locks_zh.md	2026-09-22 09:47:56.452102577 +0200
+++ Official/Lectures/02-locks_zh.md	2026-09-22 09:48:22.157176710 +0200
@@ -58,7 +58,7 @@
 
 **图解：** 两个线程分别写 `tab[0]` 和 `tab[1]`，逻辑上没有写同一元素，却可能在同一缓存行上竞争。图中各自持有旧副本、各改一部分的画法用于解释为什么必须有一致性协议，不是说正常硬件任意丢写。
 
-第 18 页列出读缺失、写缺失和共享行转写三种情况。理解它们时抓住一个问题：**下一次操作是否需要从别的核取得数据或写权限？** 不同核反复写同一个锁字，会使所属缓存行来回转移。即使临界区很短，这种交接也可能限制吞吐量。
+第 18 页列出读缺失、写缺失和共享行转写三种情况。理解它们时抓住一个问题：下一次操作是否需要从别的核取得数据或写权限？不同核反复写同一个锁字，会使所属缓存行来回转移。即使临界区很短，这种交接也可能限制吞吐量。
 
 <a id="ordering"></a>
 
@@ -91,13 +91,13 @@
 }
 ```
 
-这可以写成因果链：数据初始化 → release 发布 → 读到该发布的 acquire → 使用数据。**仅仅在两个线程里各写了 release/acquire 并不够，它们必须建立对应关系。**
+这可以写成因果链：数据初始化 → release 发布 → 读到该发布的 acquire → 使用数据。仅仅在两个线程里各写了 release/acquire 并不够，它们必须建立对应关系。
 
 **原课件勘误：** PDF 第 30 页两个 atomic 接口的 `x` 前均缺少 `&`，且无条件读取普通 `y`、`z`。若 acquire 没有读到发布值，这些读取就没有获得所需同步。上面增加了条件，是明确标注的教学修正版，不是声称原图已经这样写了。
 
 ## Consume 与顺序一致性：两处需要认真勘误的例子（第 31–33 页）
 
-consume 的原始设计关注沿读取结果延续的数据依赖，例如取得已发布的指针，再通过该指针访问对象。**第 31 页不能用于证明它给出的结论：** 原图把 `z` 声明为普通 `int` 却用于原子接口，而且读线程后面的 `r3=x` 并不依赖于刚刚读到的 `r1`。写线程中 `x` 到 `z` 的关系不能替代读取端的依赖链。本教程保留概念说明，不把该片段改名后作为正确样例。[WG14 内存顺序说明](https://open-std.org/jtc1/sc22/wg14/www/docs/n1525.htm)
+consume 的原始设计关注沿读取结果延续的数据依赖，例如取得已发布的指针，再通过该指针访问对象。**第 31 页不能用于证明它给出的结论：** 原图把 `z` 声明为普通 `int` 却用于原子接口，而且读线程后面的 `r3=x` 并不依赖于刚刚读到的 `r1`。写线程中 `x` 到 `z` 的关系不能替代读取端的依赖链。这里保留概念说明，不把该片段改名后作为正确样例。[WG14 内存顺序说明](https://open-std.org/jtc1/sc22/wg14/www/docs/n1525.htm)
 
 顺序一致性要求相关原子操作可以放进一个兼容线程内顺序的共同顺序。它并不要求多个线程每次看到同样的“快照”。
 
@@ -114,7 +114,7 @@
 
 因此课件列出的结果并没有违反共同全序。这是原例子的逻辑问题，不是 OCR 问题。若要构造标准的相反写序观察例，需要让两个读线程按相反的变量顺序读取，并重新指定结果；这里不偷偷替换原题。
 
-第 33 页十分重要：后续课件为了可读性，常省略显式原子调用，**默认使用顺序一致的原子读写**。阅读后面的锁与非阻塞伪代码时必须带着这个约定；搬到实验的 C 文件里时再落实类型和接口。
+第 33 页十分重要：后续课件为了可读性，常省略显式原子调用，默认使用顺序一致的原子读写。阅读后面的锁与非阻塞伪代码时必须带着这个约定；搬到实验的 C 文件里时再落实类型和接口。
 
 <a id="locks"></a>
 
只在 Official/Lectures 中存在：03-non-blocking.md
只在 Official/Lectures 中存在：03-non-blocking.pdf
diff -u -r .polish-backup/zh/03-non-blocking_zh.md Official/Lectures/03-non-blocking_zh.md
--- .polish-backup/zh/03-non-blocking_zh.md	2026-09-22 09:47:56.452102577 +0200
+++ Official/Lectures/03-non-blocking_zh.md	2026-09-22 09:48:03.723984677 +0200
@@ -8,7 +8,7 @@
 
 阅读路线：[性能动机与进展保证](#progress) → [栈](#stack) → [队列](#queue) → [链表](#list) → [回收边界](#reclamation) → [自检](#self-check)。
 
-本讲沿用上一讲第 33 页约定：共享访问按顺序一致原子操作理解。课件的 `CAS(address, old, new)` **返回交换前观察到的旧值**；这不同于 C `atomic_compare_exchange_strong` 的布尔返回值和 `expected` 指针接口，不能直接替换函数名后照搬。
+沿用上一讲第 33 页的约定：共享访问按顺序一致原子操作理解。课件的 `CAS(address, old, new)` 返回交换前观察到的旧值；这不同于 C `atomic_compare_exchange_strong` 的布尔返回值和 `expected` 指针接口，不能直接替换函数名后照搬。
 
 <a id="progress"></a>
 
@@ -83,7 +83,7 @@
 
 初始时 head 和 tail 都指向同一个哨兵。构造元素 A 的新节点，其 `next` 为空。入队者从 tail 出发，若发现 `next` 非空，就帮助 tail 向后追赶；找到末端后，CAS 把该末端的 `next` 从空变成新节点。
 
-**把空的 `next` 改成 A 的那次成功 CAS，才是入队的线性化点。** 后面尝试将 tail 移到 A 只是维护辅助指针。即使这个 tail CAS 失败，也不需要撤销已经成功的入队，因为其他人可能已经帮忙移动了它。
+把空的 `next` 改成 A 的那次成功 CAS，才是入队的线性化点。后面尝试将 tail 移到 A 只是维护辅助指针。即使这个 tail CAS 失败，也不需要撤销已经成功的入队，因为其他人可能已经帮忙移动了它。
 
 ## 两个入队者如何互相帮助（第 24–36 页）
 
@@ -125,7 +125,7 @@
 mark(v)    = v 的最低位
 ```
 
-第 48 页的 `ptr & -2` 表达清除最低位的意图。在具体 C 实现中须确认对齐保证、`uintptr_t` 与掩码类型、指针表示及原子访问方式；图中的位操作不自动在所有平台上成立。标的是**当前节点自己的 next 域**，不是随意给前驱链上的箭头加颜色。
+第 48 页的 `ptr & -2` 表达清除最低位的意图。在具体 C 实现中须确认对齐保证、`uintptr_t` 与掩码类型、指针表示及原子访问方式；图中的位操作不自动在所有平台上成立。标的是当前节点自己的 next 域，不是随意给前驱链上的箭头加颜色。
 
 ## 遍历与帮助摘链（第 49–59 页）
 
@@ -157,11 +157,11 @@
 
 ## 从教学图走到真实程序，还差内存回收（第 76 页及补充）
 
-本讲最后强调进展等级、数据结构不变量和线性化点。学习时应能为每个成功操作指出提交效果发生在哪里，也能说明失败重试是否代表其他线程取得进展。
+最后强调进展等级、数据结构不变量和线性化点。学习时应能为每个成功操作指出提交效果发生在哪里，也能说明失败重试是否代表其他线程取得进展。
 
 **补充，非原课件完整展开内容：** 逻辑删除、物理摘链、释放内存是三件事。一个节点已经不在链上，另一个线程仍可能持有旧指针。立即 `free` 会造成释放后使用；地址被回收再复用，还可能使 CAS 看到同一地址值而误以为状态没变，这就是常见的 ABA 风险。仅给指针加版本号，并不能单独解决旧指针解引用的安全性。
 
-因此本讲的论证先假设节点在使用期间有效，并且不会发生破坏假设的地址复用。真实系统需要垃圾回收、hazard pointers、epoch 等适当机制，并检查内存分配及回收是否改变整体进展保证。这里点明缺口，不假装已经实现完整回收算法。[Michael 的 hazard pointers 论文](https://research.ibm.com/publications/hazard-pointers-safe-memory-reclamation-for-lock-free-objects)
+因此这里的论证先假设节点在使用期间有效，并且不会发生破坏假设的地址复用。真实系统需要垃圾回收、hazard pointers、epoch 等适当机制，并检查内存分配及回收是否改变整体进展保证。这里点明缺口，不假装已经实现完整回收算法。[Michael 的 hazard pointers 论文](https://research.ibm.com/publications/hazard-pointers-safe-memory-reclamation-for-lock-free-objects)
 
 对应实验：[非阻塞算法](../Labs/tp-non-blocking/tp-non-blocking.html)。开始写代码前，先画出读指针、验证指针、使用节点与释放节点的关系。
 
只在 Official/Lectures 中存在：04-stm.md
只在 Official/Lectures 中存在：04-stm.pdf
diff -u -r .polish-backup/zh/04-stm_zh.md Official/Lectures/04-stm_zh.md
--- .polish-backup/zh/04-stm_zh.md	2026-09-22 09:47:56.452102577 +0200
+++ Official/Lectures/04-stm_zh.md	2026-09-22 09:48:08.205784535 +0200
@@ -2,7 +2,7 @@
 
 M2 CHPS · Gaël Thomas
 
-> 对照：[原始 PDF](04-stm.pdf)，54 页；[原文](04-stm.md)。本篇按课件从接口到实现的顺序展开，动画按完整状态变化合并。这里的事务主要解决并发访问的一致性，不自动提供掉电后的持久性。
+> 对照：[原始 PDF](04-stm.pdf)，54 页；[原文](04-stm.md)。这一篇按课件从接口到实现的顺序展开，动画按完整状态变化合并。这里的事务主要解决并发访问的一致性，不自动提供掉电后的持久性。
 
 [课程目录](README.md) · 上一篇：[非阻塞算法](03-non-blocking_zh.md) · 下一篇：[NUMA](05-numa_zh.md)
 
@@ -16,7 +16,7 @@
 
 事务内存提供一个高层 `atomic { ... }`：对受事务协议管理的观察者，该块的效果应像整体执行。它是编程模型语法，不是标准 C 的 `_Atomic` 变量修饰符，也不是一条硬件原子指令。
 
-课件的例子是一边把 x 加 1，另一边乘 2。假设 x 初值为 21，串行顺序有两种：先加后乘得到 44，先乘后加得到 43。**第 3 页并未写出初值 21，这是由结果反推并在此明确补充的教学前提。** 图中序列 `[b,c,e]` 把右花括号 e 放进操作列表，实际写回是 d；应按读、计算、写回完整理解。
+课件的例子是一边把 x 加 1，另一边乘 2。假设 x 初值为 21，串行顺序有两种：先加后乘得到 44，先乘后加得到 43。第 3 页并未写出初值 21，这是由结果反推并在此明确补充的教学前提。图中序列 `[b,c,e]` 把右花括号 e 放进操作列表，实际写回是 d；应按读、计算、写回完整理解。
 
 把临界区改写成 atomic 可以隐藏锁的选择，但不是机械删除所有锁就能正确。事务外访问、I/O、不可重试操作及与其他同步机制的交互都需要单独定义。
 
@@ -51,7 +51,7 @@
 | 延迟更新（redo log） | 私有写日志 | 验证后将日志发布到共享内存 | 丢弃尚未发布的日志 |
 | 立即更新（undo log） | 共享内存，另存旧值 | 在合适保护下确认提交 | 按旧值恢复 |
 
-redo 避免在正常试执行期间公开中间写值，但**提交时批量写回也必须有同步协议**，不能把日志逐项随意写出。undo 需要在他人看到或依赖中间值之前进行冲突管理。两者都有各自的日志、验证和恢复成本，不能仅凭“提交多”或“失败多”就断言性能赢家。
+redo 避免在正常试执行期间公开中间写值，但提交时批量写回也必须有同步协议，不能把日志逐项随意写出。undo 需要在他人看到或依赖中间值之前进行冲突管理。两者都有各自的日志、验证和恢复成本，不能仅凭“提交多”或“失败多”就断言性能赢家。
 
 ## 两种日志的图中过程（第 13–14 页）
 
@@ -117,7 +117,7 @@
       释放提交锁
 ```
 
-**为什么是 `>=` 而不是 `>`？** 这是课件“先用 G 写版本，再增加 G”的约定。若实现改成先增加时钟再给事务分配提交版本，相应不等式也要一致调整，不能拼接两种方案。
+这里用 `>=` 而不是 `>`，是因为课件“先用 G 写版本，再增加 G”的约定。若实现改成先增加时钟再给事务分配提交版本，相应不等式也要一致调整，不能拼接两种方案。
 
 动画第 38–45 页：右侧事务较早在时钟 90 开始；其他事务把 G 推到 100；左侧在 S=100 开始并读到旧 x=4。右侧提交时用版本 100 写入新 x、y，再把 G 改成 101。左侧接着读 y，看见版本 100≥S，于是立即中止，不会带着 `(4,4)` 继续做除法。
 
@@ -137,19 +137,19 @@
 
 **图解：** 只有“验证成功→全部写回→递增时钟”的组合在提交锁内完成。读取端虽然不拿这把锁，也必须按语言内存模型正确读取已发布对象和时钟；不能把课件普通字段直接搬进 Java 或 C 就假设可见性自动成立。
 
-**原文边界：** 第 47–51 页混用 Java 风格类型与方法。`HashMap<int,int>`、`Map<int,Value> entry` 等不构成可直接编译的 Java；本文保留算法意图。旧的不可变对象也不能立即释放，仍需垃圾回收或其他安全回收方案。课件这一实现明确使用提交锁，所以事务接口本身不意味着底层 lock-free。
+**原文边界：** 第 47–51 页混用 Java 风格类型与方法。`HashMap<int,int>`、`Map<int,Value> entry` 等不构成可直接编译的 Java；这里保留算法意图。旧的不可变对象也不能立即释放，仍需垃圾回收或其他安全回收方案。课件这一实现明确使用提交锁，所以事务接口本身不意味着底层 lock-free。
 
 ## 退避、外部副作用与性能（第 52–54 页）
 
 失败的事务若总在同一时刻重试，可能反复冲突。随机指数退避让重试时间错开，但不会凭空提供每个线程都能成功的保证。
 
-**第 52 页勘误：** 原例在 `doTransaction()` 内重新设 `n=16`，失败后递归调用该函数。因此新一轮又从 16 开始，无法按所写代码持续扩大退避窗口，还会增长调用栈。正确表达该意图应把 n 放在外层循环的持续状态中，成功或放弃时退出，而不是每次递归重新初始化。本教程只说明修正方向，不把它伪装成已验证运行库。
+**第 52 页勘误：** 原例在 `doTransaction()` 内重新设 `n=16`，失败后递归调用该函数。因此新一轮又从 16 开始，无法按所写代码持续扩大退避窗口，还会增长调用栈。正确表达该意图应把 n 放在外层循环的持续状态中，成功或放弃时退出，而不是每次递归重新初始化。这里只说明修正方向，不把它伪装成已验证运行库。
 
 外部 I/O 不一定能撤销。事务执行中发送了请求、写了日志文件，之后内存事务 abort，并不会使外部世界回到过去。课件用一个夸张的函数名说明这点；不可撤销模式需要在操作前协调提交能力，或者把外部动作放到提交后的明确协议中。
 
 第 54 页写“无死锁、无饥饿”，但这是过强的概括：运行时仍可能使用锁，冲突事务也可能持续重试，程序还可能等待永远不会成立的业务条件。接口减少了手工锁顺序负担，并不自动证明全部进展性质。
 
-课件的性能例子是在双核上让 100 个线程各递增计数器 10,000 次：STM 无退避约 3.0 秒、有退避 0.48 秒、锁 0.19 秒。它说明这个高冲突工作负载中退避有帮助、锁仍更快；不是 STM、HTM 和细粒度锁在所有机器上的统一排名。
+课件的性能例子是在双核上让 100 个线程各递增计数器 10000 次：STM 无退避约 3.0 秒、有退避 0.48 秒、锁 0.19 秒。它说明这个高冲突工作负载中退避有帮助、锁仍更快；不是 STM、HTM 和细粒度锁在所有机器上的统一排名。
 
 对应实验：[事务内存](../Labs/tp-stm/tp-stm.html)。先把读集合、写集合、事务开始时钟和提交时钟四者区分清楚，再跟踪一条具体事务。
 
@@ -157,7 +157,7 @@
 
 ## 自检与判断要点
 
-1. **事务内存的 atomic 是否意味着掉电后数据不丢？** 否，本讲关注并发原子效果；持久性需另有协议。
+1. **事务内存的 atomic 是否意味着掉电后数据不丢？** 否，这一讲关注并发原子效果；持久性需另有协议。
 2. **src 出队成功、dst 入队触发 retry 时，组合事务应怎样？** 整体撤销试执行并从外层重新开始，不能保留只出队的提交效果。
 3. **redo 为何还需要安全的提交过程？** 多位置写回本身仍可能暴露部分状态，需版本及同步协议约束观察者。
 4. **x≠y 在每个已提交状态中都成立，为什么读者仍可能除零？** 它混合了不同提交版本的 x 和 y。
只在 Official/Lectures 中存在：05-numa.md
只在 Official/Lectures 中存在：05-numa.pdf
diff -u -r .polish-backup/zh/05-numa_zh.md Official/Lectures/05-numa_zh.md
--- .polish-backup/zh/05-numa_zh.md	2026-09-22 09:47:56.452102577 +0200
+++ Official/Lectures/05-numa_zh.md	2026-09-22 09:48:13.416955887 +0200
@@ -28,13 +28,13 @@
 
 第 13 页另列缓存层次，L1、L2、L3 示例约为 5、15、50 cycles。未命中缓存后才进一步涉及本地或远端内存控制器。NUMA 的“非均匀”表示同一共享地址空间中的内存位置不同，访问代价可能不同，不是说远端地址不可访问。
 
-第 15–16 页用四插槽主板说明复杂连接，标出 4×16 核及 128 个硬件线程。**核与硬件线程不能等量视为独立计算能力。** 图中总内存写 256GB，局部箭头写 16×8GB；课件未充分解释该标注覆盖范围，所以不据此重新计算并断言完整装机容量。
+第 15–16 页用四插槽主板说明复杂连接，标出 4×16 核及 128 个硬件线程。核与硬件线程不能等量视为独立计算能力。图中总内存写 256GB，局部箭头写 16×8GB；课件未充分解释该标注覆盖范围，所以不据此重新计算并断言完整装机容量。
 
 第 17–18 页给出本地、单跳、双跳访问示意：所用 AMD 平台有 8 个 NUMA 节点，每节点 6 核；本地内存约 155 cycles，单跳 275，双跳 380。这里一个插槽不等于一个 NUMA 节点。
 
 ![节点与跨节点路径](Images/05-numa/pdf_p018.png)
 
-第 19 页进一步让所有核访问同一个节点上的不同缓存行，示例延迟升到约 870 cycles。没有写同一缓存行，也会因内存控制器或互联拥塞变慢。因此优化要同时考虑**局部性、内存节点负载和互联负载**，不能只最小化远程访问次数。
+第 19 页进一步让所有核访问同一个节点上的不同缓存行，示例延迟升到约 870 cycles。没有写同一缓存行，也会因内存控制器或互联拥塞变慢。因此优化要同时考虑局部性、内存节点负载和互联负载，不能只最小化远程访问次数。
 
 <a id="placement"></a>
 
@@ -77,7 +77,7 @@
 
 第 34–37 页逐步画 first-touch：线程在节点 1 运行，访问尚未建立实际物理页的虚拟页，内核选取物理页并建立映射。教学实验通常通过并行写入初始化来触发私有页分配；单纯 `malloc` 或首次读取不总是等价于真正分配独立物理页。本地内存不足、显式策略、线程迁移等也会改变结果。[Linux NUMA 概念](https://docs.kernel.org/mm/numa.html)
 
-第 38–39 页的 Carrefour 同时关心节点拥塞与互联拥塞：前者可能需要分散，后者可能需要把页面移近使用者。课件把 Dashti 等的工作标作 ASPLOS’15；对应 Carrefour 原论文发表于 **ASPLOS 2013**，这是文献年份勘误。[作者提供的原论文](https://people.ece.ubc.ca/sasha/papers/asplos284-dashti.pdf)
+第 38–39 页的 Carrefour 同时关心节点拥塞与互联拥塞：前者可能需要分散，后者可能需要把页面移近使用者。课件把 Dashti 等的工作标作 ASPLOS’15；对应 Carrefour 原论文发表于 ASPLOS 2013，这是文献年份勘误。[作者提供的原论文](https://people.ece.ubc.ca/sasha/papers/asplos284-dashti.pdf)
 
 ## 没有一种策略对全部应用最好（第 41–42 页）
 
@@ -136,7 +136,7 @@
 - 有本地工作时使用 local 模式，把远端工作发给对应节点。
 - 本地空闲时使用 thief 模式，窃取并处理远端对象，以远程访问换取更好的并行利用率。
 
-这体现了贯穿本讲的取舍：尽可能本地化，但不为了零远程访问而让大量核空闲。
+这体现了贯穿这一讲的取舍：尽可能本地化，但不为了零远程访问而让大量核空闲。
 
 ## 吞吐提升如何传导到应用完成时间（第 75–77 页）
 
只在 Official/Lectures 中存在：06-nvmm.md
只在 Official/Lectures 中存在：06-nvmm.pdf
diff -u -r .polish-backup/zh/06-nvmm_zh.md Official/Lectures/06-nvmm_zh.md
--- .polish-backup/zh/06-nvmm_zh.md	2026-09-22 09:47:56.453102591 +0200
+++ Official/Lectures/06-nvmm_zh.md	2026-09-22 09:48:13.449186005 +0200
@@ -14,7 +14,7 @@
 
 普通易失内存断电后不保留数据；传统文件或块存储可以保留数据，但接口和访问成本不同。课件介绍的 NVMM 结合了按字节寻址、普通 load/store 访问和非易失性。图中的“DDR4 + flash”是特性类比，不是所有 NVMM 的硬件制造配方。
 
-第 3 页用 DDR4、Optane DC 与 NVMe 给出历史性能对比。以下保留原表单位，**不擅自把 Gbps 改成 GB/s**：
+第 3 页用 DDR4、Optane DC 与 NVMe 给出历史性能对比。以下保留原表单位，不擅自把 Gbps 改成 GB/s：
 
 | 介质 | 顺序读 | 顺序写 | 顺序带宽 | 随机读 | 随机写 | 随机带宽 |
 |---|---|---|---|---|---|---|
@@ -26,7 +26,7 @@
 
 ## DAX 去掉的是哪一层缓存（第 4–6 页）
 
-普通文件映射常由内核 page cache 承载，修改随后写入存储设备。DAX 使适合的存储介质直接映射给进程，省去这份页缓存中转。**绕过内核 page cache，并不等于绕过 CPU cache。** 后面的持久化问题恰恰可能发生在 CPU 缓存及写入队列中。[Linux DAX 文档](https://www.kernel.org/doc/html/v6.9/filesystems/dax.html)
+普通文件映射常由内核 page cache 承载，修改随后写入存储设备。DAX 使适合的存储介质直接映射给进程，省去这份页缓存中转。绕过内核 page cache，并不等于绕过 CPU cache。后面的持久化问题恰恰可能发生在 CPU 缓存及写入队列中。[Linux DAX 文档](https://www.kernel.org/doc/html/v6.9/filesystems/dax.html)
 
 ![DAX 与普通文件访问路径](Images/06-nvmm/pdf_p006.png)
 
@@ -109,7 +109,7 @@
 
 **图解：** `committed` 为恢复协议服务，`visible` 为本次运行中的线程通信服务。`psync` 在发布 visible 之前，读者才可以把读到该发布值作为“生产者已等到持久化完成”的依据；它还需要正确的原子同步语义。恢复后易失的 visible 必须重新初始化，并从持久状态决定后续行为。
 
-第 12 页把 durable linearizability 简述为写入可见与持久之间的联系，不能作为该一致性条件的完整定义。严格讨论还需规定操作历史、返回、崩溃以及恢复后的抽象状态。本教程将这幅图限定为“持久后发布”的示例，而不声称已证明任意数据结构满足耐久线性化。
+第 12 页把 durable linearizability 简述为写入可见与持久之间的联系，不能作为该一致性条件的完整定义。严格讨论还需规定操作历史、返回、崩溃以及恢复后的抽象状态。这里将这幅图限定为“持久后发布”的示例，而不声称已证明任意数据结构满足耐久线性化。
 
 <a id="platform"></a>
 
只在 Official/Lectures 中存在：Images
只在 Official/Lectures 中存在：OCR
diff -u -r .polish-backup/zh/README.md Official/Lectures/README.md
--- .polish-backup/zh/README.md	2026-09-22 09:47:56.453102591 +0200
+++ Official/Lectures/README.md	2026-09-22 09:48:13.459817734 +0200
@@ -2,7 +2,7 @@
 
 从线程与同步开始，依次理解锁、非阻塞算法、事务内存、NUMA 和持久内存。每讲都有原始 PDF、按页校对的原文和中文详解；中文稿沿课件顺序解释问题、代码与图示，并提供自检题。
 
-| 顺序 | 本篇回答的问题 | 中文教程 | 对照材料 |
+| 顺序 | 这一篇回答的问题 | 中文教程 | 对照材料 |
 |---|---|---|---|
 | 导读 | 多核程序为什么需要同时考虑正确性和性能？ | [课程介绍](01-welcome_zh.md) | [PDF](01-welcome.pdf) · [原文](01-welcome.md) |
 | 1 | 多个线程如何共享状态、互斥更新并等待条件？ | [进程、线程与同步](01-threads_zh.md) | [PDF](01-threads.pdf) · [原文](01-threads.md) |
只在 .polish-backup/zh 中存在：SHA256SUMS
