# 第1章 连续介质的运动学（Cinématique des milieux continus）

> 对照来源：[教师课件 PDF](IMMC_CM_Chap1_2025-2026.pdf)、[原文转换稿](IMMC_CM_Chap1_2025-2026.md)、[课堂转录 cour1](cour1.md)。正文沿课件 1.1–1.2 的顺序保留公式与图；“课堂来源”标出讲解取材范围，补充算例另行注明。

本章提供描述和表征连续介质运动学所需的基本概念和工具。其目的是根据具体情境，用位移或速度等方式给出运动（即运动学）的表述。

**先抓住本章的问题。** 老师从可变形物体出发：物体可以整体平移、旋转，也可以改变内部质点之间的距离与方向。要讨论“有没有变形”，第一步得先说清每个质点在什么时刻、相对于哪个观察者位于哪里。本章先建立这套描述运动的语言；如何从运动中量出变形，留到第 2 章。泡沫、结构和流体都可以用这条思路分析，只是后续采用的模型不同。

*课堂来源：[cour1](cour1.md)，约第 17–49 行及 281–290 行；此段是课程导入，不把口述的应用范围当成已证明结论。*

## 1.1 时间与空间中的定位（Repérage dans le temps et l'espace）

### 1.1.1 时间的概念（La notion de temps）

在经典力学中，瞬时空间被表示为一个一维实数域上的定向仿射空间 $T$。 每一个瞬时事件都与 $T$ 中的一个元素 $t$ 相对应，该元素表示事件发生的时刻。 伽利略时间测定是一个从 $T$ 到 $\mathbb{R}$ 的严格递增仿射映射。

如果我们固定一个时间原点（即"零时刻" $t_0$，通常取 $t_0 = 0$）以及一个持续时间单位向量 $\vec{u}$（非零且指向正方向），则对于每一个时刻 $t$，可以关联其标量日期 $\tau$，满足：

$$
\overrightarrow{t_0t} = \tau \vec{u} \tag{1.1}
$$

若上述非内在的量（时间原点与方向）一经选定，我们便可混同 $t$ 与 $\tau$，后续中将默认如此处理。

**读法。** $t_0$ 是人为选的计时起点，$\vec u$ 规定“一个时间单位”和正方向；选定后，日常计算便直接用实数 $t$。老师特别说明这段抽象定义只为建立记号，后面主要关心初始时刻与当前时刻的对应。

*课堂来源：[cour1](cour1.md)，第 293–308 行。*

### 1.1.2 观察者的概念（La notion d'observateur）

观察者（亦称为空间观察者或空间参考系）对应于对足够刚性的固体的建模： 这些固体可被视为完全刚性体，因为其不同质点间的距离在时间上保持不变。

一个观察者空间可抽象为带有内积的三维定向欧几里得仿射空间 $E$。 通常，与之关联的固体 $\Sigma$ 仅占据 $E$ 的一部分（有界子集），而 $E$ 的其他点则与 $\Sigma$ 相联系。 在实践中，我们为每一个要建模的固体引入一个类似 $E$ 的空间。

**课堂例子：房间里的气流。** 可以把房间当作观察空间，在其中选择固定的原点和坐标轴，再记录空气质点的位置、速度或某个固定点的温度。房间本身并不是待跟踪的空气质点；“观察者”回答的是**相对于谁测量**。换用一个移动或旋转的观察者，同一质点的坐标和速度可能改变，所以速度记号中要写明参考系 $R$。

*课堂来源：[cour1](cour1.md)，第 311–326、359–383 行。*

给定一个观察者空间 $E$，可以通过以下映射定义点 $M$ 相对于 $E$ 的运动：

$$
T \rightarrow E : t \mapsto M(t) \tag{1.2}
$$

若在 $E$ 中选定一个原点 $O$，则可用向量函数描述点 $M$ 的运动：

$$
T \rightarrow E : t \mapsto \overrightarrow{OM}(t) \tag{1.3}
$$

其中 $\mathbf E$ 表示与仿射空间 $\mathcal E$ 关联的向量空间。

若在 $E$ 中选定一个基（通常为正交归一基）$B = (\vec{e}_1, \vec{e}_2, \vec{e}_3)$， 则运动可由三个标量函数描述：

$$
T \rightarrow \mathbb{R} : t \mapsto x_i(t), \quad (i = 1, 2, 3) \tag{1.4}
$$

这些函数为 $\overrightarrow{OM}(t)$ 在基 $B$ 中的坐标函数，或等价地，是点 $M(t)$ 在正交坐标系 $R = (O; B)$ 中的坐标。 若该坐标系一经选定，则常将观察者空间 $E$、与之相关的固体及坐标系 $R$ 视为同一实体。

在固定的正交基中，$\overrightarrow{OM}=x_i\vec e_i$ 是 $x_1\vec e_1+x_2\vec e_2+x_3\vec e_3$ 的简写：重复指标 $i$ 要从 1 加到 3，这就是课堂引入的 **Einstein 求和约定**。后文写张量分量、梯度和散度时也会用到它。求时间导数时要留意所选基是否固定；式 (1.5)–(1.6) 在固定基下可以直接逐项求导。

*课堂来源：[cour1](cour1.md)，第 335–371 行。*

点 $M$ 在参考系 $R$ 中的速度定义为：

$$
\vec{V}(t, M/R) = \frac{d}{dt} \overrightarrow{OM}(t) = \dot{x}_1(t)\vec{e}_1 + \dot{x}_2(t)\vec{e}_2 + \dot{x}_3(t)\vec{e}_3 \tag{1.5}
$$

加速度为：

$$
\vec{\Gamma}(t, M/R) = \frac{d^2}{dt^2} \overrightarrow{OM}(t) = \ddot{x}_1(t)\vec{e}_1 + \ddot{x}_2(t)\vec{e}_2 + \ddot{x}_3(t)\vec{e}_3 \tag{1.6}
$$

![图 1.1：两个观察者之间的速度合成](Images/IMMC_CM_Chap1_2025-2026_01.jpg)

*图 1.1：速度合成。*

#### 备注 1.1

运动学的真正困难在于将"同一点"在不同观察者中的运动联系起来。 这便是运动合成问题的研究对象（在此假设读者已熟悉，但在后续课程中仅少量使用）。 若 $R = (O; \vec{e}_1, \vec{e}_2, \vec{e}_3)$ 与 $R^* = (O^*; \vec{e}^*_1, \vec{e}^*_2, \vec{e}^*_3)$ 为两个观察者空间，则有（见图 1.1）：

$$
\vec{V}(t, M/R) = \vec{V}(t, M/R^*) + \left[ \vec{V}(t, O^*/R) + \vec{\Omega}(t, R^*/R) \wedge \overrightarrow{O^*M} \right] \tag{1.7}
$$

## 1.2 连续介质的概念（La notion de milieu continu）

此处的目标是描述可变形物体相对于给定观察者 $E$（或坐标系 $R$）的运动。 连续介质力学（Mécanique des Milieux Continus, MMC）的基本假设是： 在所谓"宏观"尺度下研究物理体，忽略物质的离散结构。 因此我们假设，在任意时刻 $t$，待研究的物理体占据空间 $E$ 中的一个"连续"区域。

在这种处理下，每一个构成连续介质的质点都占据一个物质体积单元，其特征最小尺度约为 $10^{-8}\,\mathrm{m}$ 至 $10^{-9}\,\mathrm{m}$。

**连续是建模尺度的选择。** 老师用物体与稀薄气体作对照：从结构尺寸观察一块泡沫，可以把密度、速度等量视为随位置连续变化；放大到原子间隙，或研究极稀薄环境，连续近似就需要重新检验。上面的 $10^{-8}$–$10^{-9}\,\mathrm m$ 是课件给出的示意尺度，不是所有材料与环境通用的最小体积单元。这里的“质点”是连续体模型中的小物质部分，也不是单个原子。

*课堂来源：[cour1](cour1.md)，第 386–398 行；尺度适用性的限定依据课堂对稀薄环境的说明。*

#### 备注 1.2

"连续域"的概念当然可以从数学角度利用拓扑定义严格描述。 但在此初步介绍中，我们仅从直观角度出发加以理解。

### 1.2.1 运动及其表示（Le mouvement et ses représentations）

#### 拉格朗日描述（Description lagrangienne）

为描述运动，定义一个向量映射 $\varphi$ 表示介质的变换：

$$
\varphi : [0, T] \times \Omega_0 \rightarrow E , \quad (t, M_0) \mapsto M = \varphi(t, M_0) \tag{1.8}
$$

其中 $[0, T]$ 为研究的时间区间，$\Omega_0$ 为初始时刻 $t_0$ 的配置，即连续介质在 $t_0$ 时刻所占据的区域（见图 1.2）。 此配置也称为"初始配置"、"参考配置"或有时称为"未变形配置"。 在拉格朗日描述中，物理量定义在与物质点绑定的坐标上，即这些点随物质运动。

**怎样读 $\varphi$？** 给一个初始标签 $M_0$ 和一个时间 $t$，它返回**同一质点**当时的位置 $M$。老师把这比作给每个质点贴标签、让网格随物质移动。$\varphi$ 是一般的运动映射，通常不是线性映射；“固定 $t$，遍历所有 $M_0$”得到整块物体的当前配置，“固定 $M_0$，让 $t$ 变化”得到一个质点的轨迹。不要把这两种固定变量的操作混在一起。

*课堂来源：[cour1](cour1.md)，第 407–470 行；转录中曾口误称 $\varphi$ 为线性映射，此处按课件的一般映射解释。*

固定某一时刻 $t$。当 $M_0$ 遍历 $\Omega_0$ 时，所有点 $M = \varphi(t, M_0)$ 的集合记作 $\Omega(t)$，称为"时刻 $t$ 的配置"（见图 1.2）。 也可称为"当前配置"、"瞬时配置"或"变形配置"。 它是 $\Omega_0$ 经映射 $\varphi_t$ 的像：

$$
\varphi_t : M_0 \mapsto \varphi(t, M_0) \tag{1.9}
$$

因此，配置 $\Omega(t)$（或记作 $\Omega_t$，简写为 $\Omega$）对应于时刻 $t$ 时域内所有质点的位置。

![图 1.2：初始构型到当前构型的拉格朗日映射](Images/IMMC_CM_Chap1_2025-2026_02.jpg)

*图 1.2：拉格朗日描述下质点的当前位置。*

#### 轨迹（Trajectoires）

固定一个 $M_0$，令 $M(t) = \varphi(t, M_0)$，则当 $t \in [0, T]$ 时， 点 $M(t)$ 的集合即为在初始时刻位于 $M_0$ 的质点的轨迹。 根据不同记法，可写作：

$$
\overrightarrow{OM}(t) = \vec{\varphi}(t, \overrightarrow{OM_0}), 
\quad \vec{M}(t) = \vec{\varphi}(t, \vec{M}_0),
\quad M(t) = \varphi(t, M_0) \tag{1.10}
$$

或以坐标形式表示（$i = 1, 2, 3$）：

$$
x_i(t) = \varphi_i(t, X_1, X_2, X_3)
\quad \text{或} \quad
x_i(t) = \varphi_i(t, x^0_1, x^0_2, x^0_3) \tag{1.11}
$$

显然有：

$$
\vec{M}_0 = \vec{\varphi}(t=0, \overrightarrow{OM_0}) \quad \text{且} \quad X_i = \varphi_i(t=0, X_1, X_2, X_3) \tag{1.12}
$$

#### 备注 1.3

映射 $\varphi$ 必须具有足够的光滑性。这对其雅可比行列式（Jacobian）有直接影响。 雅可比行列式 $J(t, M_0)$ 为 $\varphi$ 的梯度矩阵的行列式：

$$
J(t, M_0) = \det[\nabla \varphi(t, M_0)]
$$

它表示某质点当前体积（时刻 $t$）与初始体积（时刻 $t_0$）的比值。 例如，除自然条件 $J(t=0)=1$ 外，还可施加如下约束：

- $\varphi$ 对时间二次可导（除有限多个时刻外，以处理冲击情形）；

- 对于固定的 $t$，要求映射 $M_0\mapsto\varphi(t,M_0)$ 在局部可微逆且整体一一对应，并保持定向；此时可要求 $J(t,M_0)>0$。仅凭“$C^1$ 且双射”不能推出 $J\ne0$，例如 $x\mapsto x^3$ 在 $0$ 的导数为零；

- 若 $\varphi$ 对空间变量连续可导，则 $J$ 连续。

因此，结合 $J(t=0)=1$ 与 $J(t,M_0)\neq0$ 可得：

$$
J(t, M_0) > 0
$$

在更复杂问题（如解的存在性）中，可能需要更严格的光滑性假设，但这些超出入门课程的范围。

**为什么看行列式？** 想象把初始配置里一个很小的体积单元映到当前配置：在局部可逆、足够光滑的条件下，$J=\det\nabla\varphi$ 给出它的有向体积缩放。$J=2$ 对应局部体积约为原来的两倍；$J>0$ 表示不翻转定向。老师用“不能连续地把右手变成镜中的左手”帮助理解保定向。这个例子只是物理直觉，数学上仍须单独要求局部可逆，不能仅由“双射”推出 $J\ne0$。

*课堂来源：[cour1](cour1.md)，第 488–527 行；局部可逆的条件沿用本节已有校对说明。*

#### 备注 1.4

在拉格朗日描述中，任意物理量由函数 $f(t, M_0)$ 表示，即在时刻 $t$ 时，对应于初始位于 $M_0$ 的质点的物理量值。 例如： 

$$
\text{速度：}  \quad \vec{V}(t, M_0) = \frac{\partial \vec{\varphi}(t, M_0)}{\partial t} \tag{1.13}
$$

$$
\text{加速度：}  \quad \vec{\Gamma}(t, M_0) = \frac{\partial^2 \vec{\varphi}(t, M_0)}{\partial t^2} \tag{1.14}
$$

#### 欧拉描述（Description eulérienne）

在欧拉描述中，不偏重任何特定配置。 运动学通过给出每个时刻 $t$ 和位置 $M$ 处的速度场 $\vec{V}^E(t, M)$（相对于参考系 $R$）来描述。 此时，$t$ 与 $M$ 是两个独立变量（见图 1.3）。 在这种描述下，我们通常谈论"流动"（écoulement）而非"运动"（mouvement）。 在欧拉描述中，物理量定义在参考系中的固定空间点上。

**课堂对照：车载 GPS 与路边测速仪。** GPS 跟着一辆车，持续记录“这辆车在哪里”，对应以初始质点 $M_0$ 为标签的拉格朗日描述；路边仪器固定在位置 $M$，记录此刻经过的车的速度，对应欧拉描述。固定空间点处先后经过的通常是不同质点，所以欧拉场 $\vec V^E(t,M)$ 不能直接读成“同一质点随时间的速度变化”。温度探针固定在房间里测气流，也属于后者。

*课堂来源：[cour1](cour1.md)，第 545–587 行。*

![图 1.3：当前构型中的欧拉速度场](Images/IMMC_CM_Chap1_2025-2026_03.jpg)

*图 1.3：欧拉描述下的速度场。*

#### 两种描述的等价性

任何物理场都可以通过拉格朗日或欧拉两种方式表示。 由于 $\varphi$ 是双射映射，可逆，因此对于任意标量函数（拉格朗日形式记为 $f$，欧拉形式记为 ${}^Ef$），有：

$$
{}^Ef(t, M) = f[t, \varphi_t^{-1}(M)] \tag{1.15}
$$

$$
f(t, M_0) = {}^Ef[t, \varphi_t(M_0)] = {}^Ef[t, \varphi(t, M_0)] \tag{1.16}
$$

需注意：

- $f$ 与 ${}^Ef$ 是两个不同的函数；

- 但它们代表同一物理量，对同一质点取相同数值。

这里的“等价”指**给定可逆运动映射时可以换变量**：要问当前点 $M$ 上的欧拉值，先用 $\varphi_t^{-1}$ 找出经过此处的质点原先是谁，再读取其拉格朗日值。两个函数的自变量不同，不能在固定 $M$ 和固定 $M_0$ 之间直接画等号。

*课堂来源：[cour1](cour1.md)，第 590–602 行。*

#### 两种描述之间的转换

1.  若已知运动的拉格朗日描述，可轻易求得欧拉速度场： 

$$
{}^E\vec{V}(t, M) = \vec{V}(t, \varphi_t^{-1}(M)) = \frac{\partial \vec{\varphi}(t, M_0)}{\partial t} \tag{1.17}
$$

2.  若已知 ${}^E\vec{V}(t, M)$ 及初始配置 $\Omega_0$，则（在足够光滑条件下）可重建 $\varphi$： 

$$
\vec V(t,M_0)=\frac{\partial\vec\varphi(t,M_0)}{\partial t}={}^E\vec V\bigl[t,\varphi(t,M_0)\bigr] \tag{1.18}
$$

$$
\frac{\partial \vec{\varphi}(t, M_0)}{\partial t} = {}^E\vec{V}[t, \vec{\varphi}(t, M_0)], 
\quad \text{且} \quad \varphi(t_0, M_0) = M_0 \tag{1.19}
$$

**用一个一维例子走一遍。** 若 $x(t,X)=X+ct$，跟踪初始标签 $X$ 得到的拉格朗日速度是 $\partial_t x=c$；换成当前坐标 $x$ 后，欧拉速度场仍为 $v^E(t,x)=c$。反过来，只给 $v^E=c$，还必须给初始条件 $x(0,X)=X$，才能把 $\dot x=c$ 积分为 $x(t,X)=X+ct$。更一般的速度场也按式 (1.19) 建立常微分方程组，而不能只把 $x$ 改写成 $X$。

*课堂来源：[cour1](cour1.md)，第 602–632 行；常速数值形式是补充算例。*

#### 欧拉描述下的时间导数

设 $f$ 为某物理量，其拉格朗日与欧拉表示分别为：

$$
f(t, M_0), \quad {}^Ef(t, M)
$$

原文先将拉格朗日和欧拉表示并列写为：

$$
f(t,M_0)\quad;\quad {}^Ef(t,M) \tag{1.20}
$$

则两者的时间导数关系为：

$$
\frac{d}{dt}[{}^Ef(t, M)] = \frac{\partial}{\partial t} [{}^Ef(t, \varphi(t, M_0))] = \frac{\partial f(t, M_0)}{\partial t} \tag{1.21}
$$

因此：

$$
\frac{d}{dt}[{}^Ef(t, M)] = 
\underbrace{\frac{\partial {}^Ef}{\partial t}}_{\text{局部导数}} + \underbrace{\nabla_M [{}^Ef] \cdot {}^E\vec{V}}_{\text{对流项}} \tag{1.22}
$$

这种导数称为**物质导数**或**随体导数**，以区别于单纯的时间偏导数。

**课堂要点：道路每天都一样，车仍会减速。** 老师设想车流经过道路收窄处：路边观察者看到的空间速度分布可以不随时间变化，即 $\partial_t v^E=0$；一辆车沿路前进却进入速度较低的区域，仍有加速度。这正是对流项 $v^E\partial_x v^E$ 的作用。比如在 $v^E(x)=v_0-kx$ 的一段路上（$k>0$，且只看 $v^E>0$ 的位置），随车加速度为 $\mathrm dv^E/\mathrm dt=-kv^E<0$。这是帮助读式 (1.22) 的简化算例，并非交通流的完整模型。

*课堂来源：[cour1](cour1.md)，第 635–671 行；$v_0-kx$ 及计算为补充算例。*

原文接着逐步展开全微分。注意原 PDF 的 (1.23) 把左侧自变量误印成 $\mathrm dM$；下式按全微分定义改为 $M$：

$$
\mathrm d[{}^Ef(t,M)]=\frac{\partial[{}^Ef(t,M)]}{\partial t}\,\mathrm dt+\frac{\partial[{}^Ef(t,M)]}{\partial M}\,\mathrm dM \tag{1.23}
$$

两边除以 $\mathrm dt$：

$$
\frac{\mathrm d[{}^Ef(t,M)]}{\mathrm dt}=\frac{\partial[{}^Ef(t,M)]}{\partial t}+\frac{\partial[{}^Ef(t,M)]}{\partial M}\frac{\mathrm dM}{\mathrm dt} \tag{1.24}
$$

代入 $\mathrm dM/\mathrm dt={}^E\vec V$，得到：

$$
\frac{\mathrm d[{}^Ef(t,M)]}{\mathrm dt}=\frac{\partial[{}^Ef(t,M)]}{\partial t}+\overrightarrow{\operatorname{grad}}_M[{}^Ef(t,M)]\cdot{}^E\vec V \tag{1.25}
$$

推广至向量场：

$$
\vec\Gamma(t,M_0)=\frac{\partial\vec V(t,M_0)}{\partial t}=\frac{\mathrm d[{}^E\vec V(t,M)]}{\mathrm dt}=\frac{\partial[{}^E\vec V]}{\partial t}+\frac{\partial[{}^E\vec V]}{\partial M}\frac{\partial M}{\partial t} \tag{1.26}
$$

$$
\begin{aligned}
\vec{\Gamma}(t, M_0) &= \frac{\partial \vec{V}(t, M_0)}{\partial t} 
= \frac{d {}^E\vec{V}(t, M)}{dt} 
= \frac{\partial {}^E\vec{V}}{\partial t} + \nabla_M [{}^E\vec{V}] \cdot {}^E\vec{V} \tag{1.27}
\end{aligned}
$$

#### 备注 1.5

在实践中，拉格朗日与欧拉描述往往用相同符号表示，具体取决于上下文及变量 $(t, M_0)$ 或 $(t, M)$。

#### 备注 1.6

物质导数常记为 $\dot{f}$。 因此在拉格朗日描述下 $\dot{f} = \frac{\partial f}{\partial t}$， 而在欧拉描述下 $\dot{f} = \frac{d f}{dt}$。

#### 流线（Lignes de courant）

::: definition
在给定时刻 $t$，流线是欧拉速度场 $\vec{V}^E(t, M)$ 的包络线。其方程为： 

$$
d\vec{M} \wedge {}^E\vec{V} = \vec{0} \tag{1.28}
$$

:::

流线是**固定一个时刻**后速度场的切线；轨迹则是**固定一个质点**后记录它跨越时间的位置。稳态流中两者重合；反过来，仅凭画出的线重合不能断定速度场不随时间变化。例如 $\vec v(t,x,y)=(a(t),0)$ 且 $a(t)>0$ 时，每个时刻的流线和每个质点的轨迹都沿水平直线，速度大小仍可随时间变化。

*课堂来源：[cour1](cour1.md)，第 689–734 行；$a(t)$ 是补充的反例。*

#### 特殊运动（Mouvements particuliers）

- **稳态运动（Stationnaire）**：若速度场与时间无关，则运动为稳态： 

$$
\frac{\partial {}^E\vec{V}}{\partial t} = 0 \Leftrightarrow \text{稳态运动} \tag{1.29}
$$

- **不可压运动（Incompressible）**：若速度场的散度为零： 

$$
\mathrm{div}\,{}^E\vec{V} = 0 \Leftrightarrow \text{不可压运动} \tag{1.30}
$$

- **无旋运动（Irrotationnel）**：若速度场的旋度为零： 

$$
\mathrm{rot}\,{}^E\vec{V} = \vec{0} \Leftrightarrow \text{无旋运动} \tag{1.31}
$$

#### 备注 1.7

对于稳态运动，轨迹与流线重合。然而二者重合并不必然意味着运动是稳态的。

**三个词分别检查不同性质。** 稳态看欧拉速度是否随时间变化；不可压看物质小体积是否保持不变；无旋看局部旋转速率。不可压不等于“不变形”：把一摞扑克牌顶层横向推开，体积可以不变，层间夹角却改变。刚体运动必然保持体积，但可以旋转、加速，不一定稳态或无旋。

*课堂来源：[cour1](cour1.md)，第 710–749 行；扑克牌是对第 2 章课堂剪切例子的提前提示。*

### 1.2.2 刚体的情形（Cas des corps rigides）

当且仅当满足下式时，连续介质表现为刚体运动：

$$
\forall M, N : \overrightarrow{M(t)N(t)} = Q(t)\,\overrightarrow{M_0 N_0} \tag{1.32}
$$

其中 $Q(t)$ 是保定向的等距变换（即正交算子）：

$$
Q(t)^T Q(t) = I, \quad \det(Q(t)) = 1 \tag{1.33}
$$

**如何判断是否为刚体运动？** 选任意两枚物质标记，看它们之间的向量：若整个运动中只由同一个旋转 $Q(t)$ 改变方向，长度不变，就没有内部变形。$Q^TQ=I$ 保长度和角度，$\det Q=1$ 排除镜像翻转。这里说的是**运动的性质**：一块本来可以变形的泡沫，也可以在某段时间里只做平移或转动。

*课堂来源：[cour1](cour1.md)，第 746–779 行。*

#### 备注：

转置运算符采用英式记号 $M^T$，而非常见的法式 ${}^tM$。

因此刚体运动的拉格朗日表示为：

$$
\overrightarrow{OM}(t) = \vec{\alpha}(t) + Q(t)\,\overrightarrow{OM_0} \tag{1.34}
$$

对于刚体，有：

$$
\forall M, N : \vec{V}(t, N) = \vec{V}(t, M) + \vec{\omega}(t) \wedge \overrightarrow{M(t)N(t)} \tag{1.35}
$$

其中 $\vec{\omega}(t)$ 仅依赖于时间，称为**瞬时角速度向量**。 因此刚体的欧拉速度场由任意时刻某点的速度和 $\vec{\omega}(t)$ 决定。

#### 附注

等距变换保持距离和角度，若其还保持空间定向，则称为“直接”或“保定向”的。 在正交基中，其矩阵为正交矩阵，即其逆等于其转置； 若行列式为 1，则为直接等距。
