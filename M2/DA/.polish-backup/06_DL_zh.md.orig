# 深度学习：表示、反向传播与训练

M2 CHPS · Nicolas Vayatis

> 对照来源：[原课件](<06_DL.pdf>)，共 52 页；[原文转换稿](<06_DL.md>)。按原页顺序翻译，保留原图、表格与文献线索，合并重复过渡标题。正文含为自学补充的解释，图解和校对说明分别标注。历史结果按原页保留，不作为当前系统比较或研究现状结论。

神经网络给出一种表示函数的方式。训练时仍要确定损失、计算梯度并更新参数。本篇先看网络怎样产生预测，再沿链式法则理解反向传播，最后讨论结构和训练过程怎样影响学习结果。

初读先串起[网络表示](#network-function)、[训练目标](#training-objective)、[反向传播](#backpropagation)和[正则化](#regularization)。通用逼近与历史应用可随后回读；[三类误差](#learning-errors)帮助区分表达能力、数据与优化的限制，末尾的研究结论主要用于了解问题边界。

[课程目录](README.md) · [本篇自检](#self-check)

## 学习流程的全貌（第 1–2 页）

![从人工规则到多层表示学习](<Images/06_DL/image_001.jpg>)

**图解：** 规则系统由人写输入到输出的规则；传统机器学习常由人设计特征、算法拟合预测关系；表示学习进一步学习特征；深度学习把表示组织成多层变换。学习改变的是规则和表示如何得到，并没有消除实际执行的数值计算。

以手写数字识别为例，输入是一张图片，输出是各个数字类别的预测。网络先根据当前权重计算预测，再用真实标签衡量误差，最后根据梯度调整权重。理解训练时，可以沿着“输入与权重 → 前向输出 → 损失 → 梯度 → 更新权重”追踪每一步的对象。

## 复习：监督学习与方法的组成（第 3–6 页）

训练数据为 $(X_1,Y_1),\ldots,(X_n,Y_n)$，$X_i$ 是第 $i$ 个样本的特征，$Y_i$ 是标签。选择假设类 $\mathcal H$，对单个样本定义非负损失 $\ell(h(X),Y)$，经验风险为

$$
\widehat L_n(h)=\frac1n\sum_{i=1}^n\ell(h(X_i),Y_i).
$$

**校对说明：** 原第 4 页把序列末项重复写成 $(X_1,Y_1)$，已改为 $(X_n,Y_n)$。

学习方法由损失、假设空间、正则化三个要素描述；算法再指定怎样优化正则化后的经验目标。此前的例子包括一般 ERM、由最小二乘或惩罚最小二乘估计的线性模型，以及核岭回归。

原课件练习：核岭回归的假设空间、损失、正则化和求解方法分别是什么？

对应核的再生核 Hilbert 空间、平方损失、函数范数平方惩罚，以及通过 Gram 矩阵求解线性系统。这把上一课的核模型与下面的神经网络放在同一个比较框架中。

<a id="network-function"></a>

## 深度前馈网络的函数形式（第 7–8 页）

先固定参数，只看输入怎样产生输出。每层把上一层的表示变换成下一层的表示，多层复合后得到预测。层数和连接方式规定能表达哪些函数，训练再从这些函数中选择参数。

![从输入图像到预测的前馈网络](<Images/06_DL/image_002.jpg>)

原文将网络写成复合函数

$$
h(x,\theta)=\sigma_m\circ A_m\circ\sigma_{m-1}\circ\cdots\circ A_2\circ\sigma_1\circ A_1(x),
$$

其中 $\theta=(A_1,\ldots,A_m)$ 是待学习的参数化变换，$\sigma_1,\ldots,\sigma_m$ 是激活函数。层数、每层宽度及激活选择属于结构设计。

可明确写成 $a_0=x$、$z_l=W_la_{l-1}+b_l$、$a_l=\sigma_l(z_l)$，最后输出 $a_m$。这里把 $A_l$ 理解为仿射变换，补明原文省略的偏置。输出层激活要匹配任务，并非每层都必须使用同一种非线性。

**图解：** 原图实际展示了卷积、池化与全连接构成的前馈网络，因此它是前馈结构的一例，不意味着每个 $A_l$ 都是稠密全连接矩阵。

<a id="training-objective"></a>

## 训练目标与随机梯度（第 9 页）

能够算出预测以后，还要把预测与标签比较，得到一个关于网络参数的损失。损失决定训练要改善什么；反向传播负责求出各参数的梯度；随机梯度等优化方法决定根据这些梯度如何更新参数。

![相同网络的训练对象](<Images/06_DL/image_003.jpg>)

目标为

$$
\min_\theta\frac1n\sum_{i=1}^n\ell(h(X_i,\theta),Y_i).
$$

对多层网络参数而言，目标一般非凸。随机梯度以部分样本估计梯度，例如

$$
\theta_{t+1}=\theta_t-\eta_t\nabla_\theta\ell(h(X_{i_t},\theta_t),Y_{i_t}).
$$

实际也可使用小批量的平均梯度。迭代编号 $t$ 与样本编号 $i_t$ 分开，避免把“走一轮数据”误作整个训练。

损失规定预测错在哪里，梯度告诉我们当前权重如何影响损失，优化器根据梯度更新权重。反向传播负责算梯度，SGD 负责利用梯度更新，这两个环节不同。

## 多层表示为什么有用（第 10–11 页）

![多个隐藏层组成的网络](<Images/06_DL/image_004.jpg>)

![人脸输入示例](<Images/06_DL/image_005.jpg>)

![较低层的边缘特征](<Images/06_DL/image_006.jpg>)

![边缘的组合与局部部件](<Images/06_DL/image_007.jpg>)

![更高层的对象模式](<Images/06_DL/image_008.jpg>)

**图解：** 从输入经过多个隐藏层，局部边缘可以被组合成部件，再组合成对象模式。这是一种帮助理解层次表示的示例，不保证任意网络每一层都形成同样可命名的语义。深度首先指输入到输出之间的多层复合。

## 原课件中的应用：视觉、语音与语言（第 12–14 页）

![道路场景的车辆与行人检测](<Images/06_DL/image_009.jpg>)

**图解：** 视觉系统对图像给出类别、位置框与分数。它说明输出可以包含多个对象的位置和类别，不只是给整张图分一类。

![Deep Speech 的语音识别示例](<Images/06_DL/image_010.jpg>)

原课件对比表如下，数值按原页保留：

| 系统 | 清晰语音（94） | 含噪语音（82） | 合并（176） |
|---|---:|---:|---:|
| Apple Dictation | 14.24 | 43.76 | 26.73 |
| Bing Speech | 11.73 | 36.12 | 22.05 |
| Google API | 6.64 | 30.47 | 16.72 |
| wit.ai | 7.94 | 35.06 | 19.41 |
| Deep Speech | 6.56 | 19.06 | 11.85 |

**校对说明：** 本页没有在表头明确写出指标名称、单位与完整实验协议，故不擅自补成已核实的词错误率或百分比，也不能用它判断当前产品性能。括号中的数字按源表保留。

![传统 NLP 流程与深度 NLP 流程](<Images/06_DL/image_011.jpg>)

**图解：** 上方流程包含多种人工预处理与任务特定建模，下方把文档转为稠密表示并经过隐藏层，接到情感、分类、实体抽取、翻译、主题等输出。该图突出学习表示的作用，并非宣称预处理完全不再需要。

## 浅层与深层：历史比较和本课目标（第 15–16 页）

课件以 USPS 手写邮政编码数据为背景，对比两条历史路线：

- LeCun、Boser 等（1989），《将反向传播用于手写邮政编码识别》，发表于 Neural Computation；原页标注约 1000 个单元、70,000 条连接。
- Cortes 与 Vapnik（1995），《支持向量网络》，发表于 Machine Learning；原页以“1 个核、2 个参数”概括其设置。

![USPS 手写邮政编码示例，补自 PDF 第 15 页](<Images/06_DL/pdf_p15_usps.jpg>)

原页用“在视觉任务上达到人类表现的早期算法”描述它们。**校对说明：** 这是历史教学措辞，当前页没有完整的对比协议；“2 个参数”也不能理解为训练后的 SVM 只有两个可学习系数，它还依赖支持向量和对偶系数。

本课希望理解深度学习何时有效、何谓有效，建立优化与工程直觉，并把近似、复杂度、正则化三个问题与此前理论连接起来。

## 神经网络的三个历史阶段（第 17–21 页）

原课件按教学主线划分为：控制论阶段（20 世纪 40–60 年代），建模并训练单个神经元，代表是 Rosenblatt（1958）的感知机；联结主义阶段（80 年代），训练一两层隐藏层，代表是 Rumelhart、Hinton、Williams（1986）的反向传播工作；深度学习阶段（课件标为 2007 年起），训练多层表示，列出 Hinton（2006）、Bengio–LeCun（2007）与随机梯度方法。

![单神经元与感知机更新](<Images/06_DL/image_012.jpg>)

**图解：** 单元先做 $w^Tx$ 再输出类别。配图算法从零权重开始，遇到非正有符号间隔 $y_iw^Tx_i\le0$ 时加上 $y_ix_i$，下方展示分隔面的变化。图中的 0/1 输出示意与更新式的 $\pm1$ 标签约定需要区分。

第二阶段的两个核心问题是：这样的函数族能否表达复杂关系；如果能，怎样训练参数。前者由逼近理论讨论，后者由反向传播等计算方法支持。历史划分不意味着这些思想只存在于相应年代。

## 通用逼近：先理解存在性（第 22–25 页）

原课件以 Stone–Weierstrass 为标题，先给出区间上的多项式一致逼近结论：对连续 $f:[a,b]\to\mathbb R$ 和任意 $\epsilon>0$，存在多项式 $P$ 满足

$$
\sup_{x\in[a,b]}|f(x)-P(x)|<\epsilon.
$$

这个区间特例通常称为 Weierstrass 逼近定理。类似地，一个隐藏层、$N$ 个单元的网络采用

$$
h(x)=c_0+\sum_{k=1}^N c_k\sigma(a_k^Tx+b_k),
\quad a_k\in\mathbb R^d,\quad b_k,c_k,c_0\in\mathbb R.
$$

![单隐藏层网络](<Images/06_DL/image_013.jpg>)

**图解：** 每个隐藏单元先计算不同的输入投影和激活，再由输出层加权组合。这里“单层”指一个隐藏层，不是只有一次线性计算。

![sigmoid、tanh 与 ReLU](<Images/06_DL/image_014.jpg>)

激活示例为 $\sigma(z)=1/(1+e^{-z})$、$\tanh(z)$ 和 $\max(0,z)$。ReLU 在零点不光滑，所以原页对所有激活笼统称“光滑”不准确。

Cybenko 的经典结论使用满足条件的连续 sigmoid 激活：对任意连续 $f:[0,1]^d\to\mathbb R$ 和任意 $\epsilon>0$，存在适当宽度与参数，使

$$
\sup_{x\in[0,1]^d}|f(x)-h(x)|<\epsilon.
$$

原文还列出 Hornik、Stinchcombe、White（1989）和 Barron（1993）的后续工作。

**校对说明：** PDF 第 23、25 页的表达漏了输出权重 $c_k$，偏置下标也不完整；对 sigmoid 而言，原来的无权正值求和甚至无法表示负函数。第 25 页上确界区域还误写成一维 $[a,b]$，已统一为 $[0,1]^d$。

通用逼近回答“某组参数是否存在”，没有给出训练算法必然找到它的保证，也没有保证所需宽度小、样本少或执行快。

<a id="backpropagation"></a>

## 多层感知机与反向传播目标（第 26–28 页）

前面的逼近结论回答了某种网络是否有能力表达目标函数。下面开始处理训练中的实际计算：给定当前参数，怎样知道改变每个参数会让损失怎样变化？多层复合使这个问题可以逐层使用链式法则求解。

多层网络由重复的仿射变换和逐元素激活复合而成：

$$
h(x,\theta)=\sigma\circ A_m\circ\sigma\circ\cdots\circ A_2\circ\sigma\circ A_1(x).
$$

![多层感知机的层间连接](<Images/06_DL/image_015.jpg>)

平方损失下，给定参数 $\theta_1$，先计算

$$
\mathcal L(\theta_1)=\frac1n\sum_{i=1}^n(h(X_i,\theta_1)-Y_i)^2,
$$

再求梯度并更新

$$
\theta_2=\theta_1-\eta\nabla_\theta\mathcal L(\theta_1).
$$

反向传播从输出端的损失导数开始，按链式法则逐层计算损失对中间量和参数的导数。沿反向路径传递的是梯度或向量—Jacobian 乘积。得到梯度以后，才由优化器执行参数更新。

## 选读：链式法则与激活导数（第 29–31 页）

对标量复合函数 $f(u)=\ell(\sigma(g(u)))$，设 $z=g(u)$、$t=\sigma(z)$，则

$$
\frac{df}{du}=\frac{d\ell}{dt}\frac{d\sigma}{dz}\frac{dg}{du}
=\ell'(\sigma(g(u)))\sigma'(g(u))g'(u).
$$

原第 30 页混用了 $u,x$，此处统一。

![常见激活函数及其形状](<Images/06_DL/image_016.jpg>)

sigmoid 的导数为

$$
\sigma'(z)=\sigma(z)(1-\sigma(z)).
$$

链式法则把一次复杂求导拆成每个操作的局部导数与乘法；当一条路径上多个局部导数很小时，前层梯度可能变小。这说明激活选择也会影响训练的数值行为。

## 选读：单神经元的完整梯度（第 32–35 页）

设 $h(x,a)=\sigma(a^Tx)$，平方损失为 $\ell(t,y)=(t-y)^2$，则

$$
\mathcal L(a)=\frac1n\sum_i\ell(\sigma(a^TX_i),Y_i).
$$

对第 $j$ 个权重，

$$
\frac{\partial\mathcal L}{\partial a_j}
=\frac1n\sum_i
\frac{\partial\ell}{\partial t}(\sigma(a^TX_i),Y_i)
\sigma'(a^TX_i)X_{ij}.
$$

令 $z_i=\sigma(a^TX_i)$，使用 sigmoid 与平方损失：

$$
\frac{\partial\mathcal L}{\partial a_j}
=\frac2n\sum_i(z_i-Y_i)z_i(1-z_i)X_{ij}.
$$

$(z_i-Y_i)$ 表示预测与标签的偏差，$z_i(1-z_i)$ 表示激活对输入的敏感度，$X_{ij}$ 表示第 $j$ 个权重如何影响该输入的线性组合。三者相乘再对样本求和，才得到权重的梯度。上式仍是梯度，不是更新后的权重；实际更新还需 $a_j\leftarrow a_j-\eta\,\partial\mathcal L/\partial a_j$。选读部分到此结束。

## 多层网络中的反向传播（第 36 页）

![前向计算沿层间传播](<Images/06_DL/image_017.jpg>)

![损失梯度沿连接反向传播](<Images/06_DL/image_018.jpg>)

**图解：** 前向节点保存激活值；反向节点保存损失对相应量的导数。多条下游路径汇入同一节点时，梯度贡献需要相加。原课件引向 Jake Abernethy 的讲义，但提供的 nbviewer 地址只到课程目录，未给出完整具体文件，本文不补造下载地址。

前一节只求一个神经元的梯度，多层网络则把同样的过程继续向前一层展开：先求损失对输出的导数，再乘上当前层的局部导数，得到损失对上一层输出及当前层参数的导数。逐层重复，就能求出整个网络的参数梯度。得到梯度以后，仍要由优化算法决定怎样更新参数；正确求导不保证一定找到全局最优解。

<a id="learning-errors"></a>

## 三类误差与计算预算（第 37 页）

训练效果不理想时，需要分清三种原因：允许的网络还表达不了目标关系，有限数据不足以选准函数，或者实际优化还没有把训练目标做得足够好。这些原因对应不同改进；增加层数、增加数据和继续训练不能相互替代。

原课件引入近似、估计与优化三项。统一符号：$f^*$ 是真实风险的全局最优函数，$f_{\mathcal F}^*$ 是给定类内真实风险最优函数，$f_n$ 是训练目标的精确最优函数，$\widetilde f_n$ 是实际算法返回的函数。用 $R$ 表示真实风险，则代数上

$$
\begin{aligned}
\mathbb E[R(\widetilde f_n)-R(f^*)]
&=\underbrace{R(f_{\mathcal F}^*)-R(f^*)}_{\mathcal E_{\mathrm{app}}}
+\underbrace{\mathbb E[R(f_n)-R(f_{\mathcal F}^*)]}_{\mathcal E_{\mathrm{est}}}\\
&\quad+\underbrace{\mathbb E[R(\widetilde f_n)-R(f_n)]}_{\text{原页称为 }\mathcal E_{\mathrm{opt}}}.
\end{aligned}
$$

![全局最优、类内最优、经验最优与实际返回函数](<Images/06_DL/image_019.jpg>)

图中符号依次是 $f_{\mathcal D}^*$（真实分布下全局最优）、$h_{\mathcal D}^*$（类内最优）、$h_S^*$（样本上最优）与 $\bar h$（算法返回）。它们分别对应上面的四个函数。

**校对说明：** 最后一项使用真实风险时不一定非负，因为提前停止可能改善泛化。标准非负优化误差通常定义在训练目标上，即 $\widehat R_n(\widetilde f_n)-\inf_{f\in\mathcal F}\widehat R_n(f)$。不能一边使用真实风险差，一边宣称每项都必然是非负误差。

原页还用下表概括趋势，$n$ 为样本数，$\rho$ 为优化容差，$T$ 为计算时间：

| 量 | 函数类扩大 | 样本数增加 | 容差增大、停止条件放宽 |
|---|---|---|---|
| 近似误差 | 下降或不变 | 无直接作用 | 无直接作用 |
| 估计误差 | 可能增加 | 通常下降 | 无直接作用 |
| 优化误差 | 依问题而定 | 依问题而定 | 通常允许更大训练目标误差 |
| 计算时间 | 通常增加 | 通常增加 | 通常减少 |

这张表是设计直觉，不是对所有网络和算法的单调性定理。即使网络表达能力足够、数据也足够，还要看给定计算预算能否把训练目标优化到足够好。

## 深度学习工程与计算图（第 38–40 页）

第三阶段讨论怎样构造深层网络及其理论问题。原课件列出 Theano、Keras、TensorFlow 等以计算图组织计算的软件环境。

![前向计算、反向梯度与数组依赖](<Images/06_DL/image_020.jpg>)

**图解：** 输入数组和参数进入全连接、sigmoid、softmax、损失节点，梯度依照反向依赖计算。图例还区分参数数组、中间数组、梯度存储和共享内存关系。

计算图用节点表示输入或运算，用边表示值的流动与依赖。原文把边上传递的值称作“权重”，在此应理解为该边承载的计算结果，不代表每条数据依赖边都有独立可学习参数。

计算图把一个复合函数拆成相互依赖的步骤。前向按依赖顺序求出预测和损失，反向沿这些依赖应用链式法则，求出损失对各个参数的导数。这样，每一步只需要知道当前运算怎样影响它的输出，就能参与整个网络的求导。

<a id="regularization"></a>

## 深度学习的正则化机制（第 41 页）

原课件列出：

- 权重衰减：抑制参数过大；具体是否等价于 $\ell_2$ 惩罚取决于优化器与实现。
- 权重共享：不同位置共用参数，限制允许的函数结构。
- 提前停止：按验证表现等条件在完全拟合之前停止。
- 模型平均：组合多个模型或状态的预测。
- Dropout：训练时随机屏蔽部分单元或连接。
- 数据增强：使用保持任务标签或目标关系的变换生成训练输入。
- 对抗训练：把对输入扰动的稳健性纳入训练过程。

**校对说明：** 原文将它们总称为隐含在工程中的正则化，但这些方法性质不同：有的显式改变目标，有的限制结构，有的改变数据或优化过程。不能都归为严格意义上的隐式正则化，也不能说它们无条件改善泛化。

## 原课件提供的实现资源（第 42 页）

以下为原页列出的 GitHub 地址，修复了换行，未核验当前仓库状态和依赖兼容性：

- `https://github.com/enggen/Deep-Learning-Coursera/`
- `https://github.com/aymericdamien/TensorFlow-Examples/`

## 网络结构选择（第 43–44 页）

令 $\mathcal T$ 表示结构设置，包括架构、激活函数和正则化方式。给定设置后，训练得到 $\widehat f_{\mathcal T}$，用留出或交叉验证估计预测误差 $\widehat L(\widehat f_{\mathcal T})$。结构选择写成

$$
\min_{\mathcal T}\widehat L(\widehat f_{\mathcal T}).
$$

它一般非凸、不光滑，还可能含离散选择。原课件把它称为元学习问题，并列出经验、启发式、离散优化、实验设计等路线。

内层训练调权重，外层选择决定训练哪一种网络；现代语境下也常称为超参数优化或架构搜索。最小化验证估计不保证得到真实分布上的最佳架构，大量搜索同样可能过拟合验证集。

## 其他网络结构（第 45–47 页）

原课件列出卷积神经网络、循环神经网络、长短期记忆网络 LSTM、自编码器、Boltzmann 机、信念网络，以及生成对抗网络 GAN。这是课件的历史清单，不是当前架构的完整目录。

![卷积、池化、展平与分类输出](<Images/06_DL/image_021.jpg>)

**图解：** 卷积用局部连接与共享权重提取空间特征，池化汇聚局部区域，后续层形成输出。下方方格展示卷积窗口如何移动。同一个局部模式可能出现在图像的不同位置，权重共享让网络能在这些位置使用同一组特征提取规则。

![编码器、瓶颈表示与解码器](<Images/06_DL/image_022.jpg>)

**图解：** 自编码器将 $x$ 编码为 $z=g_\phi(x)$，再解码为 $x'=f_\theta(z)$，希望重构接近原输入。瓶颈或其他约束迫使模型学习有用结构；没有约束的高容量网络可能只学到复制。它与 PCA 的联系是都研究重构，但自编码器可以采用非线性映射。

## 深度学习的三个理论问题（第 48–50 页）

原课件提出：深层网络在逼近上是否优于浅层？高维非凸优化为什么能有效训练？如此大的函数类为什么仍可能泛化？

逼近部分引用 Poggio 与 Liao（2018）的复合函数研究、Liang 与 Srikant（2017）的多项式函数研究，概括出：对某些具有适当结构的目标函数，在达到同样误差时，浅层网络所需神经元数可能比深层网络大得多，甚至存在指数级差距。

**校对说明：** 这种深度优势依赖目标函数、网络、激活与误差定义等条件。原页的简短概括不能推广为“任意函数、任意深网络都比浅网络指数级高效”。所需神经元数量更少，也不直接保证训练更容易或预测更快。

## 非凸优化的研究线索（第 51 页）

![非凸函数的临界点](<Images/06_DL/image_023.jpg>)

图注译文：红点示意不同临界点，(a,c) 为平坦区域，(b,d) 为全局极小值，(e,g) 为局部极大值，(f,h) 为局部极小值。

原课件提出，在某些条件下可能没有坏的局部极小值；梯度类算法可能避开某些不良临界点；更宽网络可能有更有利的优化结构。它列出：

- Soudry 与 Carmon（2016）：关于多层网络训练误差保证与无坏局部极小值的研究。
- Kawaguchi（2016）：关于深度学习中无坏局部极小值的研究。
- Haeffele 与 Vidal（2017）：神经网络训练中的全局最优性。
- Janzamin、Sedghi 与 Anandkumar（2015）：使用张量方法获得神经网络训练保证。
- Panageas 与 Piliouras（2016）：梯度下降、非孤立临界点和不变区域。
- Brutzkus、Alon 等（2017）：在线性可分数据上，过参数化网络的 SGD 学习与泛化保证。

**校对说明：** 这些论文研究的网络、损失、数据与算法条件不同，不能把标题并起来当作所有深网的共同定理。“SGD 一定避开坏点”“所有大网络的局部最优都是全局最优”均超出了本页证据。

## VC 复杂度与最后的问题（第 52 页）

![多个线性切分的组合复杂度](<Images/06_DL/image_024.jpg>)

**图解：** 多个简单切分组合后可以产生复杂区域，用来说明表达能力随组合增大。

原页对使用阶跃激活、具有 $\omega$ 个参数的多层前馈网络，给出

$$
V\le2\omega\log_2(e\omega).
$$

**校对说明：** 此处按 PDF 恢复了对数底数 2。原页没有给出足够的网络结构假设及证明来核定该具体常数界的完整适用范围，因此保留为课件陈述，不将它推广到任意 ReLU、sigmoid 或现代网络。

大函数类的最坏情况复杂度界可能很大，而具体训练后的模型仍可能有效；解释两者之间的差异，需要同时考虑数据结构、网络约束和训练算法。至此，本篇重新回到前几课的主线：能表达什么、从数据中能学准什么，以及实际能优化到什么程度。

<a id="self-check"></a>

## 读完后检查

下面的问题用于自检，不是新增的原课件考题。先尝试用自己的话作答，再回到相应推导检查。

1. 给定网络和参数，一次前向计算得到什么？反向传播计算什么？优化器又改变什么？
2. 通用逼近定理保证某组参数存在，为什么还需要讨论优化误差和泛化误差？
3. 改变学习率、改变网络层数和增加训练样本，分别主要作用于哪个环节？为什么不能把它们当成同一种改进？

完成六篇后，可以用[历年试卷与备考覆盖](../Exams/README.md)检查理解。RANSAC、软 k-means 和概率图模型等考试主题还需要额外材料，不能仅凭读完本篇判断已覆盖全部考试。
