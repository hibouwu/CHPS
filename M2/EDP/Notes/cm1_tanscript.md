La réunion de rentrée pour vous donner ple plein d'informations sur le le déroulement du master en général. Donc, en ce qui concerne le cours, bonjour, vous êtes— Enzo. Oui, bien sûr. Alors, je parle d'abord des intervenants, donc le. Module, a il y a 2 intervenants, essentiellement je prends une— on va dire les 2/3 du module.
开学会议将为您提供关于硕士课程整体进展的丰富信息。关于课程，您好，您是——恩佐。是的，当然。那么，我先介绍一下讲师，模块中主要有两位讲师，我负责模块的大约三分之二内容。

Et je suis secondée par Monsieur Sébastien Bougeot, du CEA, qui va faire une deuxième partie, notamment le TP sur le workload et l'étude de cas, avec la présentation. Comme tous les autres modules de ce master, l'organisation du cours se fait avec, on va dire, un mélange de cours : travaux dirigés pratiques, analyse scientifique. Donc ce n'est pas juste un cours slide et évaluation écrite à la fin, c'est vraiment : on va voir les différents aspects pour.
我由CEA的塞巴斯蒂安·布热奥先生协助，他将负责第二部分，特别是关于工作负载的实践课和案例研究的讲解。与本硕士项目的其他模块一样，课程的组织采用了一种混合方式：包括理论课、实践指导课和科学分析。因此，这不仅仅是幻灯片讲解和期末书面评估，而是我们将全面探讨各个方面。

Travailler le. Avec des concepts au niveau du cours, des travaux dirigés pour appliquer, et des travaux pratiques pour vraiment passer à la programmation et à l'analyalyse, parce que vous êtes quand même un commerciant, et c'est le but de la formation. Les copopaains, négligésez l'aspect scientifique, parce que à la fin de ce master, il y a un grand nombre d'étudiants— ça dépend des promos— mais en général, la moitié va plutôt s'orienter vers la recherche, ou la recherche et développement, pas forcément la recherche scientifique pure. Et donc, on va voir le volet un peu plus concepts et études de cas, avec une analyse scientifique d'articles, des présentations. Alors, l'avantage, c'est que vous allez voir par le biais de cette analyse une étude scientifique aussi appliquée, mais avec des idées scientifiques nouvelles dans le domaine de l'évaluation de performance. Et pour ceux qui voudront s'orienter plutôt vers ces aspects-là, ce sera donc une première approche.
学习课程中的概念，通过指导性作业进行应用，再通过实践作业真正进入编程和分析，因为你们毕竟是商科学生，这也是培训的目的。各位同学，不要忽视科学方面的内容，因为在这个硕士课程结束时，学生人数众多——具体取决于不同届次——但一般来说，大约一半的学生会倾向于从事研究，或者研发工作，而不一定是纯粹的科学研究。因此，我们会更多地关注概念和案例研究部分，结合对文章的科学分析和展示。这样做的好处是，通过这种分析，你们将看到一个应用性的科学研究，同时涉及绩效评估领域中的新科学理念。对于那些希望更多关注这些方面的同学来说，这将是一个初步的入门。

En ce qui concerne l'évaluation, c'est une évaluation, comme dans dans toute université Paris-Saclay et UVSQ, l'évaluation se fait avec, sur le mode de contrôle continu, donc ce ce n'est pas un examen à la fin, mais une série de notes. Donc vous avez un TP noté déjà, avec Monsieur Bougeot, vous avez la présentation orale d'une étude de cas, et vous avez évidemment une évaluation écrite à la fin avec moi.i.
关于评估，评估方式与巴黎萨克雷大学和UVSQ的其他院校相同，采用持续考核的模式，因此不是期末考试，而是一系列成绩。你们已经有了由Bougeot先生评分的实验课成绩，还有一个案例研究的口头陈述，当然，最后还有我负责的书面评估。

Donc, quelques éléments de bibliographographie : Bien sûr, il y a pas mal de livres et de documents sur le web, et certains vont très bien pour. Avoir accès à ces informations,s. maisais je cible j juste 3. Le premier, c'est vraiment une bible pour l'évaluation de performance, c'est. On peut faire plusieurs lectures et plusieurs utilisations de ce livre, parce qu'il balaye vraiment l'évaluation de performance dans plusieurs domaines. Et je disais, plusieurs plusieurs lectures, parce qu'il y a le côté théorique pour ceux qui veulent vraiment se pencher sur la théorie. Il y a le côté application sur des cas pratiques, pour ceux qui veulent plus une utilisation fonctionnelle de.
所以，关于参考书目有几点说明：当然，有很多书籍和网络资料，其中一些非常适合获取这些信息。但我这里只选了三本。第一本真的是绩效评估的圣经。这本书可以多次阅读和多种方式使用，因为它全面涵盖了多个领域的绩效评估。我之所以说多次阅读，是因为它既有理论部分，适合那些真正想深入研究理论的人，也有实际案例应用部分，适合那些更注重功能性使用的人。

Ces concepts. Avec des cas pratiques dans des systèmes réels. Donc vraiment, c'est une bible. Je ne sais pas s'il est toujours édité, mais il y a plusieurs exemplaires à la bibliothèque scientifique de l'université. Il y a également ce. Livre qui est plus sp spécialement dédié à la simulation, avec des principes de base, des méthodologies, des applications, mais surtout vraiment ce que j'aime dans dans ce livre, c'est les applications et et les cas pratiques, parce que vous avez la simulation dans tous les domaines : la finance, la biologie, les applications scientifiques telles que tout ce qui est chimie, physique, matériaux, mécanique, tout ça. Donc c'est vraiment très complet, du point de vue application.
这些概念结合了真实系统中的实际案例。真的可以说是一本圣经。我不知道它是否还在出版，但在大学的科学图书馆里有多本副本。还有这本书，更专门地致力于模拟，涵盖了基本原理、方法论和应用，但我特别喜欢这本书的是它的应用和实际案例，因为模拟应用于各个领域：金融、生物学、科学应用，如化学、物理、材料学、机械学等等。所以从应用的角度来看，它真的非常全面。

Et vous avez le dernier, qui est vraiment une référence dans le monde de la modélisation en m mathématique pour l'évaluation de performance, et qui concerne surtout le systèèmemes de file d'attente, parce que elles sont vraiment très utilisées dans le domaine. Donc le CLEROC, c'est vraiment une référence, et vous avez également plusieurs exemplaires à la bibliothèque scientifique de l'université.
最后一个是真正的数学建模领域中性能评估的权威参考，主要涉及排队系统，因为它们在该领域中被广泛使用。所以CLEROC确实是一个权威参考，科学图书馆里也有多本副本。

Et vous avez, pour ceux qui sont inscrits, vous avez votre carte et vous pouvez emprunter le livre. Alors, en ce qui concerne le module et l'organisation des différents cours volets qu'on va voir pendant ce module, donc. To Tout d'abord, comme dans tout cours, on va commencer par une introduction avec les concepts de base, l'application au HPC, les métriques utilisées, comment les choisir, quels sont les méthodes et comment les choisir. Ensuite, on va voir les 3 3 techniques emblématiques d'évaluation de performance, notamment la modélisation mathématique, la simulation et la mesure.
对于已注册的同学，您会有您的借书卡，可以借阅图书。关于本模块及其各个课程部分的安排，首先，和所有课程一样，我们将从介绍开始，讲解基本概念、高性能计算（HPC）的应用、所使用的指标、如何选择指标以及相关方法的选择。接着，我们将学习三种典型的性能评估技术，特别是数学建模、仿真和测量。

Donc dans l'ordre, : du du plus plus théoriqueque du du plus plus abstrait et conceptutuel, au plus proche de la machine. M Mais quelque chose de commun à ces 3 techniques, c. Quelque chose de commun, c'est les charges de travail. Donc ces workloads vontont nourrir la modélisation, et la simulation et la mesure, avec des détails différents, à des niveaux d' d'abstraction différents.
因此，按照顺序，从最理论化、最抽象和概念化的，到最接近机器的。但这三种技术有一个共同点，那就是工作负载。这些工作负载将为建模、仿真和测量提供支持，虽然细节不同，抽象层次也不同。

Donc on va voir d'abord les charges de travail.il, en français Donc comment les représenter, comment les générer, comment les caractériser, comment les analyser. Ensuite, on va voir les 3 tech techniques. Alors, pour ce cours-là, ce pour les concepts et l'application au HPC, il y a un PD, donc cet après-midi. Ensuite, il y a le cours sur sur les workloads qui sera suivi du TP noté avec Monsieur Bougeot.ot,ot.
首先，我们将了解工作负载：如何表示、生成、描述和分析它们。接着，我们将学习三种技术。关于本课程，即高性能计算（HPC）的概念与应用，今天下午有一个讲座。随后，将有关于工作负载的课程，紧接着是由Bougeot先生主讲的带评分的实验课。

La modélisation mathématique, comme elle est plus théorique, donc ce sera un PD. Là aussi, ce sera un PD. Sur l'application des lois de. Calculer certaines métriques. Là, c'est un PD. Pour la simulation et la mesure, donc on va faire un PD avec, qui va concerner les mesures des tests statistiques qu'on va utiliser, donc qui sont très utilisés sur les résultats issus de la simulation et de la la mes mesure pour les corriger et.
数学建模更偏理论，因此这将是一个研究项目。在这里，也将是一个研究项目。关于定律的应用，计算某些指标，这也是一个研究项目。对于仿真和测量，我们将进行一个研究项目，涉及我们将使用的统计测试测量，这些测试在仿真结果和测量结果中被广泛应用，用于对结果进行校正。

Voir comment les caractériser. Et s'ils sont représentatifs ou pas, toutes les problématiques qu'on verra dans la sim simulation et la la mesure. Alors, les retardataires qui sont ven venusus, il y en a deuxure ou trois qui sont rentrés. Depuis quelque temps, je je ne ne sais pas si vous êtes en mobilisation.
看看如何对它们进行特征描述，以及它们是否具有代表性，所有我们将在仿真和测量中看到的问题。至于那些迟到的人，有两三个已经进来了。不知道你们是否正在动员中。

Alors, au départ, aujourd'hui, les concepts de base, en ce qui concerne l'évaluation de performance, alors tout d'abord, avant de. Je vous remets les slides après le cours. Je fais d'abord le cours et après je vous envoie les slides. Et alors, normalement, je les mets sur eCampus, je vais juste voir s'ils ont ouvert le.
那么，首先，今天我们讲的是关于绩效评估的基本概念。首先，在开始之前，我会在课后把幻灯片发给大家。我先讲课，然后再把幻灯片发给大家。通常我会把它们上传到eCampus，我只是想确认他们是否已经开放了。

Dépôt pour ce cours cette année. Et je vous mets normalement tous les supports : cours et PD sur eCampus. Bonjour, vous êtes. Alors, on va d'abord voir la motivation pour l'évaluation de performance et la probl problémématique qui se pose avec ces évaluations-là. Quelle est l'approche à suivre ? Donc là, c'est beauaucoup plus des.
本课程今年的资料已上传。我通常会把所有的教学材料：课程内容和练习题上传到eCampus。大家好，你们好。首先，我们将讨论绩效评估的动机以及这些评估所面临的问题。应该采取什么方法？这里主要是关于……

Méthodes, des points à suivre, des étapes à suivre. Avec une checklist pour éviter les erreurs. Cette checklist est tr très, très connue, elle est suivie, c'est'est vraiment une référence, quasiment une norme, qui est donnée dans le livre, justement, le livre que j'ai mis en premier, « The Laura Right Jane.
方法、要遵循的要点和步骤。附有一份检查清单以避免错误。这份检查清单非常非常著名，被广泛采用，几乎成为一种标准，正是在我首先提到的那本书《The Laura Right Jane》中提供的参考。

Donc je la mets quand même aussi, même si pour ceux qui ne l'ont pas pris, le livre, alors vous êtes le retardataire ou dernier qui est rentré ? C'est quel nom ? Ensuite, on va voir les métriques de base à évaluer. Donc quand je dis métriques de base, c'est-à-dire que quel que soit le champ d'application que.
所以我还是放上它，尽管对于那些没有拿到书的人来说，那么你是最后一个进来的人吗？叫什么名字？接下来，我们将看看需要评估的基本指标。所谓基本指标，就是无论应用领域是什么。

Ce soit de la biologie, de la physique, de la chimie, de la finance, peu importe, quels sont les métriques de base à évaluer. On verra par la suite sur le deuxième volet quels sont les métriques qui sont très parmi ceux-là qui sont très utilisés dans le HPC. Comment caractériser un bon métrique ? Et quels sont les méthodes à adopter pour évaluer les performances ?
无论是生物学、物理学、化学还是金融，关键在于评估哪些基本指标。接下来在第二部分，我们将探讨在高性能计算（HPC）中哪些指标被广泛使用。如何定义一个好的指标？以及评估性能应采用哪些方法？

Ensuite, donc ça c'est le premier volet un peu plus global pour l'évaluation de performance. Dans n'importe quel système, donc n'importe quel contexte, et après on va voir un peu les spécificités du calcul de performance, quels sont les objectifs qu'on veut atteindre pour évaluer les performances quand.
接下来，这是关于绩效评估的第一个更为整体的部分。无论在任何系统或任何环境中，我们都会探讨绩效计算的具体细节，以及我们希望达到的目标，以便评估绩效。

On évalue les performances dans le HPC, on n'est plus axé parallélisme et performance que dans les autres domaines. Parce qu'onon est très proche de la machine, et puis on travaille toujours sur l'adaptation et très étroite entre soft et hard. On va voir quels sont les métriques parmi ceux qu'on a.
在高性能计算（HPC）中评估性能时，我们比其他领域更注重并行性和性能。因为我们非常接近机器，并且始终致力于软件与硬件之间的紧密适配。接下来我们将看看我们拥有的指标有哪些。

Qui sont très, très utilisés pour le HPC. Quelques modèles d'évaluation et les méthodes. Alors, je vais juste ra rappppelereler quel est le but de tout système. Donc n'importe quel système informatique, pete. Le contexte ou l'usage, sont. But, c'est de fournir le meilleur rapport possible entre le coût et la performance.ce.
它们在高性能计算（HPC）中被广泛使用。一些评估模型和方法。现在，我将简单回顾一下任何系统的目标。任何计算系统，无论其背景或用途，目标都是在成本和性能之间提供尽可能最佳的平衡。

Le coût peut englober donc le coût initial plus la maintenance, et la performance elle peut être. Calculée en représentée par différentes manières. Ça dépend du contexte. Ici, on peut des contextes où on s'intéresse au temps, il y a des contextes où on s'intéresse à la fiabilité, il y a des contextes où on s'intéresse à.
成本可以包括初始成本加上维护费用，性能则可以通过不同的方式来计算和表示，这取决于具体的情境。在某些情境中，我们关注时间，在另一些情境中，我们关注可靠性，还有一些情境中，我们关注其他方面。

L'exactitude de la précision, donc la performance elle peut av avoir plusieurs aspects. Donc quand on a un système, on veut évaluer ses performances. Cette évaluation de performance, elle peut se faire à différentes étapes, et elle doit se faire sur toutes les étapes du cycle de vie du système.
准确性和精确性，也就是性能，可能具有多个方面。因此，当我们有一个系统时，我们希望评估其性能。性能评估可以在不同阶段进行，并且必须贯穿系统生命周期的所有阶段。

Donc d'abord à la conception. Même si on ne dit pas vraiment évaluation de performance à cette étape-là, mais c'est l'évaluation de performance à l'étape conception, ça concerne la prédiction, mais pas la prédiction dans le sens des. Big data et l'IA actuellement, la prédiction c'est faire un.
首先是设计阶段。虽然在这个阶段我们并不真正称之为性能评估，但这实际上是设计阶段的性能评估，涉及预测，不过这里的预测并不是指当前大数据和人工智能意义上的预测，预测就是做出一个。

Design du système, une conception du système, et dire qu quels sont les outilsils que vous allez utiliser, quels sont les modules que vous allez utiliser, quels sont les services que vous allez off offrir pour ce système. Et quels sont les marges de. Performance que vous voulez atteindre. Je ne sais pas moi si sur j'aimerais être compétitif sur le temps et être en dessous des 10 secondes que les systèmes actuels offrent, j'aimerais travailler sur des programmes 10 fois plus complexes que ceux qui sont là, actuellement. Donc faire un peu un cahier des charges du système et.
系统设计，即系统的构思，说明您将使用哪些工具，使用哪些模块，为该系统提供哪些服务。以及您希望达到的性能指标。我不知道，比如说，我希望在响应时间上具有竞争力，低于当前系统提供的10秒钟；我希望处理的程序复杂度是现有系统的十倍左右。因此，需要制定一份系统需求说明书。

Qu'on veut évaluer. Ensuite, on va passer à la réalisation. Une fois qu'on a fait notre cahier des charges,es, on va utiliser notre cahier des charges, notre design du système, on passe à la réalisation et à ce moment-là, l'évaluation de performance va concerner le choix du matériel et du du logiciel.
我们想要评估的内容。接下来，我们将进入实施阶段。一旦我们完成了需求规格说明书，我们将利用需求规格说明书和系统设计，进入实施阶段，此时性能评估将涉及硬件和软件的选择。

Donc parmi le logiciel et le matériel existant, qu'est-ce qui convient le plus et qui répond le mieux aux exigences de mon cahier des charges ? Et comment interconnecter cela ? Des fois, on prend du matériel ou du logiciel qui existe, mais on demande également si ça ne répond pas à l'évolution que je vais faire de mon système. Je peux également anticiper et commencer à développer un logiciel qui s'adaptera mieux ou à étendre les logiciels existants pour l'évolution que je vais donner à mon système. Parce que quand on conçoit un système et on le réalise, on on le réalise pour qu'il puisse vivre un certain temps.
那么，在现有的软件和硬件中，哪一款最适合且最能满足我的需求规格？又该如何将它们互联？有时我们会选择现成的硬件或软件，但同时也要考虑它们是否能满足我对系统未来发展的需求。我还可以提前预见，开始开发更适合的软 件，或者扩展现有软件，以适应我对系统的未来规划。因为当我们设计并实现一个系统时，是希望它能够运行一段较长的时间。

Une fois le système réalisé, on passe à la commercialisation. Donc à cette phase-là, l'évaluation de performance sera juste axée sur les métriquques, donc tout ce qui est temps, vitesse, débit, rés. Aux pannes, etc. Donc l'évaluation de performance ici, elle est plus quantitative que. Pour évaluer les métriques, pour pouvoir donc vendre, commercialiser le système qu'on a construit.
系统完成后，进入商业化阶段。因此，在这个阶段，性能评估将主要侧重于指标，如时间、速度、吞吐量、故障率等。因此，这里的性能评估更多是定量的，目的是评估这些指标，以便能够销售和推广我们构建的系统。

Vient après l'utilisation. Une fois qu'on a commercialisé notre système et qu'il est utilisé, donc l'utilisation dépend toujours du contexte. Parce que que la même machine, le même logiciel peut peut être utilisé dans différents contextes, et donc on s'attend de ce système on n'a pas les mêmes attentes selon le con contexte où on est. Et donc on va faire du tuning ou le paramétrage ou reparamétrage
使用之后。一旦我们的系统投入市场并被使用，使用情况总是取决于具体的环境。因为同一台机器、同一款软件可能会在不同的环境中使用，因此我们对该系统的期望也会根据所处的环境而有所不同。因此，我们会进行调优、参数设置或重新参数设置。

donc selon le contexte, il. Du systèe. Donc le tuning, c'est vraiment une partie importante parce que qu'on va prendre des applications représentatives du système, des benchmarks représentatifs du contexte, pardon, et on va les exécuter, on va changer les paramètreses, et adapter la machine et le système au contexte qu'on a.
因此，根据具体情况，调优确实是一个非常重要的环节，因为我们会选取系统中具有代表性的应用程序，以及具有代表性的基准测试，抱歉，是代表具体环境的基准测试，然后执行它们，改变参数，调整机器和系统以适应我们所处的环境。

Ensuite, donc le système est utilisé, mais il est utilisé sur un laps de temps le plus long possible. Ça serait bien pour justement avoir un bon rapport qualité-performance/ coûût. Don Donc on a réalisé ça à à cô côté à un certain enfin c'était un un certain coût, la commercialisation, l'acheter, on l'utilise, et donc il dure longtemps et qu'il reste performant avec l'évolution donc des demandes et là c'est plutôt la mise à jour et la maintenance. Alors il y a le contrôle pour la sûreté du fonctionnement, et il y a le monitoring pour une adaptation continue.
然后，系统被使用，但尽可能在较长的时间内使用。这样做有助于实现良好的性能与成本的平衡。我们在这方面付出了一定的成本，包括商业化和购买，使用后系统能够长时间保持性能，并随着需求的变化不断发展。这主要依赖于系统的更新和维护。同时，有安全运行的控制措施，以及持续适应的监控机制。

Parce que le contexte est évolué tout le temps, donc c'est un peu du tuning sur la durée. Le tuning initial, c c'est pour adapter au contexte, fixer les paramètres donc selon le contexte, et ensuite on continue à faire du tuning qui s'appelle à ce moment-là du monitoring mais de manière très fine pour adapter c'est ça on reste dans le même contexte mais les choses évoluentent dans le temps, les applications évoluent, leurord demande évolue,e. Donc même si c'est dans le même contexte, on continue à faire du tuning de manière plus fine.
因为环境一直在变化，所以这是一种持续的微调。初始微调是为了适应环境，确定参数，根据环境进行调整，之后我们继续进行微调，这时称为监控，但以非常细致的方式进行调整，以适应变化。虽然仍处于同一环境，但随着时间推移，事物在发展，应用在演进，需求也在变化。因此，即使在同一环境中，我们也会继续进行更细致的微调。

Donc l'évolution de la l'évaluation de performance peut prendre plusieurs aspts. Selon le. Moment de vie du système. Donc à partir du tout début de la conception, enut cas la maintenance. Pendant l'exécution à long terme. L'utilisation à long terme. Alors quels sont les objectifs de cette étude de performance ? Donc déjà au au to tout début, au niveau de la conception et de la réalisation, c'est surtout on est plus axé sur la faisabilité que sur les performances.
因此，性能评估的发展可以有多个方面，取决于系统的生命周期阶段。从设计之初，到维护阶段，再到长期运行和使用。那么，这项性能研究的目标是什么呢？在设计和实现的最初阶段，重点更多是可行性，而不是性能。

Donc on veut déjà fabriquer ce syst système et voir qu'il fonctionne, après on verra ses performances par la suite. Donc une fois qu'on a réalisé la. Faisabilité, on arrête la faisabilité, on va donc passer à comprendre les limites du système, pousser le système dans ses limites pour voir je peux répondre aux demandes mon système est réalisable, je peux répondre aux demandes du cahier des charges mais jusqu'à quel point. Donc faisabilité, limitation, et une fois que j'ai vu les limitations, comment am améliéliorer donc ce système pour pousser encore plus ses ses limitations et faire en sorte que mon système puisse suivre les évolutions des demandes dans dans le temps et donc je pourrais atteindre ceit.
所以我们首先想制造这个系统并确认它能正常工作，之后再观察它的性能。一旦完成了可行性验证，我们就停止可行性阶段，接着会去了解系统的限制，推动系统达到极限，看看我的系统是否能够满足需求规格说明书的要求，但能满足到什么程度。也就是说，先是可行性，然后是限制，了解了限制之后，再考虑如何改进系统，进一步突破这些限制，使系统能够跟上需求随时间的变化，从而实现目标。

Doffre performance coût et même augmenter ce trait d'offre. Donc les améliorations possibles, ça peut être des améliorations matérielles donc rajouter des processeurs, rajouter des de la mémoire, rajouter des ordinateurs, changer le réseau, avoir un réseau encore plus rapide. Ça peut être des améliorations logicielles donc comment réorganiser les traitements, regarder comment paralléliser plus les les tâchches, retravailler mes workflow,ows, comment réorganiser mes charges pour qu'elles puissent mes données pour qu'elles puissent être.
提供性能和成本优势，甚至提升这一供应特性。因此，可能的改进包括硬件方面的改进，比如增加处理器、增加内存、增加计算机数量、更换网络设备，拥有更快速的网络；也可以是软件方面的改进，比如如何重新组织处理流程，研究如何更好地并行化任务，重新设计工作流程，如何重新安排负载，使数据能够更有效地处理。

Accessibles en très peu de temps, doncc voilà les améliorations sur les deux volets logiciels et matériel. Alors maintenant quelle est l'approche à suivre ? Donc il y a plusieurs étapes. Donc d'abord définir le système, donc ça c'est au niveau de la conception, définir le système et les buts à atteindre ensuite on va lister les services que peut fournir ce système. N'importeorte quel système, il peut fournir plusieurs services
可以在很短的时间内实现，以下是软件和硬件两个方面的改进。那么现在应该采取什么方法呢？有几个步骤。首先是定义系统，这属于设计阶段，确定系统及其要达到的目标。接下来，我们会列出该系统可以提供的服务。任何系统都可以提供多种服务。

et ensuite sé sélectionner les métriques par rapport aux services. On a choisi donc est-ce que je vais prendre plutôt les débits parce que je suis dans un service financier et je m'intéresse au nombre de transactions faites par seconde ou bien je suis dans un système de traitement batch où je vais plutôt m'intéresser au job par minute, est-ce que je suis dans un système transactionnel, est-ce que je suis dans un système scientifiique sur une application scientifique trèsès pointue de physique, chimie, de simulation, et là ce qui m'intéresse c'est plutôt la précision. Donc il y a est-ce que je suis dans un milieu contraint scientifique contraint où ce qui m'intéresse c'est que l'application s'exécute dans un temps précis donc c'est pas forcément la précision, c'est pas forcément la performance, je ne veux pas forcément que mon application s'exécute vite, mais je veux qu'elle s'exécute en un temps T parce que par la suite il y a d'autres applications qui ont des comptes qui attendent des résultats. Donc vous avez plusieurs exigences exigences que ce soit la performance pure donc réaliser le traitement le plus le plus rapide possible, la précision, dans les milieux contraints donc c'est l'application est exécutée dans un temps précis, pas pas for forcément rapide mais dans un temps précis, il y a également le souci de l'énergie, l'application est exécutée en.
然后根据服务选择相应的指标。我们需要决定是关注吞吐量，因为我处于金融服务领域，关注每秒处理的交易数量；还是处于批处理系统，更关注每分钟完成的作业数量；或者我处于事务处理系统，亦或是在一个非常专业的科学应用中，比如物理、化学或仿真领域，这时我更关注的是精确度。也就是说，我是否处于一个受限的科学环境中，关注的是应用程序能否在特定时间内完成执行，这时重点不一定是精度，也不一定是性能，我不一定要求应用程序运行得很快，但我希望它能在某个确定的时间内完成，因为后续还有其他应用程序等待结果。因此，存在多种需求：纯粹的性能，即尽可能快速地完成处理；精确度；在受限环境中，应用程序必须在特定时间内执行，速度不一定快，但必须准时完成；还有能耗问题，应用程序的执行也需考虑能源消耗。

Consommantant le mo moins d'énergie possible, notamment les applications dans des des milieux un peu contraints je sais pas moi maintenant on met des des capteurs de surveillance dans l'attendra dans les déserts pour voir pour les applications climatiques et donc je veux que ça consomme le moins possible parce que c'est des endroits où on il n'y a pas accès tout le temps donc on met les machines pour faire les calculs capter les données et faire les calculs mais donc il y a plusieurs servicesices à fournir et donc on choisit les métriques par rapport aux services.
尽可能减少能量消耗，特别是在一些受限环境中的应用。比如，现在我们会在沙漠中安装监测传感器，用于气候应用。因此，我希望它们的能耗尽可能低，因为这些地方并不总是有人值守。我们安装设备来采集数据并进行计算，但需要提供多种服务，所以我们会根据服务需求选择相应的指标。

On établit les paramètres donc et après on choisit les facteurs. Alors les paramètres c'est tous les paramètres tous les éléments qui peuvent influencer impacter ce que vous voulez calculer. Donc La taille des codes, la taille des données, la taille des éléments de mémoire sur lesquels vous allez mettre vos codes et vos et vos données pour accéder, donc la vitesse des.
我们先确定参数，然后再选择因素。参数是所有可能影响你想要计算内容的因素。比如代码的大小、数据的大小、存放代码和数据的内存单元的大小，以及访问速度等。

C Câbles de communication tout ça, donc tous les paramètres qui qui peuvent impacter les performances ensuite on va en général les paramètres c'est la liste des paramètres est très très grande donc on va sélectionner les plus importants qu'on appellera les facteurs et on va essayer de faire une évaluation en balayant toutes les valeurs que peuvent prendre ces facteurs pour.
通信电缆等所有可能影响性能的参数，通常参数列表非常庞大，因此我们会选择最重要的参数，称之为因素，并尝试通过遍历这些因素可能取的所有数值来进行评估。

Voir et prédire les performances si mes câbles de communication changent si je fais changer ma taille mémoire si mes codes changent je passe d'un code de taille N à un code de taille deux fois N ou quatre fois N donc je vais essayer de balayer toutes les valeurs possibles ou intéressantes pour évaluer les performances de mon système. Ensuite je sélectionne la méthode donc il y a trois méthodes principales il y a la modélisation mathématique la simulation et et la mesure je choisis la méthode appropriée alors ça dépend ça dépend du contexte forcément ça dépend aussi surtout de la.
查看并预测如果我的通信电缆发生变化、如果我更改内存大小、如果我的代码发生变化——比如从大小为N的代码变为大小为2N或4N的代码——系统的性能表现。因此，我会尝试扫描所有可能或有意义的数值，以评估系统的性能。接着，我选择方法，主要有三种方法：数学建模、仿真和测量。我会选择合适的方法，这当然取决于具体的情境，也主要取决于……

Période ou l'étape à laquelle je suis dans ma dans. L'élaboration de mon système. Au début quand je suis dans la conception et. Je ne veux pas faire de la mesure par exemple doncc là je suis à la conception j'ai pris une machine dit logiciel ni rien donc je peux faire je fais j'ai fait mon design je ne peux faire que la modélisation de.
我目前所处的阶段是系统的制定阶段。一开始我处于设计阶段，比如说我不想进行测量，所以我处于设计阶段，我没有使用任何机器或软件。我只能进行设计和建模。

Je ne peux faire que la modélisation mathématique donc quand je le fais je conçois mon système j'ai le flux de données entre les différentsents modules et je peux réaliser un modèle mathématique que ce soit par les fils d'attente ou autre et je peux étudier donc évaluer mon système de manière quantitative mais très haut niveau très approximatif ensuite quand je suis au niveau de la réalisation donc.
我只能进行数学建模，所以当我做这件事时，我设计我的系统，确定不同模块之间的数据流，并且可以建立一个数学模型，无论是通过排队论还是其他方法。我能够以定量的方式研究和评估我的系统，但这是非常高层次且非常粗略的。然后，当我进入实现阶段时，情况就不同了。

Là je fais mon choix de matériel et de mes logiciels là. Aussi je peux faire de la modélisation mais. À un niveau un peu plus fin parce qu'ici je donne des valeurs bon selon mon expertise je peux donner des valeurs plus représentatives mais quand je suis dans la réalisation j'ai des données plus fines qui sont propres au matériel que je choisis et le logiciel que je choisis pour paraméétrtrer donc mon modèle mathématique et avoir une une évaluation beaucoup plus repr représésentative de.
现在我正在选择我的硬件和软件。我也可以进行建模，不过是在更精细的层面上。因为这里我给出的是基于我的专业知识的数值，可以提供更具代表性的数值，但当我进入实际操作时，我会有更精细的数据，这些数据是针对我选择的硬件和软件来参数化我的数学模型，从而获得更具代表性的评估。

Du mat Matériériel et du logiciel que j'ai choisi pour mon système donc ça sera une modélisationisation un pe peu plus fine. Ensuite quand je construis mon ou avant la construction réalisation c'était quoi après commercialisation je crois je. L'utilisation donc déjà là. Je fais de la modélisation fine mais je peux aussi faire de la simulation je peux faire un simulateur et là je peux reproduire si j'ai pas l'ensemble du matériel ou je peux où je me dis tiens il y a du matériel qui va sortir dans deux trois ans je vais faire un simulateur qui va reprendre les fonctions qui pe peuvent être disponibles à court terme et je peux faire une simulation pour voir comment mon système va réagir quand ce matériel sera mis à disposition donc je peux voir ensuite quand je réalise mon système pour le commercialiser je dois faire une évaluation quantitative et.
我选择了用于我的系统的硬件和软件，因此这将是一个更精细的建模。然后，在构建之前或构建过程中，接下来是商业化，我想是关于使用的部分。到目前为止，我已经进行了精细建模，但我也可以进行仿真，制作一个模拟器。如果我没有全部的硬件，或者我想说，有些硬件将在两三年内推出，我可以制作一个模拟器，涵盖那些短期内可能可用的功能，通过仿真来观察当这些硬件投入使用时我的系统会如何反应。然后，当我实现我的系统并准备商业化时，我必须进行定量评估。

Contextuelle. Et là je fais de la simulation et surtout de la mesure si je veux vendre je vais prendre des benchmarks de telle et telle contexte je vais faire de la mesure je vais évaluer la performance de mon système et je vais le vendre dire voilà vous vous êtes des physiciens chimistes vous faites beaucoup de simulation vous avez des codes de tel type des données de telle taille etc des workflows de telle nature voilà j'ai pris des benchmarks représentatifs de votre contexte et voilà comment se comporte mon système avec avec.
情境相关。在这里，我进行模拟，尤其是测量。如果我要销售，我会采用某些情境的基准测试，进行测量，评估我的系统性能，然后进行销售。就是说，你们是物理化学家，做大量模拟，拥有某种类型的代码、某种规模的数据，以及某种性质的工作流程。这里我选取了与你们情境代表性的基准测试，展示我的系统在这些条件下的表现。

Votre votre. Dans votre contexte pareil je vais voir les gens de la finance je vais prendre des benchmarks leur application typique je vais faire des tests ah non ici on peut faire la mes mesure mais ça sera toujours un peu plus du règle de la mesure. Je dirais pas théorique parce que ça va pas mais.
您的您的。在类似的情况下，我会去找金融领域的人，参考他们的典型应用，进行测试。哦不，这里我们可以进行测量，但这总会有点偏离测量规则。我不会说是理论上的，因为那样不合适。

Idéaliser le cas idéal et. Parce que quand on est dans quit. Le dép déploiement vraiment en production des de ces systèmes là ça va certainement les performancesces vont bouger mais. On va faire la commercialisation selon ces performances ces mesures un peu dans le cadre idéal. Parce que les applications réelles ne sont vraiment ne sont pas tout à fait tout à fait pareil que les benchmarks qui reprennent juste les noyaux des traitements et.
理想化理想情况。因为当我们处于退出状态时，这些系统真正部署到生产环境中，性能肯定会发生变化。但我们会根据这些性能指标在理想框架下进行商业推广。因为实际应用与基准测试并不完全相同，基准测试只是涵盖了处理的核心部分。

Als. Ensuite on va sélectionner les charges qu'elles soient synthétiques ou réelles alors synthétique c'est surtout quand je suis je fais de la modélisation mathématique et aussi de la simulation donc je sais que telle et tellele application ont un certain Utilise certaines données je sais comment el elleles se comportent comment elles sont elles se représentent donc je vais les générer je vais écrire un programme qui va générer donc ces ces charges de travail je vais les utiliser donc c'est pour ça qu'on dit synthétique parce que on les génère nous-mêmes donc on contrôle tout toute leur e.
首先，我们将选择负载，无论是合成的还是实际的。合成负载主要是在我进行数学建模和仿真时使用的，因为我知道某些应用程序会使用特定的数据，我了解它们的行为和表现形式，所以我会生成这些负载，编写程序来生成这些工作负载并使用它们。因此，我们称之为合成负载，因为是我们自己生成的，所以我们可以完全控制它们的所有特性。

Modification enfin toutes leurs valeurs euh et on peut peut faire les tests avec ensuite on peut quand on est dans les mesures on peut utiliser des traces réelles donc qui sont mesurées sur des vrais systèmes sur des avec des vraiesies applications et on va dire voilà telle application quand je l'utilise sur telle machine voilà la trace que j'ai donc je reprends cette trace et je vais l'utiliser sur une autre machine pour voir un peu comment elle se comporte donc ça dépend à quel moment je suis dans mon dans mon système alors concevoir après des jeux de test donc je suis dans la le système est réalisé je faisais ma mes jeux de test pour tester les limites pour voir comment optimiser pour aller au-delà de ces limites de suite aprs.
最后修改所有的数值，然后我们可以进行测试。接着，当我们进行测量时，可以使用真实的跟踪数据，这些数据是在真实系统和真实应用上测量得到的。比如说，当我在某台机器上使用某个应用时，我得到了相应的跟踪数据。然后我会拿这份跟踪数据，在另一台机器上使用，看看它的表现如何。这取决于我在系统中的哪个阶段。之后设计测试用例，当系统完成后，我会进行测试，测试系统的极限，看看如何优化，突破这些极限。

Bon interpréter les données avec des tests statistiques et. Pour enlever le bruit pour surtout quand on fait de la mesure on a beaucoup de bruit sur les données donc pour enlever le bruit ou tout simplement on l'enlève pas mais on peut dire que dans l'état actuel elle les résultats que j'ai sont trop impactés par le bruit donc je peux pas les exploiter je refais etc e.
正确地使用统计测试来解释数据。尤其是在进行测量时，数据中通常会有很多噪声。为了去除噪声，或者有时不去除噪声，但可以说明目前的结果受到噪声的影响太大，因此无法利用这些结果，需要重新进行测量等。

Également des fois on a des résultats numériques mais avec des tests statistiques on peut voir quelle est la loi de probabilité qui du. Distribution qui qui épouse plus le relief de de ces résultats pour pouvoir les générer par la suite de manière synthétique mais très représentative et après donc la dernière étape présenter les résultats alors présenter les résultats çaa va ça peut vous paraître voilà une étape qui n'est pas vraiment importante mais attention des fois on a des résultats mais on sait pas les mettre en valeur on sait pas choisir la forme graphique la plus appropriée donc je prends un un histogramme alors que il faut prendre autre chose parce que les résultats varient beaucoup donc vaut mieux faire un moton de sp splot ou autre chose je.
有时候我们会得到数值结果，但通过统计检验，我们可以确定哪种概率分布更符合这些结果的特征，从而能够以合成且具有代表性的方式生成这些数据。最后一步是展示结果。展示结果这一步看似不重要，但要注意，有时我们有了结果，却不知道如何突出它们，也不知道如何选择最合适的图形形式。比如我选择了直方图，但其实应该选择其他图表，因为结果变化很大，最好用箱线图或其他类型的图表。

Neisis pas la la bonne échelle et donc la représentation n'est pas vraiment claire il y a des des points importants de changement de comportement que je ne couvre pas donc la présentation et le choix de de. La forme graphique est très important également alors ici je vaisais pas m'attarder sur ça c'est juste pour dire.
没有使用正确的比例尺，因此表示并不十分清晰，有一些重要的行为变化点我没有涵盖，所以演示和图形形式的选择也非常重要。这里我不会详细讲解，这只是想说明这一点。

Qu'il y a une liste donc de d's. À éviter qui a été établie par Euganeène et donc c'est vraiment une annexe qu'on ne couvre parce que je pense qu'il y a pas beaucocoup de livres à la bibliothèque on a trois ou quatre e. Donc peut-être qu'ils ont ramené beaucoup plus je sais plus donc là je la mets dans le cours parce que c'est vraiment je dirais pas une norme mais mais quasiment une norme dans dans les vagues de verre donc j'ai mis en rouge celles qui sont vraiment les plus importantes donc toujours bien cibler le problème et les services bien déterminer les métriques de performance qui sont relatives à ces services regarder sur la charge de de travailil que je j'utilise pour les tests qu'elle soit synthétique ou réelle est-ce qu'elle est vraiment correcte et appropriée quelle est la technique que je dois utiliser est-ce que j'ai bes besoin juste d'avoir une idée globale et donc j'utilise la les la modélisation mathématique ou est-ce que j'ai besoin d'aller dans les détails et et que j'ai la le moyen de faire de la simulation et de la mesure euh est-ce que mes jeux de test euh sont bien faits pour regarder toutes tous les aspects de du fonctionnement de mon système et est-ce que je peux les les réaliser dans le temps imparti parce que des fois les tests durent très longtemps et donc même s'ils sont bien conçus en fait ils arrivent après le besoin euh est-ce que les données sont bien représentées analysées est-ce que les erreurs sont.
有一份由Eugène制定的需要避免的清单，这实际上是一个附录，我们没有详细覆盖，因为我认为图书馆里相关书籍不多，只有三四本。也许他们带来了更多，我不太确定。所以我把它放在课程里，因为这实际上可以说不是一个标准，但几乎是玻璃波段中的一个准则。我用红色标出了最重要的部分。始终要准确定位问题和服务，明确与这些服务相关的性能指标，查看我用于测试的工作负载，无论是合成的还是实际的，它是否真正正确且适当。我应该使用什么技术？我是否只需要一个整体的概念，因此使用数学建模？还是我需要深入细节，并且有条件进行仿真和测量？我的测试集是否设计得当，能够覆盖系统运行的各个方面？我能否在规定时间内完成测试？因为有时测试时间很长，即使设计得很好，实际上也可能赶不上需求。数据是否被充分代表和分析？错误是否被...

Significatives et peuvent changer les résultats donc là c'est étude de la sensibilité de mon système et est-ce que les. Players ont été bien traités doncc quand vous avez un point où le comportement du système change il faut vraiment traiter ce point-là donc voilà après c'est la même chose des des points à e.
这些因素具有重要意义，可能会改变结果，因此这是对我的系统进行敏感性分析，看看参与者是否得到了妥善处理。当系统行为发生变化的临界点出现时，必须认真处理这一点。总之，后面也是类似的情况。

Est-ce que le workload a été bien modélisé pour les futurs changements parcece que que le système est conçu réalisé pour vivre très longtemps il faut voir l'évolution du système ça fait partie il faut pas contenter un cahier des charges on répond pas juste aux besoins actuels on répond aux besoins actuels mais à la suite dans sur les années à à ven venir de ce besoin comment ce besoin va évoluer et comment je peux répondre aux bes besoins actuels et à son évolution dans le futur et et après donc est-ce que l'analyse que que vous avez fait est facile à expliquer parce que s'il y a que que vous qui qui qui.
工作负载是否已针对未来的变化进行了良好的建模？因为系统设计和实现是为了长期运行，我们必须考虑系统的发展演变。这是设计的一部分，不能仅满足于一份需求规格说明书。我们不仅要满足当前的需求，还要考虑未来几年这些需求将如何演变，以及如何同时满足当前需求及其未来的发展。那么，你们所做的分析是否容易解释？因为如果只有你们自己能理解的话……

L'analyse vous pouvez pas commercialiser votre système et la représentation également il faut la l'adapter à l'audience alors maintenant on va va passer aux choses plus plus. Techniques et. Doncc on va d'abord identifier les métriques communs que vous avez déjà entendu donc vous avez déjà entendu parler utiliser dans plusieurs domaines parcece que vous avez des parcours un peu différents mais c'est un peu donc les métriques communs dans les vagues de perf c'est d'abord le temps de réponse c c'est vraiment le premier métrique parce que tous les utilisateurs sont très sensibles au temps tout le monde va aller plus vite tout le monde veut même si pas aller vite mais avoir la réponse sans retard on va dire je veux pas aller vite mais je veux pas être retardé non plus le débitbit qui a une conséquence un peu de de temps c'est fournirir beaucoup de travail dans un temps dans une une un unité de temps l'accélération je suis dans une situation mais mais si je change comment je peux accélérer mon système et comment je peux surtout quantifier parce que là c'est des mét métriques communs mais que quantifier.
分析方面，您不能仅仅推广您的系统和展示内容，还需要根据受众进行调整。现在我们将进入更技术性的内容。首先，我们会识别一些您可能已经听说过的常见指标，这些指标在多个领域都有应用，因为大家的路径略有不同，但这些是性能波动中的常见指标。首先是响应时间，这是最重要的指标，因为所有用户都非常关注时间。每个人都希望速度更快，或者至少希望得到无延迟的响应。虽然不一定追求快，但也不希望被延迟。其次是吞吐量，它与时间有关，指的是在单位时间内完成的工作量。还有加速比，假设我处于某种状态，如果我改变条件，如何加速我的系统，尤其是如何量化这种加速。以上是一些常见的指标，但关键是如何进行量化。

Pour quantifier les performances d'un système parce que quand vousus av avez parlé de votre système il faut parler avec des valeurs numériques pour que les choses soient claires pour vous soient claires pour ceux qui vont l'utiliser et éventuellement l'acheter et. L'efficacité alors l'efficacité euh elle dépend de l'accélération mais elle dépend aussi de l'investissement.
为了量化一个系统的性能，当你介绍你的系统时，必须使用数值来说明，这样对你自己以及将要使用或可能购买该系统的人来说，事情才会清晰明了。效率方面，效率不仅取决于加速性能，还取决于投入。

L'investissement c'est pas uniquement en termes de de coût financier ça peut être en coût mat matériériel en engagement et donc est-ce que avec l'accélération que j'ai les le temps avec lequel dans lequel s' s'exécute mes applications est-ce que rapporter à l'investissement que j'ai mis dans ce système-là est-ce que je suis bien est-ce que j'ai un bon rapport est-ce que mon investissement est justifié ça vaut la peine parce que j'ai beaucoup accéléré les traitements donc là c'est vraiment un un argument très important le taux d'utilisation j'ai fait un investissement mais est-ce qu'il est bien ut utilisé est-ce que j'ai surdimensionné euh la machine mes demandes ne sont pas si importantes mais du coup j'utilise pas les ressources que j'ai achetées donc mon investissement n'est pas très très rentabilisé il y a une forte connexion entre l'efficacité et le taux d'utilisation euh ce taux d'utilisation me permet aussi de faire de l'optimisationisation et.
投资不仅仅是财务成本，还可能包括物质成本和投入的时间与精力。因此，随着应用程序执行速度的加快，我投入到该系统中的投资是否得到了合理回报？我的投资是否合理？是否值得？因为我大大加快了处理速度，这确实是一个非常重要的论点。利用率方面，我进行了投资，但是否得到了充分利用？我是否配置过度了？如果我的需求并不大，那么我购买的资源就没有被充分使用，投资的回报率就不高。效率与利用率之间有着密切的联系，而利用率也帮助我进行优化。

Si une ressource ne sont pas utilisées de la même m manière ou elles sont sous-utilisées je vais optimiser leur utilisation pour les les utiliser de manière plus intelligente beaucoup plus euh je peux également euh jouer sur la durée de vie l'usure. Est-ce que je n'utilise pas de manière égale mes ressources donc il y en en a qui vont être usées plus rapidement que d'autres et ça peut déstabiliser un peu le fonctionnement de la globalité de mon système donc je peux faire cette étude d'usure et je peux après proposer une stratégie et.
如果某些资源没有以相同的方式使用，或者被低估利用，我会优化它们的使用，使其更加智能化。此外，我还可以调整资源的使用寿命和磨损情况。是否我的资源使用不均衡，有些资源会比其他资源更快磨损，这可能会影响整个系统的运行稳定性。因此，我可以进行磨损分析，然后提出相应的策略。

D'équilibrage de charge approprié et. Alors la fiabilité estce. Que les données que j'utilise les résultats que je retourne sont fiables ou pas la fiabilité c'est pas la disponibilité parce que souvent on mélange les deux la disponibilité c'est mon système est en fonctionnement mais je ne suis pas sûre donc je me pose pas la question sur les résultats qu'il me fournit est-ce qu'ils sont euh corrects ou pas est-ce que mes données sont cohérentes ou pas donc elles sont disponibles je peux les lire mais je ne sais pas si elles sont cohérentes correctes ou pas la fiabilité c'est un métrique qui me dit les données sont correctes garanaranties correctes les résultats sont garantis précis donc ça travaille plus sur les erreurs et.
适当的负载均衡。那么，可靠性是什么呢？我使用的数据和返回的结果是否可靠？可靠性不是可用性，因为这两者常常被混淆。可用性是指我的系统正在运行，但我不确定它提供的结果是否正确，或者我的数据是否一致。也就是说，数据是可用的，我可以读取它们，但我不知道它们是否一致或正确。可靠性是一个指标，它告诉我数据是正确且有保证的，结果是准确且有保障的，因此它更多地关注错误方面。

Là c'est sur les pannes donc voilà les métriques que j'ai et maintenant on peut facilement d'ailleurs j'ai des perf facilement rajouter la consommation d'énergie parce que c'est devenu vraiment vraiment très très important et quand on présente maintenant d'ailleurs dans le le top 500 des machines maintenant on a la performance mais on a également à côté la colonne de l'énergie donc lesP.
这里讲的是故障情况，这就是我拥有的指标。现在我们可以很容易地——顺便说一下，我有性能数据——轻松地加入能耗数据，因为这变得非常非常重要。现在在展示500强机器时，不仅有性能指标，还有旁边的能耗栏。

U qui qui me donnent tous ces toutes ces performances mais qu'on sort beaucoup d'énergie donc il faut faire le le trad tradeer donc voilà les métriques communs pas forcément et pas uniquement pour le HPC mais c'est commun donc sur surtout les systèmes et. Alors on va les voir un à un als. D'abordord le temps de réponse donc le temps de réponse 1 unant.
这些性能指标虽然消耗大量能量，但必须进行权衡。因此，这些指标不仅适用于高性能计算（HPC），而且是通用的，尤其是在系统方面。接下来我们将逐一查看。首先是响应时间，也就是响应时间的概念。

Le temps de réponse c'est le temps qui sépare la requête utilisateur de la réponse du système c'est vraiment entre ces deux points-là je donne la requête et j'attends d'avoir la réponse et le temps entre les deux deux c'est le temps de réponse donc le temps de réponse il est composé de l'attente parce qu'il y a pas que moi qui envoie des requêtes à ce système donc le temps de d'attente et le temps de ce qu'on appelle le temps de service ou le temps de de d'exécution de cette requête donc il y a le temps d'attente et le temps de d'exécution il est composé des deux quand le système n'est pas très utilisé donc le temps de service en général ile.
响应时间是指用户发出请求到系统给出响应之间的时间。也就是说，在这两个点之间，我发出请求，然后等待得到响应，这段时间就是响应时间。响应时间包括等待时间，因为不仅仅是我一个人在向系统发送请求，所以存在等待时间；同时还有所谓的服务时间或请求执行时间，也就是执行该请求所需的时间。因此，响应时间由等待时间和执行时间两部分组成。当系统使用率不高时，服务时间通常较短。

Pas le temps de traitement de ma requête il change pas mais le temps d'attente par contre lui peut beaucoup varier quand vous avez un système chargé donc le temps d'attente va augmenter et du coup il va faire augmenter le temps de réponse si vous avez un système qui est peu chargé donc le temps de d'attente est est très peu et est très bas donc vous aurez un système un temps de de de réponse qui est plus bas donc ça dépend de la charge du système et.
处理我的请求的时间不会改变，但等待时间会有很大变化。当系统负载较高时，等待时间会增加，从而导致响应时间变长；而当系统负载较低时，等待时间非常短，响应时间也会更快。因此，这取决于系统的负载情况。

Alors en ce qui concerne toujours le temps de réponse vous avez ce qu'on appelle le facteur d'extension ou le stretch factor si enfin le c'est vrai qu'on utilise beaucoup le terme anglophone mais bon en français c'est le facteur d'extension c'est quoi le facteur d'extension c'est tout simplement le rapport entre le temps de réponse pour certaines charges pour certaines euh utilisations et.
关于响应时间，您会听到所谓的扩展因子（stretch factor），虽然我们经常使用英文术语，但法语中称之为扩展因子。扩展因子是什么呢？它就是某些负载或某些使用情况下的响应时间与基准响应时间的比值。

Rapporté à à ce temps quand la charge est minimale et. Donc on calcule on envoie une requête sur le le système qui est analysé avec une utilisation on le met dans une situation où l'utilisation est vraiment minimale et on envoie donc le temps de d'attente est vraiment presque négligeable on va dire et on on calcculeule le temps de réponse ensuite donc.
在系统负载最小的情况下进行测量。我们发送请求到被分析的系统，使其处于使用率极低的状态，此时等待时间几乎可以忽略不计。然后，我们计算响应时间。

On est dans une autre situation de de de charge dans laquelle on veut on que qu'on veut analyser on calcule le temps de réponse et on fait le rapport entre les les deux temps et on dit quand je passe d'une charge minimale à la charge actuelle donc je vais multiplier par exemple le le temps par un facteur x et ce facteur c'est le rapport entre les deux temps aussi donc le le facteur d'extension à ce moment-là qu'est-ce qu'il il joue le rôle de quoi il joue tout simplement le rôle de du du deice.
我们处于另一种负载情况中，想要进行分析，计算响应时间，并比较两个时间的比值。当负载从最小值增加到当前负载时，比如我会将时间乘以一个因子x，这个因子也是两个时间的比值。那么，这个扩展因子在此时起什么作用呢？它实际上起到了延迟的作用。

De l'augmentation de la charge sur l'impact de cette augmentation de charge sur l'allongement du temps est-ce que quand j'augmente la charge le temps augmente très vite ou est-ce qu'il augmente moins vite et à ce moment-là ça vous donne une une idée sur l'utilisation des des ressources est-ce que elles sont très très utilisées ou peu utilisées ou donc ici vo.
关于负载增加对时间延长的影响，当我增加负载时，时间是迅速增加还是增加得较慢？这可以让您了解资源的使用情况，是被大量使用还是较少使用。这里就是这样。

Le le le fac facteur d'extension c'est un indice sur l'impact de charge et. Sur le temps notamment c'est un indice indicateur précis et très très ut utilisé et. Ensuite on passe au débit le débit c'est quoi c'est juste la vitesse c'est t toujours calculé de la même manière c'est quand vous prenez votre voiture la vitesse c'est quoi c'est le nombre de kilomètres que vousus faites sur le temps que vous avez utilisé pour parcourir cette distance donc ça reste exactement le la même idée sauf que quand vous êtes dans un système interactif c'est le nombre de requêtes que vous avez envoyées qui ont été exécutées sur le temps donc mais le temps exprime en secondes pour avoir le nombre de requêtes par seconde quand on on est dans un système de calcul et qu'on s' s'intéresse uniquement au calcul donc le débit ou la vitesse de calcul va s'exprimer en x millions d' d'instructions par seconde au mégaflop donc e.
扩展因子是一个关于负载影响的指标，特别是在时间方面，它是一个精确且非常常用的指标。接下来谈谈吞吐量，吞吐量是什么？它就是速度，计算方式始终相同。就像开车时，速度是你在一定时间内行驶的公里数，原理完全一样。只不过在交互式系统中，吞吐量是指你发送并执行的请求数量除以时间，时间以秒为单位，从而得到每秒请求数。在计算系统中，如果只关注计算性能，吞吐量或计算速度通常以每秒执行的百万条指令数（MIPS）或每秒百万浮点运算次数（MFLOPS）来表示。

Le le nombre de millions d'instructions flottantes par seconde quand on s'intéresseesse à des systèmes de traitement batch donc c'est le nombre de jobs par seconde quand on s'intéresse au début réseau donc c'est le nombre de paquets ou de bits par seconde et quand on s'intéresse au système transactionnel qui soit bancaire qui soit le.
浮点运算每秒的数量适用于批处理系统；作业每秒的数量适用于网络交换系统，即每秒的数据包或比特数；而事务处理系统，无论是银行系统还是其他类型的系统，则关注相应的事务处理量。

E-commerce qui soit donc c'est le nombre de transactions par seconde alors en général la seconde c'est l'unité de temps et on calcule donc le nombre de de travaux qui soit requête instruction ou instruction flottante ou job ou pa paququet ou transaction donc cette unité de temps donc ce métrique-là il est un peu particulier parce que regardez il a plusieurs unités ça dépend du contexte alors que le temps il a une seule unité c'est la seconde ou la nanoseconde ça dépend à quel niveau mais il a une seule unité alors le le débit le débit toujours augmente avec la charge donc vous augmentez le le nombre de requêtes vous nourrissez vous envoyez plus de requêtes donc le le débit augmente jusqu'à un certain point quand on arrive à à la charge maximale quand on sature le système là le débit n'augmente plus au contraire là le temps d'attente va augmenter et du coup votre temps de réponse va augmenter et le débit va stagner il est est sature le système saturé donc on arrive à la borne supérieure par exemple c'est la borne passçonante du réseseau ou donc on sature le système et à ce moment-là le débit n'augmente plus et.
电子商务中的交易量通常指的是每秒的交易次数。一般来说，秒是时间的单位，我们计算的是工作量，可以是请求、指令、浮点指令、作业、数据包或交易等。因此，这个时间单位和指标有些特殊，因为它有多种单位，具体取决于上下文。而时间只有一个单位，即秒或纳秒，取决于测量的精度，但单位是唯一的。吞吐量随着负载的增加而增加，也就是说，当你增加请求数量，发送更多请求时，吞吐量会提升，直到达到某个临界点。当系统达到最大负载，饱和时，吞吐量不再增加，反而等待时间会增加，响应时间也会变长，吞吐量趋于稳定，系统进入饱和状态。此时达到上限，比如网络的带宽上限，系统饱和，吞吐量不再提升。

Als. Pareil donc pour le temps on a vu le le stretch factor le facteur de donc là pour le débit on va voir le débit d'usage et le début et le débit nominal le débit nominal ou on peut dire aussi la capacité nominale c'est le débit max qu'on peut atteindre qu'on peut avoir lorsqu'on a une charge idéale et.
同样地，对于时间，我们已经看到了伸展因子。那么对于流量，我们将看到实际流量和额定流量。额定流量，也可以称为额定容量，是在理想负载条件下可以达到的最大流量。

Donc par exemple je prends e. Un pipeline d'exécution et il y a aucune dépendance donc je n'ai aucune pénalité j'ai un pipeline d'exécution j'envoie mes instructions l'une après l'autre la première est là le cycle suivant la première est là la deuxième est là le cycle suivant la première est là la deuxième est là la troisième est là et là toutes les exécutions donc s'il y a pas de dépendance si je casse pas le pipeline j'aurai après une ex exécution par cycle donc c'est vraiment le cas idéal ou bien j'ai un un multiprorocesseur avec des tâches qui sont complètement indépépendendantes donc j'ai p processeur et des tâches indépendantes donc je peux lancer p tâches en même temps elles s'exécutent et elles se terminent donc c'est vraiment le cas idéal où je n'ai aucune dépendance aucune synchronisation à faire aucune donc ça c'est vraiment ça donne le débit nominal la la référence on va dire ensuite vous avez le le débit d'usage et.
例如，我有一个执行流水线，并且没有任何依赖关系，所以我没有任何惩罚。我有一个执行流水线，我依次发送指令，第一条指令在第一个周期执行，第二个周期第一条指令执行，第二条指令到达，第三个周期第一条、第二条和第三条指令都在执行。如果没有依赖关系，也没有打断流水线，那么我每个周期可以完成一次执行。这是真正的理想情况。或者我有一个多处理器系统，任务之间完全独立，我有p个处理器和独立的任务，可以同时启动p个任务，它们同时执行并完成。这是真正的理想情况，没有任何依赖关系，也不需要同步操作，这样才能达到标称吞吐量，也就是参考值。接下来是实际吞吐量。

Le débit d'usage c'est un débit maximal comme ici mais pas du tout dans des conditions idéales c'est le débit maximal qu'on peut avoir et. En respectant une limite imposée sur le temps donc par exemple je dis qui. Est-ce que quel est le débit limite que je peux avoir la vitesse maximale que je peux avoir si j'impose une limite sur le temps d'exécution de mes de mes programmes ou de mes requêtes et je ne veux pas attendre plus de 10 minutes donc le temps de réponse ne doit pas être supérieur à 10 minut minutes donc combien de requêtes max je peux exécuter sans dépasser ce temps-là si une requête est lancée et au bout de 10 minutes je n'ai pas la réponse donc je ne vais pas la compter dans les requêtes exécutées et donc mon débit il va il va être impacté et.
使用吞吐量是指最大吞吐量，但并非在理想条件下，而是在遵守时间限制的情况下所能达到的最大吞吐量。比如，我会问：如果对程序或请求的执行时间设定限制，且不希望等待超过10分钟，那么响应时间就不能超过10分钟。在这种情况下，我最多能执行多少请求而不超过这个时间限制？如果某个请求启动后超过10分钟仍未得到响应，那么我不会将其计入已执行的请求中，因此我的吞吐量会受到影响。

Donc c'est quelque chose qui est plus réaliste qui représente plus le système donc quand vous lancez vos codes vous par partagez une machine avec toute l'université vous lancez vos codes et vous dites si allez je lance mon code le soir et le lendemain je veux avoir la réponse donc si le lendemain matin vous arrivez et que vous n'avez pas l'exécution les résultats de votre code parce qu'il y a beaucoup de de de demandes sur la la machine donc vous laissez tomber votre vous annulez votre code vous n'allez pas le compter et on va compter juste les les les les programmes qui se sont terminés dans ce temps-là dans cette limite-là et on va calculer le débit donc le nombre d'exécutions réalisées en respectant cette limite temporelle dans ce temps-là sur la durée que que vo vous avez donc c'est plus représentatif de des contraintes d'exécution réelles alors en ce qui concerne toujours le débit mais là c'est plus donc pour le calcul pour.
所以这更现实，更能代表系统的情况。因为当你运行代码时，你是与整个大学共享一台机器的。你运行代码时会想，“好吧，我晚上运行代码，第二天早上想得到结果。”但是如果第二天早上你到达时，代码还没有执行完毕，也没有结果，那是因为机器上有很多请求。所以你会放弃，取消代码，不会计入计算。我们只计算在这个时间限制内完成的程序，并据此计算吞吐量，也就是在这段时间内完成的执行次数。这更能反映实际的执行约束。关于吞吐量的计算，这就是方法。

Performance on s'intéresse donc aller plus vite exécuter le plus d'instructions possible dans un temps le moindre le moindre possible le plus petit possible donc vous avez d'abord le million d'instructions par seconde donc vous voyez déjà qu'on est dans une autre échelle on exécute un million d'instructions par seconde plusieurs millions même donc les instructions peu importe n'importe quelle instruction qu'elle soit entière flottante autre peu importe donc le.
性能方面，我们关注的是尽可能快地执行尽可能多的指令，在尽可能短的时间内完成。首先是每秒百万条指令，您可以看到我们已经处于另一个量级，每秒执行数百万条指令。指令的类型无关紧要，无论是整数指令、浮点指令还是其他类型，都没有区别。

Formule elle est enend elle est dans le nom même million d'instructions par seconde donc c'est le nombre d'instructions que j'ai exécutées sur le temps d'exécution mais je le divise d'abord par 10^6 parce que je m'intéresse au nombre de millions donc je calcule d'abord le nombre de millions que j'ai d'instructions ensuite j'utilise par le le temps d'exécution exprimé en seconde et.
公式本身就在名称中——每秒百万条指令数，因此它是我在执行时间内执行的指令数量，但我首先将其除以10^6，因为我关注的是百万的数量，所以我先计算指令的百万数，然后再除以以秒为单位的执行时间。

Je peuxux avoir mon NIPS donc après en général dans les codes scientifiques on a beaucoup de calculs flottants donc je vais plutôt m'intéresser au mégaflops toujours le million opération flottante. Par seconde c'est exactement la même chose mais au lieu de faire nombre d'instructions je vais
我可以有我的NIPS吗？一般来说，在科学计算中，我们有大量的浮点运算，所以我更关注的是兆浮点运算（megaflops），即每秒百万次浮点运算。这和指令数量是一样的，只不过我关注的是浮点操作的数量。

Plutôt compter juste les instructions flottantes et je vais diviser par 10^6 parce que je m'intéresse au nombre de millions et par le temps d'exécution donc de ces instructions-là alors c'est euh c'est deux métriques de débit de vitesse qui sont utilisées dans le HPC donc on ne s'intéresse là enfin dans le HPC et surtout pour le calcul pas juste parce que dans le le HPC il y a trois je dirais trois axes il y a le calcul.
我更倾向于只计算浮点指令，然后除以10的6次方，因为我关注的是以百万为单位的指令数量以及这些指令的执行时间。这是高性能计算（HPC）中使用的两种性能指标。我们关注的主要是在HPC中，尤其是计算方面，而不仅仅是因为在HPC中，我认为有三个方面，其中之一是计算。

Donc tout ce qui est compute il y a les données et. Il y a la communication et. Ces trois volets-là donc si on s'intéresse au calcul on va prendre ces deux métriques et surtout celui-là en fait et puisqu'on s' s'intéresse au calcul c'est un peu biaisé donc on va se focaliser sur le calcul et on va pas prendendre en considération le temps d'action donné parce que le temps d'accès aux données est beaucoup plus grand que le temps de calcul et donc il va vraiment faire chuter le augmenter le temps d'exécution et donc faire chuter ce ce métrique donc pour les deux métriques on considère que les données sont en mémoire donc on va pas prendre en considération le temps de rapatrier sur toute la la chaîne le le stockage hiérarchique voilà on on se dit les instructions les les données peu importe le code ou les données sont tous en mémoire donc on s'intéresse dans le temps d'exécution ici il n'y a que le calcul il n'y a pas le temps d'accès aux données donc c'est pas c'est des indicateurs de performance CPU de ou compute de CPU GPU c'est des indicateurs de performance compute et non pas e.
所以，计算包括数据和通信这两个方面。如果我们关注计算，就会采用这两个指标，尤其是后者。由于我们关注的是计算，这有些偏颇，因此我们将专注于计算，而不考虑数据访问时间，因为数据访问时间远远大于计算时间，这会显著增加执行时间，从而降低该指标的表现。对于这两个指标，我们假设数据都在内存中，因此不考虑从整个层级存储中调取数据的时间。也就是说，无论是指令还是数据，无论是代码还是数据，都假设全部驻留在内存中。因此，我们关注的执行时间仅包括计算时间，不包括数据访问时间。这些指标反映的是CPU或GPU的计算性能，而不是其他性能指标。

Globaux on va dire. Alors maintenant je passe à l'accélération et. Alors l'accélération c'est un un métrique qui est composé qui se base sur le le temps d'exécution donc lui-même est utilise un autre mét métrique donc c'est le rapport entre un temps et un temps de référence ici c'est le rapport entre le temps séquentiel et le temps parallèle mais si je veux écrire une une formule plus générale l'accélération ou le speedup c'est le temps sur le temps de référence donc.
总体来说。现在我来讲加速。加速是一个基于执行时间的度量指标，它本身使用了另一个度量指标。加速是某个时间与参考时间的比值，这里指的是串行时间与并行时间的比值。如果我要写一个更通用的公式，加速（或称为加速比）就是时间除以参考时间。

Peut être un temps après amélioration et. Amélioration modification parce que des fois on modifie on veut améliorer mais on améliore rien sur le temps avant. Cette amélioration ou cette modification et donc le temps avant c'est le temps de référence donc ici c. Este. C'est entre les de 1 c'est.
可能是在改进之后的一段时间。改进和修改是因为有时我们进行修改，想要改进，但实际上在之前的时间里并没有任何改进。这个改进或修改之前的时间就是参考时间，所以这里是……这是在1之间的时间。

Oui oui c'est ça tu viens écrire e. Améliélioration et. Est-ce que sinon c'est en plus que 1 euh. Après et là c'est avant et là. C'est la ref voilà. Donc ça c'est la. La formule générale le temps avant sur le temps après et donc on a la première loi d'Amda je dis bien la première parce que Amda elle a fait plusieurs lois sur ces accélérations en prenant en compte les la synchro pas la synchro les dépendances pas la dépendance mais là c'est la première loi qui tient pas compte des dépendances de la syn synchro et cetera donc vous avez comment on arrive à ça donc vous avez le temps de référence et le temps amélioré et.
是的，就是这样，你来写“改进”和“是的”。否则的话，就是超过1，嗯。然后这里是之前，这里是之后。这就是参考值。 所以这是通用公式，之前的时间除以后来的时间，因此我们得到了阿姆达定律的第一条，我特别强调是第一条，因为阿姆达提出了多条关于加速的定律，考虑了同步性，不是同步性，是依赖性，不是依赖性，这里是第一条定律，不考虑依赖性、同步性等等。 所以你们可以看到这是如何得出的，有参考时间和改进后的时间。

Les. Amélioré quand on améliore un système on peut pas l'améliorer de bout en bout à 100 % il y a toujours sou. Très souvent une partie qui n'est pas améliorée donc une fraction qui n'est pas améliorée donc si je pr prends le. De 0 à 1 il y a une fraction qui n'est pas améliorée une fraction améliorée et.
改进。当我们改进一个系统时，不可能做到从头到尾百分之百的改进，总会有一些部分没有被改进。通常会有一部分没有改进，也就是说存在未改进的部分和已改进的部分。举例来说，从0到1之间，总会有一部分没有被改进，一部分被改进。

Donc la fraction améliorée c'est fa et donc là non améliorée c'est forcément 1 - fa et. Donc ça c'est le nouveau et. Le nouveau temps ou le le temps amélioré ou donc. Je considère un système je lance l'exécution sur cette sur ce système de référence et j'ai un temps t. Ensuite j'améliore je change je modifie quelque chose sur ce système et je lance la même application et je vais voir que et.
所以改进后的比例是fa，因此未改进的比例必然是1 - fa。这就是新的时间，或者说改进后的时间。我考虑一个系统，在这个参考系统上启动执行，得到时间t。然后我进行改进，改变或修改系统中的某些内容，再运行同一个应用程序，就会看到新的时间。

Ça s'exécute en un autre temps temps amélioré et. Sauf que ce temps amélioré et. Ou cette amélioration a touché juste une partie et. Donc le temps amélioré est égal à quoi donc on a sur la partie non améliorée sur la fraction non améliorée ben je reste pareil qu'avant mais sur la partie améliéliorée je vais diminuer ce temps par un facteur d'amélioration a et.
它在另一个改进后的时间内执行。只是这个改进后的时间，或者说这种改进只影响了一部分。那么改进后的时间等于多少呢？对于未改进的部分，我保持原来的时间不变，而对于改进的部分，我将把时间缩短一个改进因子a。

Je je vais améliorer avec un facteur de 2 ou 3 ou 5 ou 2 et demi peu importe donc si je vais calculer l'accélération à ce niveauau-là donc ça sera le temps de référence t rapporté au temps améliélioré et ça sera t sur 1 - fa t + fa t/a si je prends t en facteur et. Là j'aurai 1 - fa + fa/a et.
我将提升2倍、3倍、5倍或2.5倍，具体无所谓。所以如果我要计算这个阶段的加速比，那就是参考时间除以改进后的时间，即 t 除以 1 - fa t + fa t/a。如果我把 t 提出来作为因子，就会得到 1 - fa + fa/a。

J''aiélimémine ça et j'aurai donc la première formule d'Amda et. Si vous avez t parce que l'amélioration c'est le passage de un processeur à un t processeur et. Moi aussi j'ai un téléphone je peux le prendre et et on va passer tous à un moment avec mon téléphone respectif et. Donc ici le passage de 1 à t processeur et.
我已经消除了这个问题，因此我将得到阿姆达的第一个公式。如果你有t，因为改进就是从一个处理器到t个处理器的转变。我也有一部手机，我可以拿着它，我们都会在某个时候用各自的手机。因此，这里是从1个处理器到t个处理器的转变。

Mais en général donc donc là c'est vraiment le cas idéal quand on suppose que lorsqu'on passe de un processeur à t processeur on va diviser le temps par t sur la partie qui qui est utilise qui exploite vraiment ces t processeurs donc on va améliorer avec un facteur t mais dans le cas général on améliore avec un facteur a et.
但一般来说，这才是真正的理想情况，当我们假设从一个处理器增加到t个处理器时，真正利用这t个处理器的部分运行时间会被除以t，因此我们会以t倍的速度提升性能，但在一般情况下，提升的因子是a。

Donc là c'est la première loi d'Amda qui me permet de calculer l'accélération le speedup lorsque j'améliore une fraction uniquement de l'exécution et. Je l'améliore avec un facteur t ouur. A et. Alors cette accélération ici on n'a pas tenu compte donc de euh de la synchronisation de du coût de cette amélioration mais c'est pas euh c'est pas réaliste parce que toute amélioration on améliore mais ça coûte quelque chose et ça surtout dans le cas de la par paralysation il y a toujours de la synchronisation il y a toujours une partie qui n'est pas paralysable qui est c'est ouvert un peu parce qu'il y a quelque chose et.
这是阿姆达尔定律，它让我能够计算当我只改进执行的一部分时所获得的加速比或加速倍数。我用一个因子t来改进这部分。然而，这里的加速比没有考虑同步成本和改进的开销，但这并不现实，因为任何改进都会带来一定的代价，尤其是在并行化的情况下，总会有同步开销，总有一部分是不可并行的，因为总会有一些必须顺序执行的操作。

Donc. Si on rajoute cette ce coût de l'amélioration ou de la paralysation dans ce cas-là et donc la plupart des cas on va rajouter un coût ici parce que le temps de l' l'amélioration c'est pas juste donc la partie qui reste constante qui s'exécute exactement par pareil qu'à la référence et la partie qui est améliorée mais cette partie améliorée elle est améliorée moyennant un coût qui est en fonction de la partie améliorée la section améliorée et le nombre de ressources donc qui ont été utilisées et.
所以，如果我们在这种情况下加上改进或停滞的成本，那么在大多数情况下，我们会在这里增加一个成本，因为改进的时间不仅仅是固定部分的时间，固定部分的执行时间与参考情况完全相同，而改进的部分则是经过改进的，但这部分改进是有成本的，这个成本取决于改进的部分、改进的区段以及所使用的资源数量。

Aprs. Ou des fois on peut calculer ce temps surtout lorsqu'il s'agit d'une paralysation avec donc on peut calculer les délais d'attente et cetera les délais de synchronisation et des fois on peut pas vraiment calculer de manière exacte on peut calculer de manière à approximative ce euh ce délai ou cette ce coût de paralysation et alors l'efficacité maintenant l'efficacité est également un métrique composé donc on utilise un autre métrique qui est l'accélération qu'on vient juste de calculer mais on rapporte l'accélération à l'investissement et.
有时我们可以计算这段时间，尤其是在涉及停顿的情况下，因此我们可以计算等待时间、同步时间等；但有时我们无法精确计算，只能大致估算这段时间或停顿的成本。至于效率，效率也是一个复合指标，因此我们使用另一个刚刚计算过的指标——加速比，但我们将加速比与投入进行比较。

Donc l'investissement ici il est est matériel on s'est dit j'ai utilisé t processeur pne ou peu importe le contexte euh p élément de de calcul au lieu de 1 je suis passé de 1 à p donc est-ce que la multiplication par p de de mes ressources rapporté donc en considérant l'accélération à obtenue est-ce que c'est efficace ou pas est-ce que ça vaut la peine ou pas ça dépend si j'ai fait p processeur je su suis passé de 1 à p et que j'ai accéléré je suis passé de d'un temps t à un temps t sur p donc le cas idéal effectivement j'ai une efficacité de 1 donc 1 c'est la la borne supérieure plus on s'approche de 1 plus l'accélération est maximale donc on exploite au maximum l'investissement sans délai sans coût et plus on en éloigne on on s'éloigne plus donc on on n'est pas efficace donc 1 - e c'est l'inefficacité et e qui est donc l'accélération sur p c'est la l'efficacité et alors ce ce coût ici investissement alors ici c'est simple on passe de 1 é élément de de calcul à p donc l'investissement c'est p en fait le coût ici il faut que je dise euh je vais le.
所以这里的投入是实质性的。我们假设我使用了p个处理器，或者无论什么计算元素，从1个增加到了p个。那么，将资源乘以p后，考虑到所获得的加速效果，这样做是否有效，是否值得，取决于具体情况。如果我用了p个处理器，计算时间从t减少到了t/p，那么理想情况下效率为1。效率为1是上限，越接近1，加速效果越好，说明我们最大限度地利用了投入，没有延迟和额外成本；反之，效率越低，说明效率不高。1减去效率e就是无效率，而e，即加速比除以p，就是效率。这里的成本或投入很简单，就是从1个计算元素增加到p个，所以投入就是p。至于成本，我还需要说明一下。

Accélération l'efficacité est égale x/c donc ça combien. Je vais je vais x fois plus vite et. Maisais là je. Coûte c fois l'investissement et multiplié par ou je coûte c fois plus voilà et. Pour voir pour calculer x je fais le temps de référence sur le temps amélioré mais pour calculer le le rapport de coût je vais faire le coût de l'amélioration sur le coût de référence donc là c'est un rapport aussi de coût et.
加速的效率等于 x 除以 c，那么是多少呢？我会快 x 倍。但是这里我花费了 c 倍的投资，乘以或者说我花费了 c 倍的成本。为了计算 x，我用参考时间除以改进后的时间；而为了计算成本比率，我用改进成本除以参考成本，所以这里也是一个成本比率。

J''insinsiste parce que là en fait comme je passe de 1 à p euh c'est c'est p/1 donc du coup on fait on voit pas qu'il y y a un rapport parce que c'est divisé par 1 mais là aussi c'est là on de manière générale c'est un rapport de coût et je vais calculer c'est fois plus je vais voir combien je coûte par rapport à la référence donc si je vous dis euh euh.
我之所以坚持，是因为实际上当我从1变成p的时候，嗯，这就是p除以1，所以我们看不到有比例关系，因为除以1。但这里也是，通常来说，这是一个成本比率，我会计算这是多少倍，我会看看相比参考值我花费了多少。所以如果我告诉你，嗯，嗯。

J'ai amélioré la machine je sais pas j'ai rajouté quelques éléments j'ai changé tel et telle carte et euh l'investissement m'a coûté euh euh 30 % euh de la machine initiale et. Donc le coût avant c'est x le coût après c'est 1 x 3x et donc en fait le rapport entre les deux c'est 1/3 1,3x/x donc je suis 1,3 plus coûteux que la référenceence et.
我改进了机器，我不知道，我添加了一些部件，换了这张和那张卡，投资花费了初始机器成本的大约30%。所以之前的成本是x，之后的成本是1.3x，实际上两者的比值是1.3x除以x，也就是1.3，所以我比参考成本高出30%。

Alors le taux d'utilisation pareil il est étroitement lié à à l'efficacité je veux voir quelle est la fraction du temps pendant laquelle la ressource est vraiment utilisée parce que je fais un investissement il améliore les choses sur une fraction euh avec un un facteur euh associé euhuh mais est-ce que je l'utilise beaucoup cet investissement est-ce que ces ressources que j'ai ajoutées euh sont vraiment euh utilisées euh beaucoup si je rajoute un coprocesseur flottant et que je le fais tourner que des applications qui n'utilisent pas le flottant euh c'est pas c'est pas vraiment je l'utilise pas vraiment le taux d'utilisation de ce ce composant est est très bas donc j'ai fait un investissement pour rien donc le taux d'utilisation d'occupation de la ressource sur le temps total et je vais voir quel est le le pourcentage il s'exprime en pourcentage donc euh quel est le pourcentage d'utilisation de cette ressource alors bien sûr le but si on on rajoute une ressource c'est de l'exploiter au maximum et l'exploiter de manière harmonieuse parce que ça joue sur l'usure et sur aussi les performances parce que si après cette ressource est usée et qu'elle tombe en panne forcément ça joue sur l'ensemble donc je regarde le taux mais je regarde aussi la la l'harmonisation de l'usage pour éventuellement s'il y a besoin faire d'une autre balle aussi et équilibrer la char chargege sur des composants pour ne pas euh faire tomber ma ressource un exemple typique de ça euh c'est les ssd vo vousus utilisez tous des ssd maintenant euh donc le ssd c'est quoi bon à l'intérieur il y a des plaines et des mais au niveau de du bloc on a on a les les les dies après il y a les plaines après il y a les blocs après il y a les pages si je prends un bloc donc et vous avez plusieurs pages et.
那么，利用率与效率密切相关。我想了解资源真正被使用的时间比例，因为我进行了投资，这项投资在某个时间段内以某个相关的系数提升了性能。但我是否充分利用了这项投资？我新增的这些资源是否真的被大量使用？比如，如果我增加了一个浮点协处理器，但运行的都是不使用浮点运算的应用程序，那么这个组件的利用率就会非常低，等于白白浪费了投资。所以，资源的利用率是指资源在总时间内被占用的百分比。显然，如果我们增加了一项资源，目标就是最大限度地利用它，并且要协调好使用，因为这关系到资源的磨损和性能。如果资源磨损后发生故障，必然会影响整体系统。因此，我不仅关注利用率，还关注使用的协调性，以便在必要时进行调整，平衡各个组件的负载，避免资源过载。一个典型的例子是SSD。现在大家都在使用SSD。那么SSD内部是什么呢？它包含了平面（plane），在块级别上有多个die，然后是平面，再到块，最后是页。如果我拿一个块来说，它包含多个页。

Donc ça c'est un bloc d'un ssd et là c'est une page et. Alors sur un ssd quand je veux lire je lis une page quand je veux écrire je peux écrire sur une page mais attention je ne peux pas modifier sur une page l'écriture sur le ssd c'est je regarde une page libre j'écris dedans et après j'invalide l'ancienne version c'est no in place update la politique et si je n'ai pas de plage de de page vide qu'est-ce que je fais je vais effacer le bloc il y a trois opérations il y a la lecture il y a l'écriture et il y a l'effacement vous voyez ça je ne peux pas effacer la page je peux effacer uniquement le bloc don donc de temps en temps quand j'arrive à à un à un seuil où il y a plusieurs pages qui sont pleines qui sont invalides qui sont je dois effacer le bloc le problème pour le rendre utilisable après donc avant d'effacer le bloc je prends toutes les pages qui sont valides je vais les écrire ailleurs sur un autre bloc et après je fais un effacement le problème c'est que le nombre d' d'effeffacement est finiini et.
这是一个SSD的一个块，这里是一个页。在SSD上，当我想读取时，我读取一页；当我想写入时，我可以写入一页，但注意，我不能修改一页上的内容。SSD上的写入方式是：我找到一个空闲页，写入数据，然后使旧版本失效，这是一种非原地更新策略。如果没有空闲页，我该怎么办？我会擦除整个块。操作有三种：读取、写入和擦除。你看，我不能擦除单独的一页，只能擦除整个块。因此，当达到某个阈值时，有许多页是满的、无效的，我必须擦除这个块。问题是，为了让块可用，在擦除块之前，我会把所有有效页的数据复制到另一个块上，然后再进行擦除。问题是擦除次数是有限的。

Quand j'achète un ssd ça dépend de la technologie qu'il y a j'ai un nombre fini j'ai 10^4 10^5 ça dépendend de nombre d'effacement au-delà de quel là. Vous ne pouvez plus utiliser c'est grillé donc si je le fais pas un bon équilibrage de mes lectures de mes écritures surtout sur tous les les blocs je vais griller celui-là et qui va griller tout le die donc du coup ça joue sur les les la la performance mais ça joue aussi sur l'usure et la durée de vie et dans les tous les ssd il y a dans le le le ftl donc le système qui de bas niveau qui gère ces ces ces placements il y a un un weird re reading et.
当我购买SSD时，这取决于它所采用的技术，我有一个有限的次数，大约是10^4到10^5次，这取决于擦写次数，超过这个次数后，SSD就不能再使用了，会损坏。因此，如果我没有对读写操作，尤其是对所有块的读写进行良好的均衡，就会导致某些块损坏，进而损坏整个芯片。这不仅影响性能，也影响磨损程度和使用寿命。所有SSD中都有FTL（闪存转换层），这是一个底层系统，负责管理数据的存放，还有所谓的“奇异重读”机制。

Donc le. Donc un un algorithme de il y a plusieurs algorithmes bien sûr mais bon il y a un algorithme qui est est implémenté et qui fait de du lissage d'usure pour que il n'y a pas un bloc qui qui est plus utilisé que d'autres et qui va être et qui va épuiser son nombre de d'effacement et qui va donc faire tomber tout le tout le plane et tout le die alors que les autres sont encore n'ont pas utilisé leur quota je dirais et.
所以，有多种算法，当然，但有一种算法被实现了，它进行磨损均衡处理，确保没有某个块被使用得比其他块更多，从而避免该块的擦写次数耗尽，导致整个平面和整个芯片损坏，而其他块的使用配额尚未用完。

Alors le le lien entre l'efficacité et le taux d'utilisation donc dans un environnement parallèle euh l'efficacité va refléter le taux d'utilisation des ressources qui euh qui fonctionnent en parallèle mais ça c'est juste dans un c'est on s'intéresse au au parallélisme et à la parallélisation euh de de l'utilisation parallèle de ressources qu'on a euh donc si on utilise si le taux d'utilisation des ressources est est assez élevé ça reflète l'efficacité ça reflète que on a on utilise bien les euh les les ressources qu'on a l'investissement qu'on a fait on accélère bien les choses donc plus donc l'efficacité tombe vers 1 plus euh le taux d'utilisation est meilleur et s'approche de 100 % lui aussi donc ça va ensemble et.
那么效率与利用率之间的关系是在并行环境中，效率反映了并行运行资源的利用率。但这仅仅是在关注并行性和资源并行使用的情况下。如果资源的利用率较高，就说明效率较高，表明我们很好地利用了已有的资源和投资，加快了处理速度。因此，效率越接近1，利用率也越接近100%，两者是相辅相成的。

Als. Euh le je fais une pause on va prendre une pause et on reprend par la suite et. Un peu un peu unu. Juste 10 minutes de pause s'il vous plaît est-ce que vo vous pouvez aérer un peu Ici,Quce qui c'estest éteint ou il y y a toujours de la lumière ? Ce qui va se réveiller, on doit— Non, non, c'est bon, j'ai pos posé le câble là.
嗯，我先休息一下，我们休息一下，之后再继续。就休息十分钟，请问你们能不能稍微通通风？这里……灯是关了吗，还是还有亮着？会不会吵醒别人？——不，不，没事，我已经把电线放好了。

Oui, c'est bon. C C'est bon ? Et où Is sraitël, je lis tout ça. En Et il veut adapter mon compte.te Il veut m'adapter le compte. Tu as l'abonnement ou tu— Oui, j'ai un petit l' abonnement. J'abonne à Pro, et de 0 €. J'abonne à— Vous payez la lig ligne ? Oi, oui. J'abonne à Pro., à for Et le deuxième jour, il meut perd le compte.
是的，没问题。是吗？他在哪里？我正在看这些。他想修改我的账户。他想帮我修改账户。你有订阅吗？——是的，我有一个小订阅。我订阅了专业版，价格是0欧元。我订阅了——你在线支付了吗？是的，我订阅了专业版。第二天，他把我的账户弄丢了。

Pourquoi on t'appelle ? Parce que je suzisinus,— Mais tu peux utiliser DeepS ? C'était comme comme çaa tout à l'heure ? Je je pens conseaisille que c''était plus— il y avait plus de contraste ? tu disais Mais DeepSeek c. Chinois.is, Mais Claude, c'est plus amusant. Oui, c'est vi. Non, ça ne vous gêne pas ?
你为什么被叫来？因为我有鼻窦炎——但是你可以用DeepS吗？刚才也是这样吗？我建议那样会更好——对比度更强？你说DeepSeek是中文的，但Claude更有趣。是的，确实如此。不，这不会打扰你吗？

Alors, on revient au cours. Donc, on a eu maintenant— Doncc on va regarder l'accélération de la relation entre— l'accélération et et le coût, à travers donc d'abord— bon, il y a des mots qu'on utilise de facto en en anglais, mais on on peut dire la mise à l'échelle, la forte mise à l'échelle, mais ils sont scaling. Donc, pour une taille constante, qu'est--ce que ça veut dire ? Ça veut dire que pour une taille constante d'un problème, donc on a un problème de taille N, on va augmenter le nombre de processeurs, et on va observer quelles sont les conséquences sur le temps d'exécution. Est-ce qu'il va diminuer donc de telle et telle facteur, et comment faire le lien avec cette augmentation de problème ? Donc, l'idée c'est de voir comment mettre à l'échelle, donc nos systèèmmes,e, si je rajoute donc des processeurs, qu quel est l'impact sur l'exécution de mon problème, et donc éventuellement voir si je peux augmenter mon problème pour résoudre des problèmes plus grands en moyennant un investissement physique. Donc, une extensibilité forte, ou un strong scaling, c'est implique qu'on va p fois plus vite sur avec un p processeurs qui sont au p élément de calcul, sur le même code qui est de taille N, et donc l'efficacité, elle va toujours être à 1, ou elle va tendre à 1, parce qu'il y a toujours un 1, un petit facteur de—
那么，我们回到课程内容。现在我们来看加速比与成本之间的关系，首先——虽然有些词我们习惯用英文，但也可以说“扩展”或“强扩展”，不过通常用“scaling”。那么，对于固定规模来说，这是什么意思呢？就是说对于一个规模为N的问题，我们增加处理器的数量，观察这对执行时间有什么影响。执行时间是否会按某个比例减少，以及如何将其与问题规模的增加联系起来。我们的想法是看看如何扩展系统，如果我增加处理器数量，对问题的执行有什么影响，并且是否可以通过增加硬件投入来解决更大规模的问题。所谓强扩展（strong scaling），意味着使用p个处理器时，计算速度提高p倍，处理的是同一个规模为N的代码，因此效率始终接近1，或者趋近于1，尽管总会有一些小的因素影响。

De synchronisisation.ation, Donc, on dit que j'ai une extensibilité forte, et on va rester dans— une extensibilité forte, si lorsque j'augaugmente ma machine les ressources sur ma machine, et et je— avec un facteur p, je vais également aller p fois plus vite mais avec le même— Le même code de taille N.
同步化问题。因此，我们说我具有强可扩展性，如果当我增加机器上的资源，并且以因子 p 增加时，我的运行速度也会提高 p 倍，但代码大小 N 保持不变，那么我们就称之为强可扩展性。

Ça veut dire que la— La synchronisation— le portage sur ces p processeurs, il ne coûte rien. Ça veut dire que mon programme, il peut être exécuté pratiquement avec le même effort sur p processeurs. Tout ça, c'est parce que si j'ai une extensibilité forte, ça veut dire que je peux aller vraiment beaucoup plus vite sur mon code en augmentant les ressources. Donc le problème, c'est juste matériel. Plus j'investis, je mets des ressources, plus je vais plus vite. Donc ça, c'est lorsqu'on se focalise sur la parallélisation, en termes de gain en temps. Mais la parallélisation, elle n'est pas toujours axée sur le temps.
这意味着在这些处理器上的同步和移植几乎不花费任何代价。这也就是说，我的程序几乎可以以相同的努力在多个处理器上执行。所有这些都是因为如果我有很强的可扩展性，这意味着通过增加资源，我的代码运行速度可以大大提升。所以问题仅仅是硬件方面的。投入越多，资源越多，速度就越快。这就是当我们专注于并行化时，在时间收益方面的表现。但并行化并不总是以时间为中心。

Je dis « n'est pas ». Principalement, c'est le temps. On reconnaît. Mais « n'est pas toujours axé sur le temps ». Donc on peut avoir donc une parallélisation pour affiner plus les modèles. Donc on va avoir des problèmes ou des codes avec pas de taille N, mais de taille x fois N, parce qu'on affinant le modèèle, mon problème, il change de taille. Donc quand je passe à p processeurs, je ne veuxux pas aller plus vite, je veux juste traiter des problèmes de grande taille pour avoir un modèle plus fin, par exemple, ou plus précis. Je change des instructions qui étaient en simple précision à une double précision, ainsi de suite. Donc là, vous avez également ce qu'on appelle l'extensibilité faible, ou le weak sc scalaling. Donc ça consiste à faire quoi ? Ça consiste à augmenter la taille du problème, en même temps que le nombre de processeurs.
我说“不一定”。主要是时间因素。我们可以识别出来。但“不总是以时间为中心”。所以我们可以进行并行化，以更精细地调整模型。因此，我们会遇到一些问题或代码，其规模不是N，而是x倍的N，因为通过细化模型，我的问题规模发生了变化。所以当我使用p个处理器时，我并不是想加快速度，我只是想处理更大规模的问题，以获得更精细或更精确的模型。例如，我将一些单精度指令改为双精度，等等。因此，这里你们还会遇到所谓的弱扩展性（weak scaling）。这意味着什么呢？就是随着处理器数量的增加，同时增加问题的规模。

Donc pour le strong scaling, on n'augmente pas la taille. Du problème. On n'augmente que le nombre de processeurs, et voir comment je vais aller plus vite. Par contre, pour le weak scaling, je vais augmenter les deux pour voir : est-ce que qu quandand j'augmente mon nombre de processeurs, je peux traiter des problèmes plus complexes ? ?
因此，对于强扩展，我们不增加问题的规模，只增加处理器的数量，看看速度能提升多少。相反，对于弱扩展，我们会同时增加两者，看看当处理器数量增加时，是否能够处理更复杂的问题。

Et je peux les traiter de de la même manière, plus ou moins, que quand je traite un problème plus simple sur— avec une machine plus simple. Donc ça, ça vous permet de voir : est-ce que je peux traiter— ça, c'est très important, parce que on commence toujours par des problèmes plus simples, par des matrices plus petites, et après, on va— vous allez voir ça dans le cours après, hein— on va traiter des problèmes plus complexes, plus— et on aura besoin de machines plus—
我可以用差不多相同的方式来处理它们，就像处理一个更简单的问题，使用一台更简单的机器一样。这一点非常重要，因为我们总是从更简单的问题开始，处理更小的矩阵，之后——你们在后面的课程中会看到——我们会处理更复杂的问题，需要更强大的机器。

Avec des capacités de calcul plus importantes, mais est-ce qu'on va rester dans le même— Facteur de temps ? Ça, c'est justement, c'est le weak scaling. Alors, on en introduisant la loi d'Amdel tout à l'heure, j'ai dit : la première loi d'Amdel, elle ne tient pas compte de la— de la—. Coût de parallélisation, mais la deuxième loi rajoute le coût. Donc vous avez un sur f1, un sur 1 moins f1, plus la fraction améliorée sur sur le facteur d'amélioration, plus le coût qui est lié à la fraction améliorée, et le facteur d'amélioration, ou l'investissement que je fais. Donc quand je suis— j'augmente le nombre de processeurs, donc sur a devient p, et donc le nombre de processeurs que j'ai mis— que j'ai investi, et la partie qui a été parallélisée, donc f1, c'est f parallèèle, et 1 moins f1, c'est la partie— la fraction séquentielle.
随着计算能力的提升，我们是否还能保持相同的时间因素？这正是弱扩展性的问题。刚才介绍阿姆达尔定律时，我提到第一条阿姆达尔定律没有考虑并行化的成本，但第二条定律则加入了这一成本。因此，公式中包含了1除以f1，加上1减f1，再加上改进部分的成本，改进部分对应的提升因子，或者说我所投入的资源。当我增加处理器数量时，a变为p，也就是我投入的处理器数量，而被并行化的部分f1就是并行部分，1减f1则是串行部分。

Quandand on on passe à— Alors, vous avez une autre loi qui est la loi de de Stefffanen-Bassis, qui, elle, s'intéresse donc à borner cette accélération. On sait que dans le cas réel, on ne peut pas avoir des applications avec des des tâches complètement indépendantes, où il n'y a pas de synchronisation d'attente pour se communiquer des résultats, etc. Donc on va borner cette accélération d'une— d'un d'un code de taille N, exécuté sur p processeurs, et cette accélération, elle est toujours bornée par p plus 1 moins p fois s. s
当我们继续时——那么，你还有另一个定律，即斯蒂芬-巴西斯定律，它关注于限制这种加速。我们知道，在实际情况下，不可能有完全独立的任务应用程序，没有同步等待来传递结果等。因此，我们将限制一个规模为N的代码在p个处理器上执行时的加速，这种加速总是被限制在 p + 1 - p × s 以内。

P, c'est le nombre de processeurs, s, c'est la partie séquentielle, ou la fraction séquentielle, et donc on est arrivé— enfin, ils sont arrivés, tous les deux, à établir donc cette borne supérieure, qui est toujours vraie, et pour dire : si j'ai un code de taille N, et j'ai p processeurs, quelle est la limite max de l'accélération que je peux avoir ? Sachant que sur mon code de taille N, j'ai une fraction séquentielle que je ne peux pas parallélisiser, qui est plus plus ou moins, ou je ne sais pas moi, 8 %, 10 % de mon code, et donc sur cette partie-là, j'ai beau avoir plein de processeurs, je peux utiliser que 20.
P 是处理器的数量，s 是串行部分，或者说是串行的比例。因此，他们两人都得出了这个上限，这个上限始终成立。换句话说，如果我有一个大小为 N 的代码，和 p 个处理器，那么我能获得的最大加速比是多少？已知在这个大小为 N 的代码中，有一部分串行代码无法并行化，大约占代码的 8% 到 10%。因此，对于这部分代码，即使我有很多处理器，也只能使用 20 个。

Donc— et là, j'obtiens donc cette limite, et je sais que je suis bornée pour cette part par, cette limite. Vous voyez que là, si. Votre s est grand, si vous avez une partie séquentielle du code qui est est assez conséquente, donc vous la multipliez par 1 moins p, donc ça donne quelque chose de négatif, et ça va diminuer fortement donc la borne, et là, c'est la partie idéale, on va dire..
所以——在这里，我得到了这个极限，并且我知道在这一部分上我被这个极限所限制。你们看到，如果你的 s 很大，如果你有一段相当长的代码序列，那么你将其乘以 1 减去 p，这会得到一个负值，从而大幅降低这个界限，这就是理想的部分，可以这么说。

Parce que si vous avez vraiment zéro instruction séquentielle, donc vous avez donc la partie idéale qui est p., Vous avez p fois plus vite.e. Donc on a quantifié un peu avec Gustave Steffen-Bassassis. Ilss ont ont quan quantifiié— pas quantifié, mais donné un seuil maximal, une limite, à cette fonction de—
因为如果你真的没有任何顺序指令，那么你就处于理想状态p，你的速度将是p倍。因此，我们与Gustave Steffen-Bassassis一起对其进行了量化。他们没有真正量化，而是给出了该函数的最大阈值和限制。

Ils ont ont fait fait une borne supérieure sur cette fonction. de Donc les deux derniers métriques, c'est la fiabilité et la— et la disponibilité. On va commencencer par la fiabilité, qui est un métrique plus fort que la disponibilité. Donc c'est tout simplement un métrique pour quantifier l'aptitude du système à effectuer des fonctions, et fournir des résultats non erronés.
他们对该函数做了一个上界。因此，最后两个指标是可靠性和可用性。我们将从可靠性开始，它是一个比可用性更强的指标。可靠性简单来说是用来量化系统执行功能并提供无误结果的能力的指标。

C'est surtout ça. Des données et des résultats qui qui sont corrects, et sur un domaine tenir, ce fonctionnement sur un temps T. Donc ça peut être donc le calcul. Je fais le calcul et les résultats doivent être non erronés. Sur un temps T, ça peut être aussi des données, donc mes donnénées sont gardées de manière cohérente sur un temps T, et je garantis qu'elles sont disponibles et non erronées. Ça peut être appliqué au réseau. Vous voyez, on s'intéresse à ces trois aspects dans le calcul haute performance. Malheureusement, pas de manière égale, mais ça, c'est l'historique du HPC, on s'est focalisé sur le compute, sur le calcul pendant des décennies, des décennies, mais là, on arrive à la— à la limite et les données constituent un frein dansc mon trava travail aussi aussi, beaucoup sur sur les les données actuellement. Le réseau également, parce qu'on est dans le parallélisme massif, et donc il faut un réseau qui supporte tout ça.a.
这主要是指数据和结果的正确性，以及在某个领域内在一段时间T内保持这种运行状态。比如计算，我进行计算，结果必须是无误的。在时间T内，也可以是数据，即我的数据在这段时间内被一致地保存，并且我保证它们是可用且无误的。这也可以应用于网络。你看，我们关注高性能计算中的这三个方面。不幸的是，这三者并未得到同等重视，但这就是高性能计算的历史，几十年来我们一直专注于计算。然而，现在我们已经接近极限，数据成为了我工作中的一个瓶颈，目前我也在大量处理数据。网络同样重要，因为我们处于大规模并行环境中，因此需要一个能够支持这一切的网络。

Mais donc les trois composantes. Donc vous avez la fiabilité du du calcul, les résultats sont non erronés pendant un temps T. Vous avez la fiabilité des données, donc les données sont là et fiables pendant un certain temps, quitte à faire une redondance intégrale, donc faire une copie intégrale des données sur un autre support, comme ça vous pouvez accéder à la copie native ou la copie miroir quand vous en avez besoin. Il y a la copie partielle. En fait, ce n'est pas on ne copie pas la redondance partielle, on ne copie pas une partie des données, mais on va calculer un code sur ces données, et on va sauvegarder ce code, qui nous permet donc de recalculer la donnée si besoin. Et de de temps en temps, on va vérifier si la donnée a été erronée ou pas. Donc vous av avez, par exemple,
那么，三个组成部分。首先是计算的可靠性，即在一段时间T内，计算结果不会出错。其次是数据的可靠性，数据在一定时间内是存在且可靠的，为此可以采用完全冗余，即将数据完整复制到另一个存储介质上，这样在需要时可以访问原始副本或镜像副本。还有部分复制，实际上不是部分冗余复制，也不是只复制部分数据，而是对这些数据计算一个校验码，并保存该校验码，这样在需要时可以重新计算数据。并且我们会不时检查数据是否出错。例如，您有……

vous avez par exemple des données— alle allez,z, on va va dire— on va les subdiviser un vecteur de données, de bits, on va les subdiviser en unités. On appelle ça le striping, donc. Je coupe en partie égale qui s'appelle les strip units, et après je vais les stocker.er sur les On va prendre des disques, quand je fais des disquesques comme ça, ça peut être des HDD, des SSD, peu importe le support support, de sauvegarde.
例如，您有一些数据——我们可以说——我们将把它们细分成一个数据向量，按位划分成若干单元。这称为条带化（striping）。我将数据等分成称为条带单元（strip units）的部分，然后将它们存储起来。我们会使用磁盘来存储这些数据，这些磁盘可以是机械硬盘（HDD）、固态硬盘（SSD），无论是哪种存储介质。

Donc au lieu de stocker juste les données natives, donc je vais stocker— donc j'ai D1, D2, je les— D3, D4, D5. Je vais faire donc un bloc sur chaque support, D1 ici, D2 là, D3 là, et le quatrième, je ne vais pas stocker une donnée, parce que si je stocke une donnée là, c'est un disque tombant en panne ou il y a une erreur sur— je ne peux pas— je ne peux pas régénérer cette don donnée correctement.
所以，我不仅仅存储原始数据，比如我有D1、D2、D3、D4、D5，我会在每个存储介质上做一个块，D1放这里，D2放那里，D3放那里，而第四个位置我不会存储数据，因为如果我在那里存储数据，当那个磁盘发生故障或者出现错误时，我无法正确地重建那部分数据。

Donc je vais stocker un code ici, qui va être en fonction de ces trois données, je prends un code, par exemple la parité, le XOR, D1 ou XORR D2 ou XOR D3, et je vais écrire le résultat ici. Si un disque tombant en panne, eh bien la parité ici, ce code, va— parce que c'est une fonction réversible, je peux reconstituer D2. D2 sera la parité ou.
所以我将在这里存储一个代码，这个代码将根据这三个数据来生成。比如我取一个代码，比如奇偶校验，D1的异或，或者D2的异或，或者D3的异或，然后我会把结果写在这里。如果有一个磁盘发生故障，那么这里的奇偶校验代码——因为这是一个可逆函数——我可以重建D2。D2将是奇偶校验或者。

Exclusive D1 ou exclusive D2. Je peux reconstruire. Donc ça, c'est des— il y a plusieurs configurations de placement, etc., ça fait partie de toute la série de stratégies RED, redundance array of independent disks, donc qui sont— c'est des disques indépendants qui ne sont pas synchronisés, mais il y a de la redondance dessus, de manière vectorielle, et on fait bouger la parité, et on peut peut la stocker sur un disque, sur plusieurs disques, il y a plusieurs— il y a plusieurs niveaux.
独占D1或独占D2。我可以重建。因此，这里有多种配置方式等，这属于RED系列策略的一部分，即独立磁盘冗余阵列（Redundancy Array of Independent Disks）。这些磁盘是独立的，不是同步的，但上面有冗余，以向量方式实现。我们会移动奇偶校验数据，可以将其存储在一块磁盘上，也可以存储在多块磁盘上，存在多个级别。

Donc tout ça pour maintenir une fiabilité des données. On peut les reconstruire, on peut— on garantit que les données sont fiables. Aussi, la fiabilité du réseau. Alors, la fiabilité du réseau, d'abord, elle se fait de manière logicielle, je dirais, j'envoie un acquittement à chaque fois. Si je reçois un acqu— je ne reçois pas d'acquittement, ça veut dire que mon réseau— je ne suis pas sûre que mon paquet est arrivé, donc je relance mon paquet, mais ça peut aussi congestionner le réseau si je n'ai pas d'acquittement. Déjà, je vais avoir sur le réseseau et les données et les acquittements, et si je ne reçois pas l'acquittement, même s'il est j juste sur le chemin et qu'il y a congestion, je relance mon paquet, donc j'accentue la congestion. Donc en général, on fait une redondance intégrale dans les syst systèmes critiques, et je vais doubler mon réau.
所以这一切都是为了保持数据的可靠性。我们可以重建数据，我们保证数据是可靠的。同时，网络的可靠性也很重要。网络的可靠性首先是通过软件实现的，比如我每次都会发送一个确认信号。如果我没有收到确认信号，就意味着我不确定我的数据包是否到达，因此我会重新发送数据包，但如果没有收到确认信号，也可能导致网络拥堵。因为网络上既有数据包也有确认信号，如果确认信号在传输途中发生拥堵，我仍然会重新发送数据包，从而加剧拥堵。所以通常在关键系统中，我们会进行完全冗余，并且会对网络进行双重备份。

Alors, comment se calcule cette fiabilité? Comment se calcule la fiabilité? De manière quantitative, parce que comme je disais tout à l'heure, les métriques doivent être exprimées de manière quantitative, elles s'expriment avec le temps moyen entre les les erreurs. Donc mean time bet between errors.s,
那么，这种可靠性是如何计算的呢？如何计算可靠性？以定量的方式，因为正如我刚才所说，指标必须以定量的方式表达，它们通过平均故障间隔时间来表示。也就是平均故障间隔时间（Mean Time Between Errors，MTBE）。

Donc quand j'ai une erreur, parce que de temps en temps je vais vérifier, est-ce que le calcul est vraiment correct ou pas. Si je trouve qu'il y a une erreur, donc je regarde la prochaine fois qu'il y a une erreur, si ce temps est réduit en dessosous d'un certain seuil, donc ça devient un seuil critique, et je vais faire une recontion.
所以当我发现一个错误时，因为我会不时地检查计算是否真的正确。如果我发现有错误，那么下次出现错误时，我会查看该时间是否降低到某个阈值以下，这就成为了一个临界阈值，然后我会采取相应的反应。

Des données ou re. Exécution, ou relancement de mon paquet, etc. Donc le temps moyen entre les erreurs doit être le plus réduit poss possible. On a un seuil, qui de fiabilité, qui nous permet de dire: là, je reconstruis, ou là,, ça va, les erreurs sont très éloignées les unes des autres, donc mon système reste fiable à une certaine mesure.
数据或重新执行，或重新启动我的包等。因此，错误之间的平均时间应尽可能缩短。我们有一个可靠性阈值，它让我们能够判断：在这里，我需要重建，或者这里没问题，错误之间的间隔很长，因此我的系统在一定程度上仍然是可靠的。

Quant à la disponibilité, pas la même même chose.e, C'est un métrique qui qui no nous permet juste de garder— Le système en fonctionnement, je dirais. Disponible. C'est la période où le système est en en fonctionnement, donc uptime, par opposition où le système est en panne, donc qui ne répond plus au downtime.
关于可用性，不是同一回事。这是一个指标，它不仅仅让我们保持系统运行，我会说是“可用”的状态。可用性是指系统处于运行状态的时间，也就是正常运行时间（uptime），相对的是系统故障时间（downtime），即系统不响应的时间。

Donc ici, on s'intéresse aux pannes, et non pas aux erreurs. Donc s'il y a des erreurs, quelque part, je m'en fiche. Mon système est en fon fonctionnement, il peut faire des erreurs, il peut ne pas en faire, donc— et là, on quantifie ce métrique avec le mean time to filler, ou le mean time between fillers.
所以这里我们关注的是故障，而不是错误。所以如果某处出现错误，我并不在意。我的系统正在运行，它可能会出错，也可能不会——在这里，我们用平均故障时间（Mean Time To Failure）或平均故障间隔时间（Mean Time Between Failures）来量化这个指标。

Soit le temps pour la première panne, donc qui donne l'alerte, ou bien le temps entre les pannes.es. Qui doit être le le plus grand possible. Alors, quelles sont les caractactéristiques d'un bon métrique? Les caractéristiques d'un bon métrique, d'abord la linéarité. Il faut pouvoir exprimer, donc de manière linéaire, les performances. Si un métrique vous donne cette possibilité-là, il faut le garder pour dire.
第一故障时间，即发出警报的时间，或者故障之间的时间间隔，应尽可能长。那么，一个好的度量标准应具备哪些特征呢？首先是线性。必须能够以线性的方式表达性能。如果一个度量标准具备这一点，就应该保留它。

Je changeange de contexte et le temps, par exemple, il est X fois plus grand, donc je décélère, ou il est 1 sur X fois plus grand, donc je suis en train d'accélérer. Donc si linéairement, déjà, il faut qu'il soit proportionnel, mais s'il est linéairement proportionnel, c'est encore mieux. Le problème, c'est que souvent, on n'a pas de linéarité. Donc des fois, on a des métriques qui sont logarithmiques, donc là, ça permet de faire une extrapolation assez facile, mais souvent, donc les métriques ne sont pas linéaires, et il faut à ce moment-là bien les caractériser avec des tests pour voir l'évolution de ces métriques avec le nombre de ressources, avec la taille des problèmes, et pouvoir donc faire une extrapolation selon le comportement du métriquerique qui qui peut peut être logarithmique, exponentiel, et dans le meilleur des cas, linéaire.
我改变了上下文和时间，例如，时间是原来的X倍大，所以我减速；或者时间是原来的1/X倍，所以我在加速。因此，如果是线性的，首先它必须是成比例的，但如果是线性成比例的，那就更好了。问题是，通常情况下，并不是线性的。有时我们遇到的是对数型的度量，这样的话，进行外推相对容易，但通常度量并非线性，这时就必须通过测试来准确描述它们，观察这些度量随资源数量、问题规模的变化，从而根据度量的表现进行外推，这种表现可能是对数型、指数型，最好是线性。

Alors, il faut aussi que le métrique soit fiable. Fiable, c'est-à-dire que lorsque le métrique vous dit qu'un système A est plus performant que le système B, il faut vraiment que le système A soit plus performant que le système B dans la réalité, parce que— donc vous dites: ça, c'est évident. Non, ce n'est pas toujours évident. Par exemple, le MIPS, n'est pas un métrique fiable, parce que des fois, il nous donne une mauvaise interprétation, il nous dit, par exemple, que un système est est plus performant qu'un autre. Parce qu'il calcule le million d'instructions par secononde. Il calcule juste la vitesse de travail. Mais un système qui calccule beaucoup, en une fraction de temps, une unité de temps déterminée, ça ne veut pas dire qu'il est plus performant, qu'il va plus vite. Je peux tourner en rond autour du rond-point pendant 5 heures, avant d'aller à ma destination, j'ai fait beaucoup de kilométrage, hein, maisais il y en a qui, bon, directement.
那么，指标也必须是可靠的。所谓可靠，是指当指标告诉你系统A比系统B性能更好时，系统A在现实中确实比系统B更好。因为——你可能会说：这很明显。其实并不总是如此。例如，MIPS不是一个可靠的指标，因为有时它会给出错误的解读，比如说某个系统比另一个系统性能更好。因为它计算的是每秒百万条指令数，只计算工作速度。但一个系统在一定时间内执行了很多指令，并不意味着它性能更好，运行更快。我可以在环岛上绕圈五个小时才到目的地，虽然我跑了很多公里，但有些人则直接到达。

Donc f. Beaucoup de travail avant d'arriver au résultat, ce ce n'est pas forcément— donc plus performant, ça dépend donc de— si on prend le temps d'exécution, ça nous donne plus un résultat fiable, quand un système a un temps d'exécution où mon application met un temps— s'exécute plus vite sur une machine, c'est plus fiable. Dire: elle va plus vite, c'est plus performant, alors je peux prendre le même calcul, faire un programme qui n'est pas— qui n'est pas très optimal, qui est mal écrit, qui fait le même calcul, il va mettre plus de temps, donc ce n'est pas performant. Par contre, son MIPS est beaucoup plus élevé, parce qu'on fait beaucoup de calculs.s.
所以，需要做大量工作才能达到结果，这并不一定——所以更高效，这取决于——如果我们考虑执行时间，它会给我们一个更可靠的结果，当一个系统的执行时间较短时，比如我的应用程序在一台机器上运行更快，那它更可靠。说它运行更快、更高效，我可以用同样的计算，写一个不太优化、写得不好的程序，做同样的计算，它会花更多时间，所以它不高效。然而，它的MIPS值会高得多，因为它进行了大量计算。

Donc là, c'est une illustration du MIPS, on la verra dans le TD cet après-midi, et qui a été faite sur le benchmark Watson, donc on a pris ce benchmark, qui est un benchmark de référence, on l'a exécuté sur une une machine, qui peut utiliser ou pas un cop-processeur flottant, qui ne fait que des calculs flottants, donc quand on utilise ce processeur, on a un certain temps d'exécution, qui est de 1 seconde, et on calcule le nombre de millions d'instructions en 1 seconde, c'est 1,6, et lorsqu'on ne veut pas utiliser ce coprocesseur, donc on ne fait pas appel à ce processeur, mais pour faire le calcul flottant, on utilise des librairiiries. Logiciels qui vont simuler les opérations flottantes en utilisant des opérations entières, mais ils utilisent beaucoup d'opérations entières pour simuler ce comportement flottant, et donc on exécute le calcul en 13 secondes et demie, pratiquement, et on fait beaucoup de calculs, parce qu'on fait beaucoup d'instructions entières pour faire un calcul flottant.
这里是MIPS的一个示例，我们将在今天下午的练习课中看到它。这个示例基于Watson基准测试，我们使用了这个作为参考基准，在一台可以选择是否使用浮点协处理器的机器上运行。这个协处理器专门用于浮点计算。当使用该协处理器时，执行时间为1秒，我们计算出每秒执行的百万条指令数为1.6。而当不使用该协处理器时，浮点计算则通过软件库来模拟，这些库使用整数操作来模拟浮点运算，但需要大量的整数操作来实现浮点行为。因此，计算执行时间约为13.5秒，执行了大量的整数指令来完成浮点计算。

Et donc on a un MIPS de 2,7. don Donc si je m'intéresse juste au au MIPS comme métrique, je je me dis: ah, là, j'ai 2,7 MIPS, donc cette machine, en utilisant la librairie de conversion de flottant en entier, est vraiment très performante par rapport à l'utilisation de coprocesseurs, donc je ne vais pas l'acheter.
所以我们的MIPS是2.7。如果我只关注MIPS这个指标，我会想：啊，这里是2.7 MIPS，所以这台机器在使用浮点转整数的库时，性能非常出色，相比使用协处理器来说，因此我不会买它。

Je ne vais pas le— Alors, que l'utilisateur, lu lui,i, il se dit: tiens, quand j'utilise le processeur flottant, la machine avec mon contrôle, j'ai le résultat en 1 seconde, alorsors que là, je dois attendre 13 secondes 5., Donc là, c'est le MIPS, ce n'est pas fiable. Alors, il faut qu'il soit déterministe. Ça, c'est même très, très, très important, parce que si vous faites l'expérience dans les mêmes conditions, plusieurs fois, et vous obtenez à chaque fois des valeurs complètement différentes, donc vous ne pouvez pas compter sur ce métrique pour mettre en valeur votre système.
我不会那么做——那么，用户，他会想：当我使用浮点处理器时，机器在我的控制下，结果只需1秒，而这里却要等13.5秒。所以，这个MIPS值是不可靠的。它必须是确定性的。这一点非常非常重要，因为如果你在相同条件下多次实验，每次得到的数值都完全不同，那么你就无法依赖这个指标来评价你的系统。

Parce qu'il donne des valeurs un pe peu aléatoires, je dirais, et vous ne pouvez pas quantifier la performance. Donc il faut que le métrique vous donne la même valeur plus ou moins avec des écarts à une petite marge d'erreur, donc il vous donne plus ou moins la même valeur à chaque fois que le même test dans les mêmes conditions est effectué.
因为它给出的数值有点随机，我想说，你无法量化性能。所以指标必须在一个小的误差范围内给出大致相同的数值，也就是说，每次在相同条件下进行相同测试时，它应该给出大致相同的数值。

Ça, c'est très important, le déterminisme, alors, la facilité de mesure, il faut que le métrique soit facile à mesurer, parce que quand c'est difficileile, déjà,, on peut faire beaucoup d'erreurs, et quand on fait des erreurs, déjà, on ne peut pas compter, on ne peut pas— notre métrique n'est pas fiable.
这非常重要，决定论，以及测量的便利性，指标必须易于测量，因为当测量困难时，我们很容易犯很多错误，而一旦犯错，我们就无法依赖这个指标，它就不可靠。

Et on a du mal à l'utiliser. Ça ne parle pas pas aux aux gens. Donc il faut que le métrique soit facile à mesurer, et. Facile à— donc on aura moins d'erreurs, et on pourra plus le le mettre en valeur. Alors, en ce qui concerne les méthodes d'évaluation, comme je disais tout à l'heure, il y en a 3. On va aller voir, donc ce seront les sujets des cours après.è
我们很难使用它。它无法引起人们的共鸣。因此，指标必须易于衡量，简单明了，这样我们就能减少错误，更好地发挥其价值。至于评估方法，正如我刚才所说，有三种。我们将在后续课程中详细讲解。

Donc la modélisation analytique, ou mathématique, la la simulation, et la mesure. Donc plus on met au tout début de la conception la réalisation du système, plus on est dans ces zones-là, l'analyse mathématique, la modélisation mathématique, et la simulation, parce qu'on a— déjà, on n'a pas la machine pour faire les mesures, et on n'a pas beaucoup d'éléments surtout au tout début, on n'a pas encore choisi les éléments logiciels et matériels, donc on ne peut pas vraimentiment simuler leur fonctionnement.
因此，分析建模或数学建模、仿真和测量是关键。设计越早阶段，越多地涉及这些领域：数学分析、数学建模和仿真。因为在一开始，我们没有机器进行测量，也没有太多的组件，尤其是在最初阶段，还没有选择软件和硬件元件，所以我们无法真正模拟它们的运行。

Donc ça dépend de— Et ça dépend aussi après de l'investissement qu'on peut faire, parce que la simulation— la méthode analytique, elle elle est est très peu coûteuse, elle est approximative, certes, mais elle est très peu coûteuse, en temps et en investissement humain.in. La simulation, ben, il faut des gens qui vont— qui développent des simulateurs, qui les testent, qui les déploient, qui— d'accord, sur une machine, et ça prend du temps, ça prend de la main-d'œuvre, donc c'est un coût financier et temporel.
所以这取决于——而且还取决于我们能够投入的资源，因为模拟——解析方法成本非常低，虽然是近似的，但在时间和人力投入上都很少。模拟则不同，需要有人开发模拟器，测试它们，部署它们，运行在机器上，这都需要时间和人力，因此会产生财务和时间上的成本。

La mesure, c'est encore plus plus lourd, en investissement, parce qu'il fautut disposer de la machine, il faut faire les résultats avec précision, il faut les analyser, il faut— donc il y a une main-d'œuvre, et du matériel. Donc ça dépend de l'investvestissement qu'on peut se se permettre, et de la phase dans laquelle on est dans notre système.
测量在投资上更加昂贵，因为需要拥有设备，必须精确地获得结果，进行分析，因此需要人力和设备。这取决于我们能够承受的投资以及我们系统所处的阶段。

Mais en général, même toujours, on nutilise pas qu'une seule méthode d'évaluation, on confronte toujours 2 méthodes. Donc quand on est au début du système, on fait l'analyse— La modélisation mathématique versus simulation, et. Quand on est plutôt vers le système est construit, où on a la machine, etc., on fait la simulation versus la mesure versus simulation. Alors, pourquoi au début on fait les 2? Parce que la modélisation vous donne une idée globale, approximative, rapide. Si vous voulez juste avoir une échelle de valeur, et des grandeurs comme ça, à.
但一般来说，甚至几乎总是不会只使用一种评估方法，我们总是会对比两种方法。因此，当系统处于初期阶段时，我们进行分析——数学建模与仿真对比。而当系统已经构建完成，有了机器等设备时，我们则进行仿真与测量的对比。那么，为什么在初期要做这两者呢？因为建模可以给你一个整体的、近似的、快速的概念。如果你只是想获得一个数值范围和类似的量级，建模就足够了。

Donner à votre chef, à votre équipe, pour dire: c'est faisable, c'est intéressant, on peut commencer le travail ou pas, donc vous avez besoinin de faire ça rapidement, et vous n'avez pas beaucoup de détails— donc ça, c'est idéal. Après, vousus avez besoin de faire de la simulation pour avoir des données plus plus précises, et vous calibrez. Une fois que vous avez— avec la simulation, on peut implémenter les détails, du du logiciel,, les détails du matériel, et une fois qu'on a un un sim simulateur détaillé, on peut calibrer le modèle et le— sur les mêmes tests, sur les mêmes cas, avec le simulateur, et par la suite on peut utiliser— revenir au modèle pour avoir des rés résultats très vite, très rapidement.
向您的领导和团队说明：这是可行的，很有趣，我们可以开始工作了，还是不行，所以您需要快速完成这项工作，而且细节不多——这就是理想的情况。之后，您需要进行仿真以获得更精确的数据，并进行校准。一旦有了仿真，我们就可以实现软件的细节、硬件的细节，一旦有了详细的模拟器，我们可以校准模型，并在相同的测试和案例中使用模拟器，随后我们可以回到模型，快速获得结果。

Alors, aussi, on peut utiliser la simulation de manière conjointe avec la mesure, donc on fait la mesure quand on dispose des systèmes, sur des applications réelles, au débenchmark.ks, On On a des résultats réels, parce que ça, ça parle beaucoup aux industriels, ensuite on va calibrer le simulateur avec la réalité, sur les mêmes cas, parce que souvent on a des décalages, donc on va calibrer le simulateur, pour qu'il nousus donne— qu'il représente vraiment le.
因此，我们也可以将仿真与测量结合使用。也就是说，当我们拥有系统并在实际应用中进行测量时，比如在基准测试中，我们会得到真实的结果，因为这些结果对工业界非常有意义。接着，我们会根据相同的案例，用实际数据来校准仿真器，因为通常会存在偏差，所以我们需要校准仿真器，使其能够真实地反映实际情况。

Fonctionnement réel de la machine et des applications, et ensuite on peut utiliser le simulateur pour simuler du matériel qui n'est pas encore sorti sur le marché, d'anticiper des comportementss, des applications futures, et là, c'est vraiment très intéressant, donc souvent on utilise 2 méthodes.
机器和应用程序的实际运行情况，然后我们可以使用模拟器来模拟尚未上市的硬件，预测行为和未来的应用，这非常有趣，因此我们通常使用两种方法。

Et on calibre l'une par rapport à l'autre. Donc si maintenant je vais— donc là, c'est—— on a vu les balles de perf, les concepts de base, hein, dans un cas général, même si j'ai tendance à rapporter ça au HPC, c'est normal, maintenant on va voir des choses plus spécifiques au HPC. Alors, le HPC, bon, on s'intéresse beaucoup à la parallélisation, et la platef performane,ce, c'est vraiment le cœur du métier, on a quelques métriques— des métriques classiques que j'ai citées, qu'onon utilise beaucoup plus, et.
然后我们将它们相互校准。所以如果现在我——这里，我们已经看过了性能指标和基本概念，嗯，在一般情况下，虽然我倾向于将其与高性能计算（HPC）联系起来，这是正常的，现在我们将看到更多与HPC相关的具体内容。关于HPC，我们非常关注并行化，性能平台确实是核心业务，我们有一些指标——我之前提到过的经典指标，我们会更多地使用它们。

Donc ça, c'est le plus important. Alors, qu'est-ce qui fait que le HPC so soit un peu particulier, et donc on utilise des choses un peu particulières, on év làade de perf? C'est le fait qu'on soit très proche du matériel, donc il faut connaître vraiment sa machine, pour bien l'utiliser. Pour bien exploiter toutes ses caractéristiques—
所以这是最重要的。那么，是什么让高性能计算（HPC）有些特别，因此我们使用一些特别的方法来避免性能损失呢？那就是我们非常接近硬件，因此必须真正了解自己的机器，才能很好地利用它，充分发挥其所有特性——

De performance qu'elle nous offre, et pouvoir améliorer l'application, modifier l'application pour qu'elle exploite au maximum les ressources. Donc ce n'est pas la bonne d'avoir une Ferrari si un vélo suffit. Donc on est très proche du matériel, on on se base sur les progrès technologiques, on mesure, on caractérise le matériel, et après on utilise ça pour renseigner et optimiser les codes pour les porter correctement sur les machines.
她为我们提供的性能，以及能够改进应用程序，修改应用程序以最大限度地利用资源。因此，如果一辆自行车就足够了，拥有一辆法拉利并不是明智的选择。我们非常接近硬件，基于技术进步，我们进行测量，描述硬件，然后利用这些信息来指导和优化代码，使其能够正确地运行在机器上。

En connaissant les caractéristiques vraiment d'un niveau des machines, on peut proposer des évolutions au niveau des applications, donc pour pousser un peu les limit limites. Et comme on est très proche du matériel, aussi les balles de perf, elles est sont très, très bas niveau, donc on fait des tests bas niveau, des tests unitaires, très simples, on extrait des des— applications— l'essentiel, donc tous ces benchmarks représentatifs, on stresse le matériel, on le plaçonne dans des conditions limites, donc pour voir justement à quelle limite je peux allerler, et comment repousser encore plus ces limites, alors souvent, très souvent, on va jusqu'à l'assembleur, donc ceux qui sont dans l'option informatique, vousus allez après dans la la partie des parcours, donc voir l'optimisation jusqu'à bas niveau, et.
通过真正了解机器层面的特性，我们可以在应用层面提出改进，从而推动极限的突破。由于我们非常接近硬件，性能瓶颈也处于非常底层，因此我们进行底层测试和非常简单的单元测试，提取应用的核心内容。所有这些具有代表性的基准测试都会对硬件施加压力，将其置于极限条件下，以观察我们到底能达到什么极限，以及如何进一步突破这些极限。通常情况下，我们甚至会深入到汇编层面，对于计算机专业的同学来说，在学习路径中会涉及到底层优化的内容。

Utiliser donc— faire des études en profondeur en utilisilisant des benchmarks clients, parce qu'il y a des benchmarks classiques qui vous disent: voilà, ça c'est un benchmark pour la physique, la chimie, la bio, etc., mais souvent les clients ont leurs applications, et donc on leur— applications, et on leur benchmark,k, par parce qu'ils ne vont pas vous donner l'application comme ça, donc on fait des études en profondeur avec plusieurs niveaux, le benchmark générique, je dirais, le benchmark client, l'application client, et est ainsi de suite.
因此，要进行深入研究，使用客户基准测试，因为有一些经典的基准测试告诉你：这是物理、化学、生物等领域的基准测试，但客户通常有他们自己的应用程序，所以我们会为他们的应用程序进行基准测试，因为他们不会直接提供应用程序给你。因此，我们会进行多层次的深入研究，包括通用基准测试、客户基准测试、客户应用程序测试，等等。

Et je reviendrai sur ça quand je parlerai du master en général, et du hackathon. Alors, les performances en HPC, quels sont les éléments clés dont j'ai parlé auparavant, et d'autres? Qu'on va Qu'on va utiliser. On va utiliser souvent dès le départ la loi d'Abdal, c'est vraiment le. Trait en matière, je diis.
我将在谈论硕士课程总体情况和黑客马拉松时再回到这个话题。那么，高性能计算的性能，之前我提到过的关键要素有哪些，还有其他的吗？我们会使用哪些？我们通常从一开始就会使用阿姆达尔定律，这确实是该领域的一个重要原则，我是这么说的。

La loi d'Abdal, la loi de Buffett, qui donne la borne sup sur la parallélisation des codes, l'accélération qu'on peut en tirer, on parle beaucoup de la la mise à l'échelle, c'est très important parce qu'on est dans le massivement parallèle, je prends donc le. Strong scaling et le le weak scaling, donc je vais regarder— je prends un code de taille N, je ne vais pas le changer, mais je vais multiplier le nombre d'éléments de calcul par P, et je vais voir comment il se comporte, donc je vais voir quelle est cette— le scaling que je peux tirer, de ce code, est-ce que je peux paralléliser encore et encore et encore ce code.
阿姆达尔定律，也称巴菲特定律，给出了代码并行化的上限，以及我们能从中获得的加速。我们经常谈论扩展性，这非常重要，因为我们处在大规模并行的环境中。因此，我采用强扩展（strong scaling）和弱扩展（weak scaling）的方法。具体来说，我选取一个大小为N的代码，不改变它，但将计算元素的数量乘以P，然后观察其表现。也就是说，我想看看这个代码的扩展性如何，是否能够不断地进行并行化。

Je peux également aussi après— j'augmente le nombre de codes, j'augmente donc la complexité de mon code, parce que N1, N2, et de P1 à P2, et je regarde comment la miseise à l'échelle se fa fait. Donc ça, c'est vraiment les. Techniques de mise à l'échelle pour la parallélisation qui sont très, très utilisées, et le choix des charges de travail, doncc le ch choix des benchmarks, le choix des traces réelles ou des traces génériques, synthétiques qu'on génère nous-mêmes, comment faire en sorte qu' qu'elles soient vraiment représentatives, quelles sont les métriques qu'on utilisilise. On utilise beaucoup, beaucoup le MIPS, pardon, le Megaclops, pas beaucoup le MIPS, parce que comme vous avez vu, il n'est pas très fiable, et on calcule scientifique et calcul de performance, on travaille beaucoup avec les flottants, donc on utilise le Megaclops, on utilise beaucoup le taux d'utilisation des ressources, on utilise beaucoup le taux de mise et de hit parce qu'on travaille sur la hiérarchie mémoire qui est très importante.
我还可以在之后增加代码数量，从而增加代码的复杂度，因为有N1、N2，以及从P1到P2，然后观察扩展是如何进行的。这些确实是非常常用的并行扩展技术，以及工作负载的选择，也就是基准测试的选择，真实轨迹或我们自己生成的通用合成轨迹，如何确保它们真正具有代表性，使用哪些指标。我们大量使用MIPS，抱歉，是Megaclops，而不是MIPS，因为正如你们所见，MIPS并不太可靠。在科学计算和性能计算中，我们大量使用浮点运算，因此使用Megaclops；我们还大量使用资源利用率，缓存命中率，因为我们研究的是非常重要的存储层次结构。

Donc mémoire cache, mémoire centrale, et donc parce que ça— aller plus vite, oui, c'est très bien, mais si on a l'accèsès aux aux données qui plombent le reste, donc il il faut qu'on augmente le taux de hit le plus possible, c'est-à-dire exploiter la localité spatiale et temporelle au niveau de mes c codes pour aller chercher les données de manière anticipée, les mettre dans dans des caches pour pouvoir les accéder rapidement, sans pénénalité.
因此，有缓存内存和主存内存，因为这样可以加快速度，是的，这很好，但如果访问数据的速度拖慢了整体速度，那么我们就必须尽可能提高命中率，也就是说，要利用代码中的时空局部性，提前获取数据，将其放入缓存中，以便能够快速访问，避免惩罚性的延迟。

Alors, on va utiliser donc tout ce qui est X Expanse, Compute Expanse,, Memory Expanse, I/O Expanse, donc si j'ai un code et je l'exécute sur sur une machine, qu quelel est est le facteur limitant? Est-ce que c'est le calcul? Est-ce que c'est l'accès aux données? Est-ce que c'est— donc je regarde, est-ce que c'est la mémoire, je regarde quel est le maillon faible, si vous voulez, et donc travailler sur ce maillon faible.
那么，我们将使用所有的扩展资源，包括计算扩展、内存扩展、输入输出扩展等。所以如果我有一段代码并且在一台机器上运行，限制因素是什么？是计算能力吗？是数据访问吗？还是……所以我会查看，是不是内存，找出瓶颈所在，然后针对这个瓶颈进行优化。

Donc ça, ça rejoint le taux de mise, quelle est la latence mémoire. Alors, la latence mémoire, ce n'est pas forcément lié au taux de mis mise, c'est— des fois c'est la latence physique, la latence, le temps d'accès à la mémoire, donc est-ce que j'ai besoin de faire une hiérarchie ou cette latence me me suffit, si la latence est trop grande grande, il faut que je fasse une hiérarchie, je rajoute des mémoires plus courtes, je dirais, plus moins de capacité mais plus— avec des latences moindres, donc des temps d'accès très vite, très rapides, le débit également, et les ordres de grandeur vont en général, on parle beaucoup en ordres de grandeur en HPC, je suis à la nanoseconde au niveau de la nanoseconde ou de la microseconde, ou voilà, on se place selon— si je suis dans les entrées-sorties massives, je suis au niveau de la milliseconde, si je suis au niveau de la mémoire ou du calcul, je suis à la nanoseconde, donc cs.
所以这就涉及到命中率和内存延迟的问题。内存延迟不一定与命中率直接相关，有时是物理延迟，也就是访问内存所需的时间。那么我是否需要建立一个层次结构，或者这种延迟是否足够？如果延迟太大，就需要建立层次结构，增加容量较小但延迟更低的存储器，也就是说访问时间更快、更短。还有带宽的问题，通常在高性能计算中我们会以数量级来讨论延迟。比如说，延迟可能在纳秒级或微秒级。如果是大规模输入输出操作，延迟可能在毫秒级；如果是内存访问或计算，延迟则在纳秒级。

Les ordres de grandeur. Donc si on regarde donc tout ce que je viens de lister là, je reprends— Alors, HPC, calcul de performance, donc faire du parallélisme, ou même le massivement, très massivement parallèle, mais est-ce qu'on peut tout paralléliser? Non, malheureusement non, parce que sur une application, vo vous avez toujours une partie séquentielle qui va être.
数量级。因此，如果我们看我刚才列举的所有内容，我再说一遍——高性能计算（HPC），性能计算，也就是进行并行处理，甚至是大规模并行处理，但我们能把所有任务都并行化吗？不，遗憾的是不能，因为在一个应用程序中，总会有一部分是必须顺序执行的。

Exactement comme si vous aviez un monoprocesseur à l'ancienne, qui va exécuter les instructions de manière séquentquentielle l'une après l' l'autre, parce qu'il y a une dépendance entre toutes ces instructions, vous ne pouvez pas les exécuter en parallèle. Et après, il y a une partie de plus en plus qui est composée de tâches parallèles, et qu'on peut donc paralléliser, donc si on a une ressource, et on a une tâche parallèle, on va diviser leur temps d'exécution par N, tout simplementement, si elles sont complètement indépendantes. Donc on revient à faire quoi? On revient à la loi d'Andal basique, donc qui est bridée par la partie séquentielle, et la partie non séquentielle, donc parallèle, elle peut être divisée par N si on a une ressource, et si cette partie-là est composée de N tâches, complètement indépendantes. Alors, si—
就像你拥有一个传统的单处理器一样，它会顺序执行指令，因为这些指令之间存在依赖关系，无法并行执行。然后，有一部分越来越多的是由并行任务组成的，因此可以并行处理。如果我们有一个资源，并且有并行任务，那么只要这些任务完全独立，我们就可以将它们的执行时间简单地除以N。那我们回到做什么呢？我们回到了基本的阿姆达尔定律，它受限于串行部分，而非串行的并行部分，如果由N个完全独立的任务组成，并且有相应的资源，就可以将这部分时间除以N。那么，如果——

Comme dans dans les machines actuelles, je n'ai pas de problème de ressource pour en avoir autant que je veux, mon N, le nombre de ressources, il est égal au au temps vers l'infini, massivement parallèle. Donc ce R, un temps vers l'infini, un moins 1 sur un temps vers 0éro, doncc mon— R. Mon accélération, mon speed upup, il est égal à 1 sur s pour un temps vers l'infini. Donc quand je n'ai pas de problème de ressources, je suisis donc massivement parallèle, je je suis limitée par la partie sé séquentielle, c'est inversement, proportionnel à la partie séquentielle de mon code. Donc ça,a, ça vous permet de voir que avoir des ressources physiques une grande machine parallèle, c'est bien, mais il faut aussi travailler sur son codode et l'optimiser, parce que si votre code a une partie séquentielle très importante, ça ne sert à rien d'avoir la machine parallle.le.
就像在当前的机器中一样，我没有资源问题，可以拥有任意多的资源。我的N，即资源数量，随着时间趋向于无限，是大规模并行的。因此，这个R是一个时间趋向于无限的值，减去一个时间趋向于零的值，所以我的——R。我的加速比，我的速度提升，等于1除以s，当时间趋向于无限时。因此，当我没有资源问题时，我就是大规模并行的，我受限于代码中串行部分的比例，这与串行部分成反比。所以，这让你明白，拥有物理资源丰富的大型并行机器固然好，但也必须优化代码，因为如果你的代码中串行部分非常重要，那么拥有并行机器也没有意义。

Alors, le parallélisme, qu'on cherche à exploiter au maximum, il peut être implicite ou explicite. Il peut être explicite, apparent, donc on va utiliser les langages qui exploitent cette— Concurrence entre les tâches, cette communication entre les tâches, etc., donc vous avez— vous voyez vraiment le parallélisme dans votre code, quoi, avec des langages un app peropriés,s. Il il peut peut être im implicite, lorsque vous lancez vos exécutions, et elles se déroulent en parallèle, vous n'agissez pas sur ce parallélismisme.e, vous lancez vos exécutions, et il y a un ordonnanceur de tâches derrière, les administrateurs de la— du centre de calcul, vont faire en sorte que ces jobs vont être être lancés en parallèle, on va allouer une partie partie, une partition pour chacun, et voilà, et s'ils étaient en parallèle, après, vous avez également laation.
那么，我们力求最大化利用的并行性，可以是隐式的，也可以是显式的。显式并行是明显的，因此我们会使用那些能够利用任务间并发和通信的语言，这样你就能在代码中真正看到并行，比如使用一些合适的语言。并行也可能是隐式的，当你启动执行时，任务会并行进行，而你并不直接控制这种并行性。你启动执行后，背后有一个任务调度器，计算中心的管理员会确保这些作业并行启动，为每个作业分配一个分区，就这样。如果它们是并行的，之后你还可以进行管理。

Par Monte-Carlo, qui consiste à tirer au hasard des combinaisons, et à lancer des—— une application avec ces différentes combinaisons, donc la même application s'exécute. Des millions— des milliers de fois, en parallèle, mais avec des données différentes, avec des paramètres différents. Donc là, il est— il est pareil implicite, et aussi l'exploitation paramétririque du Monte-Carlo, c'est une— unp cas particulier de l'exploration paramétrique. Vous avez une application et vous voulez, donc, la lancer avec des paramètres différentents, et après regarder ce que vous allez avoir comme résultat, et vous analyser par rapport aux paramètres que vous avez fait évoluer. Donc bon, vous pouvez écrire votre script tout seul, si votre code a trois paramètres A, B, C, A, il est dans l'échelle 1:13 avec un pas de 0:01, B, c'est 2:10 avec delta, c'est 2:22, donc à chaque fois vous mod multiifiezz par 2, le le C, il est dans 0:01 jusqu'à 0:—
蒙特卡洛方法是通过随机抽取组合，并用这些不同的组合运行同一个应用程序。这个应用程序会被执行数百万甚至数千次，并行运行，但使用不同的数据和参数。因此，这里隐含了蒙特卡洛方法的参数化利用，它是参数化探索的一个特殊案例。你有一个应用程序，想用不同的参数运行它，然后观察得到的结果，并根据你调整的参数进行分析。你可以自己编写脚本，比如你的代码有三个参数A、B、C，A的范围是1到13，步长为0.01，B是2到10，变化量是2到22，每次你将C乘以2，C的范围是从0.01到0——

Je ne sais pas moi, 15, mais le delta, il est de l' l'ordordre de 10 puissance moins 6, donc en fait vous faites trois boucles, et vous changez le A, B, C dans ces— dans ces intervalles-là avec ces deltas, et vous lancez la même application avec ces trois paramètres, donc ça vous donne déjà pas mal de—
我也不太确定，大约是15，但差值大约是10的负6次方，所以实际上你做三次循环，然后在这些区间内用这些差值改变A、B、C参数，接着用这三个参数运行同一个应用程序，这样你就已经得到了不少结果——

De versions, on va dire, de cette application, chacune avec un un certain— un certain vecteur de paramètres. De valeurs, et vous lancez ça sur une une machine par parallallèle, donc vous avez créé un parallélisme entre les différentes. Instances de cette application, avec les différents paramètres, et c'est aussi un— un parallélisme implicite, parce que vous ne contrôlez pas, vous n'allez pas dire, : « Moi, je veux tel.
这个应用程序有多个版本，每个版本都有一组特定的参数值。你可以将它们并行地运行在不同的机器上，这样就实现了不同应用实例之间的并行处理，参数各不相同。这也是一种隐式的并行，因为你并不直接控制，也不会说：“我要这个版本。”

Partie-là qui s'exécute là, telle partie-là qui s'exécute là, ou ça, elle communique avec ça », ce n'est pas comme vous avez le contrôle avec des langages.es. Alors, je reviens au problème de scalabilité, pas de problème, mais à l'intérêt de— quand on travaille en HPC, on a deux aspects très importants : la performance aller plus vite, et aussi la scalabilité, la mise à l'échelle, c'est-à-dire faire évoluer donc son environnement d'exécution, passer à unau.
这一部分在这里执行，那一部分在那儿执行，或者这里执行，它们彼此通信，这与使用编程语言时的控制方式不同。回到可扩展性的问题，这不是问题，但重点在于——当我们进行高性能计算（HPC）时，有两个非常重要的方面：性能，即更快的运行速度；以及可扩展性，即扩展能力，也就是说让执行环境能够发展和升级。

Plus haut, que ce soit matériel ou parallèle, mais on essaie de faire les deux, c'est-à-dire que quand vous êtes dans un contexte où vous avez un code de taille N sur une machine, et une machine de taille P, par exemple, et des processeurs, vous multipliez P par 2, vous exécutez le même code, et vous allez voir : est-ce que vous allez, deux fois plus vite, puisque vous avez mis deux fois plus de ressources. Si c'est le cas, ça veut dire que votre application, elle scale bien, c'est-à-dire qu'il n'y a pas beaucoup de synchronisation, et de— donc elle est encore exploitable, de la même manière, sur une machine qui est deux fois plus parle.
无论是硬件层面还是并行层面，我们都尝试两者兼顾。也就是说，当你在一个有大小为N的代码运行在一台机器上，而这台机器有P个处理器时，如果你将处理器数量乘以2，执行相同的代码，你会观察到：运行速度是否提升了两倍，因为你投入了两倍的资源。如果是这样，说明你的应用程序具有良好的扩展性，也就是说同步开销不大，因此它仍然可以在处理器数量翻倍的机器上以同样的方式高效运行。

Et vous pouvez aller plus loin, jusqu'à trouver la limite de cette scalabilité.. C'est ce que je venais de dire. Doncc, aug augmenter le nombre d'unités de calcul, je mets toujours CPU, c'est juste pour dire le calcul, que ce soit CPU ou GPU, éléments de calcul, et regarder jusqu'à quel point votre application, votre programme, est parallélisable. Donc en général, on fait ça beaucoup dans le calcul matriciel, avec les langages et les interfaces de communication faites court, on utilise— il y a beaucoup encore de— d'utilisation du Fortran, avec le Fortran parallèle, dans toutes ces problématiques-là.
您甚至可以进一步探究，直到找到这种可扩展性的极限。这正是我刚才所说的。因此，增加计算单元的数量，我这里一直说的是CPU，只是为了表示计算单元，无论是CPU还是GPU，然后观察您的应用程序或程序在多大程度上可以并行化。通常，我们在矩阵计算中经常这样做，使用简洁的语言和通信接口，我们还大量使用Fortran，尤其是并行Fortran，在所有这些问题中。

Donc ça, j'en ai parlé, alors, toujours dans la scalabilité, donc certains codes sont trèss, en distentressès scalableles dans ces propresé pensenceées, mais bon, c'est comme ça, par exemple l'impact, l' l'impact c'est vraiment un benchmark de référence depuis—is. Toujours,s, je dirais, depuis les années 70, donc qui est très scalable, qui est la référence pour vérifier la scalabilité sur les machines, il.
所以这个，我已经提到过了，还是关于可扩展性的问题。有些代码在它们自身的设计中非常可扩展，但情况就是这样。比如说，Impact 这个基准测试自70年代以来一直是一个参考标准，非常可扩展，是用来验证机器可扩展性的权威参考。

Y a d'autres codes, des codes réels, des codes clients qui le sont beaucoup moins, mais qu'on essaie d'optimiser pour les rendre plus scalables, mais pourquoi ils le sont moins ? Parce que la nature physique, même si on est dans dans un programme qui reprend un phénomène physique, phénomène chimique, je ne sais pas, la la nature même de de—
还有其他代码，是真实的代码，也有一些客户代码，它们的可扩展性要差得多，但我们试图优化它们以提高可扩展性。为什么它们的可扩展性较差呢？因为物理本质，即使我们是在一个模拟物理现象、化学现象的程序中，问题的本质还是——

Du métier, qu'on a impl implémenté, le phénomène qu'on a— qu'on essaie de simululer, ne permet pas d'avoir cette scalabilité. Il y a beaucoup de dépendancceses, donc il y a des fois, malheureusement, c'est comme ça, donc— Donc il y a beaucoup de communication, et donc ces n'est pas très scalable, ces applications-l.,
从我们实现的这个业务来看，我们试图模拟的现象不支持这种可扩展性。存在很多依赖关系，因此有时情况会比较复杂，不得不如此。因此，沟通成本很高，这些应用程序的可扩展性并不强。

Il y a beaucoup d'échanges de données, il y a beaucoup de synchronisation, et on va j juste faire une étude, pour montrer la limite, là, on va dire : « Regardez, ce code-là, ce n'est pas la peine de le mettre sur 1000 nœuds, parce que que de toute manière, déjà à 250 nœuds, il sature, ce n'est pas la peine d'aller plus loin. »
数据交换很多，同步也很多，我们只是做一个研究，来展示它的限制。我们会说：“看，这段代码没必要部署到1000个节点，因为实际上，在250个节点时它就已经达到饱和，没必要再增加节点了。”

Parce que travailler sur une machine massivement parallèle, ça coûte, ça coûte beaucoup en finance, ça coûte beaucoup en énergie, ça coûte beaucoup en temps, ça— donc il ne faut pas— il faut savoir ce qu'il faut pour l'utiliser, que ce qu'il faut. Alors, j'ai parlé tout à l'heure des charges et des des données en en mémoire, donc si on va très vite, et ça c'est le talon d'Achille dans le HPC, donc si vous allez très vite et que à chaque fois que vous avez besoin d'une donnée, elle n'est pas en mémoire, vous allez donc chercher dans le niveau le plus bas, le plus bas, et vous allez atterrir sur le disque, donc les pénalités et les ordres de grandeur de temps d'accès ne sont absolument pas les mêmes, et vous allez pénaliser votre exécution, donc l'espace occupé dans une application— n'oubliez jamais que c'est le calcul, les instructions, plus les données sur lesquelles s'exécute ce calcul. Donc il faut rapatrier de manière anticipée des données là où il faut, très proche du calcul, c'est-à-dire dans la mémoire et les caches, pour que ce calcul n'attende pas, ne soit pas pénalisé par l' l'accès aux données.
因为在大规模并行机器上工作成本很高，既花钱，也耗费大量能源和时间，所以必须清楚如何使用它。我刚才提到了负载和内存中的数据，如果运行速度非常快，这就是高性能计算（HPC）中的阿喀琉斯之踵。如果每次需要数据时数据不在内存中，就必须从更低层次去获取，最终会落到磁盘上，而访问时间的数量级差别巨大，这会严重影响程序的执行效率。因此，应用程序占用的空间——永远不要忘记——包括计算指令和执行这些计算所需的数据。必须提前将数据调取到计算附近，也就是内存和缓存中，这样计算才能顺畅进行，不会因数据访问而受阻。

Donc si une application est petite, là, super, tout le code déjà, il est dans la mémoire, ou dans le cache, donc on ne va pas aller le chercher ailleurs en pénalisant, vo. Si une application est très grande et dans le HPC, les applications et les données sont très grandes, donc forcément il y a une seule partie qui est dans le cache, donc vous avez le calcul, et vous avez toute la hiérarchie mémoire, donc vous la faites sous forme d'entonnoir, comme ça, parce que plus on s'approche du calcul, la technologie des composantss, est plus chère, donc on a— parce que c'est très optimisé, la technologie permet des latences—
所以如果一个应用程序很小，那就太好了，所有代码已经在内存或缓存中，因此我们不需要去其他地方获取，从而避免了性能损失。如果一个应用程序非常大，尤其是在高性能计算（HPC）中，应用程序和数据都非常庞大，那么缓存中只能存放其中一部分。这样，你就有计算过程和完整的内存层次结构，这个结构呈漏斗形，因为越接近计算部分，组件技术就越昂贵。正因为如此，技术非常优化，能够实现低延迟。

Comment on dit, petites, je vais mettre moins, comme comme ça, de plus en plus le temps d'accès est petit, donc— et la capacité est petite, et. Plus on s'éloigne, plus la capacité. Donc la latence d'accès— On va le faire comme ça, ça veut dire que ça va au mos. Et si vous ne trouvez pas votre donnée, votre code ici, vous allez au niveau plus bas, plus bas,as, et donc la latence est grande, et le code va attendre, les.
怎么说呢，容量越小，访问时间就越短，随着距离的增加，容量也会变小。因此，访问延迟会增加。我们可以这样理解，这意味着它会进入更慢的存储层次。如果你在这里找不到你的数据或代码，就会去更低一级的存储层次，因此延迟会更大，代码也需要等待。

Exécutions De l'application va attendre— il faut donc utiliser beaucoup, donc au niveau déjà de l'application exploiter la localité. Spatiale et temporelle, vo. Pourour pouvoir rapatrier les données, donc instruction plus données, rapatrier les charges de travail. Instruction plus données au haut niveau, pour ne pas retarder, localité spatiale, c'est-à-dire que lorsque vous accédez à une donnée, ou une instruction, vous avez plus de chances du.
应用程序的执行将会等待——因此需要大量使用，在应用程序层面上利用空间和时间局部性。为了能够调回数据，也就是指令加数据，调回工作负载。指令加数据在高层次上，为了不延迟，空间局部性意味着当你访问某个数据或指令时，更有可能访问到相关的数据或指令。

Les instructions, les données qui sont dans son entourage, dans les adresses proches, et temporelles, donc c'est la même choseose par rapport au temps, donc quand vous utilisez une instruction, vous avez plus de chances de la réutiliser dans la sue. Alors, le ratio— doncc quand on trouve ce qu'on cherche dans le cache— donc on fait un lit, et lorsqu'qu'onon ne trouve pas, c'est un miss, et on va sur le niveau juste immédiatement en dessous, et on refait la même chose, et des foisis on on va va un peuu plus plus loin. lo
指令和数据存在于其周围的地址中，且时间上也相近，因此它们在时间上是相似的。当你使用一条指令时，更有可能在随后的使用中再次使用它。命中率——当我们在缓存中找到所需内容时，称为命中；当找不到时，称为未命中，这时我们会立即访问下一级缓存，并重复相同的操作，有时甚至会访问更远的层级。

Le ratio, c'est tout simplement, comme son nom l'indique, c'est un ratio, donc c'est un nombre— le nombre de fois où un fait— si c'est un miss, donc le nombre de fois où on ne trouve pas— où on a un échec sur 100, c'est un taux, donc c'est sur 1 100, et le. Hit ratio, c'est 1 moins le miss ratio, donc c'est— alors, une localité forte, ça veut dire que le miss ratio est faible, donc vous utilisez des données qui sont vraiment tout le temps proches, et si vous avez beaucoup de sauts, là, ça devient problématique, et le miss ratio va augmenter, sies.
比率，顾名思义，就是一个比率，也就是一个数字——某个事件发生的次数——如果是未命中次数，就是找不到的次数，或者说失败的次数。百分比表示的话，就是一个百分比。命中率就是1减去未命中率。强局部性意味着未命中率很低，也就是说你使用的数据总是非常接近的；如果有很多跳跃，那就会有问题，未命中率会增加。

Instructions actives qui sont souvent utilisées, c'est-à-dire quand vous êtes dans une boucle, et vous avez le corps de boucle, le nid de boucle, vous bouclez beaucoup, donc là, c'est des instructions actives, et souvent quand on fait des traitements sur les matrices et tout, donc on peut aller à la matrice, mais les instructions, le calcul, c'est le même, donc on est dans le nid de boucle.
经常使用的活动指令，也就是说，当你处在一个循环中，有循环体、循环嵌套，你循环很多次，这时就是活动指令。通常在对矩阵等进行处理时，我们可以访问矩阵，但指令和计算是相同的，所以我们处在循环嵌套中。

Donc si on est souvent dans ce— ou on a un nid de boucle assez fourni, le taux de miss, en général, il est faible, parce qu'on utilise beaucoup les localités spatiales et temporelles. Alors, les données restent rarement longtemps dans les caches, parce qu'il y a un renouvellement, bien évidemment, et le tuning, le paramétrage va permettre de garder le plus possible cette localité, donc on anticipe avec le tuning, pour savoir est-ce que je suis proche du branchement, du saut, et donc à ce moment-là, je vais rapatrier les données qui sont un peu loin, parce que je vais sortir rapidement de cette localité, ou j'ai encore du temps, et donc je vais garder les données, les données— les charges dans le cache, et je ne vais pas faire des évictions pour le moment.
因此，如果我们经常处于这种情况——或者说我们有一个相当丰富的循环嵌套，缺失率通常很低，因为我们大量利用了空间和时间的局部性。数据很少会长时间停留在缓存中，因为显然会有更新，而调优和参数设置则有助于尽可能保持这种局部性。因此，我们通过调优来预测自己是否接近分支或跳转点，在这种情况下，我会提前调入那些稍远的数据，因为我将很快离开这个局部区域；如果还有时间，我会继续保留这些数据和缓存中的负载，而暂时不会进行驱逐。

Le tuning, à ce niveau-là, est très important.ant, Al Al Alors, ensuite, tout ce qui est latence, débit, et taux, c'est plus ou moins ceon. A vu auparavant, pour le HPC, ça ne change pas tellement. On utilise surtout le megaflops, le mix restete présent, mais voilà, on parle beaucoup de débit mémoire, de débit disque, de débit réseau, on.
在这个层面上，调优非常重要。然后，所有关于延迟、带宽和速率的内容或多或少都是如此。正如之前所见，对于高性能计算（HPC）来说，这些变化并不大。我们主要使用兆浮点运算（megaflops），混合指标仍然存在，但重点更多地放在内存带宽、磁盘带宽和网络带宽上。

Parle beaucoup de latence ou de temps de réponse, souvent on utilise le mot latence, le taux d'occupation et d'utilisation d'une ressource sont très utilisés— les métriques très utilisées, parce que l'investissement est colossal, et donc on veut l'exploiter au maximum, et. On pousse aux limites et on regarde quand est-ce qu'on ar arrive à saturer le système, donc ce chemin-là est critique, et en même temps on garde ça en mémoire pour pouvoir faire évoluer notre système pour sortir de cette saturation.
经常谈论延迟或响应时间，通常我们使用“延迟”一词。资源的占用率和利用率也是非常常用的指标，因为投资巨大，因此我们希望最大限度地利用资源。我们会将系统推向极限，观察何时达到饱和状态，因此这条路径非常关键。同时，我们会将这些信息记录下来，以便改进系统，摆脱饱和状态。

Ou bien de changer complètement le système— donc soit on entier, enfin c'est entre. On. Change complètement, c'est 100 %, et. Saturer complètement, c'est 100 %, et idle c'est 0 %, donc on va surveiller le système. Sur les bornes externes. Dans les autres métes. Ut Utilisées dans le HPC, efficacité, fiabilité, donc c'est pareil, pour l'efficacité on on a a t toujours l'efficacité crête, théorique, donc quand il n'y a pas de— quand la charge est idéale, et.
或者完全改变系统——所以要么完全改变，百分之百改变；饱和度也是百分之百，空闲则是零百分比，因此我们将监控系统的外部端口。在高性能计算中使用的其他指标也是如此，效率和可靠性也是一样的。对于效率，我们总是有理论上的峰值效率，也就是在负载理想的情况下。

Notamment tout ce qui est pipeline, et massivement parallèle, quandand vous avez pas beaucoup de synchronisation, quand les tâches sont complètementement. Indépendantes, et donc vous pouvez tirer profit au maximum de votre système, et là, l'efficacité, elle tombe à 1, la. Disponibilité, toujours avec les mêmes métriques, alors, on a toujours le.
特别是所有涉及流水线和大规模并行的情况，当你几乎不需要同步，任务完全独立时，你可以最大限度地利用你的系统，此时效率达到1。可用性，仍然使用相同的指标，我们总是有相同的情况。

Rapport performance-prix ici, vraiment prix, parce que les machines sont souvent très, très, très coûteuses en HPC, alors maintenance est coûteuse, et l'énergie consommée est coûteuse, donc on a le prix et la consommation d'énergie, qui rentrent vraiment beaucoup en considération, surtout lorsqu'on utilise le calcul flottant, et donc les flops par watt, sont maintenant un métrique qui est largement utilisé en HPC.
这里谈的是性价比，特别是价格方面，因为高性能计算（HPC）中的机器通常非常非常昂贵，维护费用高，能耗也高。因此，价格和能耗成为了非常重要的考虑因素，尤其是在使用浮点计算时。每瓦特浮点运算次数（flops/watt）现在已成为高性能计算中广泛使用的一个指标。

Alors, les problématiques XX band, est-ce que je suis dans un contexte limité par le calcul, limité par la mémoire, ou— alors, limité par la mémoire, c'est de là, ou limité par les entrées-sorties ? Alors, l'idée c'est de savoir quel est le facteur limitant, pour bien sûr optimiser et résoudre le problème, là où il se— là où il se passe.
那么，XX频段的问题是，我是在计算受限的环境中，还是内存受限，或者——那么，是内存受限，还是输入输出受限？关键是要弄清楚限制因素是什么，以便针对性地优化和解决问题，解决问题发生的地方。

On a trois axes, donc le calcul, l'accès mémoire, et les entrées-sorties, la communication avec les nœuds d'entrées-sorties, ou carrément les systèmes de stockage directement. Donc si c'est un CPU band, ça veut dire que la performance est vraiment limitée par la fréquence de l'élément de calcul, on peut agirir sur la fréquence, comment savoir qu'on est CPU band, on peut agir sur la fréquence du CPU, avec le CPU governor ou d'autres outils, on peut changer la fréquence, et siil les performances changent, ça veut dire qu'on est CPU band. Si les performances ne changent pas, ou changent vraiment de manière négligeable, ça veut dire qu'on n'est pas CPU band, c n'est pas la peine, donc, d'aller chercher quelque chose sur le calcul.
有三个方面：计算、内存访问和输入输出，即与输入输出节点的通信，或者直接与存储系统的通信。如果是CPU瓶颈，意味着性能主要受计算单元频率的限制。我们可以调整频率，如何判断是否是CPU瓶颈呢？可以通过CPU调节器或其他工具改变CPU频率，如果性能随之变化，说明是CPU瓶颈。如果性能没有变化，或者变化非常微小，说明不是CPU瓶颈，那么就没必要在计算方面做改进。

Alors, si on est, donc,c, limité par la hiérarchie mémoire, alors, on peut peut être limité par la hiérarchie mémoire, de deux manières, soit par le débit mémoire, soit par la— donc la latence et les accès, que j'ai— Je pensais qu'il y' avavait plus— bon, je peux exp expliquer. Donc ici, le code, quand c'est mémory band, donc le code est limité par le débit, donc la vitesse— on va dire— le nombre de fois où j'accède à la mémoire, et je trouve le.
那么，如果我们受限于内存层次结构，那么可能有两种方式受到限制：要么是内存带宽，要么是延迟和访问次数。我原以为有更多的限制——好，我可以解释一下。这里，当代码受限于内存带宽时，代码的限制在于带宽，也就是访问内存的速度或次数。

L'information dans cette mémoire. Donc je regarde le miss, le miss rate, ou le hit rate, si le miss rate est élevé, donc je suis mémory band, je suis limitée par la mémoire, parce que les données que je cherche ne sont pas toujours là, je suis obligée d'aller les chercher ailleurs, et donc là, c'est soit je travaille sur la localité, que j'ai, pour anticiper et remonter les données le plus vite possible, au bon moment, soit je.
内存中的信息。因此，我查看未命中率或命中率。如果未命中率很高，那么我就受限于内存带宽，因为我寻找的数据并不总是在这里，我不得不去其他地方获取数据。因此，我要么利用已有的局部性，提前预取并尽快在合适的时机加载数据，要么我……

Limimitité par la capacité, et donc il faut augmenter la capacité pour garder les données, parce que souvent quand je suis— j'exploite au mieux ma localité, qu'est-ce qu'il y a, ce qui cause ce problème de mémory band, c'est que la taille est trop petite, donc même si j'ai une connaissance, je vais— des données que je vais utiliser par par la suite, je peux pas vraiment l'exploiter, parce que j'ai pas assez de place pour remonter ces données. Donc je les mets de manière provisoire, dans les niveaux plus bas. Donc je regarde d'abord si j'exploite bien ma localité, si j'exploite bien le taux de miss reste élevé, c'est qu'il faut absolument agir de manière physique, et augmenter la ta taille des niveaux les plus hauts. Si je suis— alors, latency band, donc je suis limitée par la latenceence mémoire, c'est-à-dire que mon miss rate, n'est pas élevé, donc je trouve assez souvent les données que je veux, dans les niveaux de cache les plus élevés. Par contre, la latenceence est est très importante, la différence entre la latence mémémoire et la fréquence de calcul est vraiment très importante, ça veut dire que mon support est tout simplement vieillissant, et la.
受容量限制，因此必须增加容量以保存数据。因为通常当我充分利用局部性时，问题出在内存带宽上，原因是容量太小，所以即使我知道后续要使用哪些数据，也无法真正利用它们，因为没有足够的空间来加载这些数据。因此，我会将它们临时存放在较低层级的存储中。首先，我会检查是否充分利用了局部性，如果未命中率仍然很高，就必须采取物理措施，增加最高层级的容量。如果我受限于延迟带宽，也就是说未命中率不高，说明我经常能在最高级别的缓存中找到所需数据。但延迟非常重要，内存延迟与计算频率之间的差异非常大，这意味着我的存储设备已经相当老旧。

Performance— enfin, matériellement, elle est pas adéquate. Là, j'ai une Ferrari, et là, j'ai un un— je sais pas quelle voiture qui— une 4L, ou une vie vieille voiture. Donc là, il faut que je change le matériel, forcément. Il n'y a plus d'autre chose à gratter sur le soft, etc. Là, il faut que j'achète et je mette une mémoire, qui a une latence déjà à l'origine qui est plus petite.
性能——说到底，硬件是不合适的。现在我有一辆法拉利，而另一辆车，我也说不清是什么车——可能是一辆4L，或者是一辆很旧的车。所以我必须更换硬件，别无选择。软件方面已经没有什么可以改进的了。现在我必须购买并安装一块延迟本来就更低的内存。

Donc ça, c'est plus petit. Alors, après, le I/O band, c'est un code qui est limité par le débit I/O, donc souvent déjà on regarde la pro proportion d'entrées-sorties sur le code, par rapport au calcul, si on a beaucoup de phases d'entrées-sorties, si on manipule des données très, très volumineuses, donc là, il y a beaucoup de manières de faire, soit qu'on va paralléliser cette partie-là, et on fait donc des traitements sur des bouts de données et on synchronise après, on peut également agrandir les supports de stockage, on peut anticiper et ramener— et faire le calcul sur les nœudss de calcul, les nœuds, pardon, d'entrées-sorties, et pas sur les nœuds de calcul, donc c'est-à-dire migrer le calcul et le mettre proche des données, les nœuds d'entrées-sorties, maintenant, ont des capacités de calcul qui sont pas très utilisées, parce que maintenant on met une carte qui carbure, mais on l'utilise uniquement pour le mapping des adresses, etc., donc on peut migrer une partie des calculs dans les nœuds d'entrées-sorties, pour utiliser les données,— faire les calculs sur les donnénées là où el elles sont. Il y a aussi maintenant, c'est le computing memory, donc vous avez des cartes mémoire qui sont dotées d'éléments de calcul, qui font du calcul, notamment tout ce qui est IA, parce que les calculs sont très simples, donc on fait des calculs très proches des données, là où elles sont.
所以这个更小一些。然后，I/O带宽是指受限于I/O吞吐量的代码，因此我们通常会先观察代码中输入输出操作与计算的比例，如果有大量的输入输出阶段，或者处理非常庞大的数据，那么有很多方法可以应对。比如，我们可以并行化这部分操作，对数据片段进行处理，然后再进行同步；也可以扩大存储介质的容量；还可以提前准备并将计算任务转移到I/O节点上，而不是计算节点上，也就是说，将计算迁移到靠近数据的位置。现在的I/O节点具备一定的计算能力，但通常没有被充分利用，因为我们通常只用它们来做地址映射等操作。因此，我们可以将部分计算任务迁移到I/O节点上，利用数据所在的位置进行计算。现在还有一种叫做计算内存的技术，即内存卡上配备了计算单元，能够进行计算，尤其适合人工智能相关的计算，因为这些计算非常简单，可以在数据所在的位置进行计算。

Alors, quand on est dans le HPC, et on dit I/O band, alors, jusqu'à, c'est dernières années, les gens disaient, : « bon, moi je suis en calcul haute performance, je m'intéresse au calcul, le I/O band, ça devrait pas signerer risque, ça devrait pas être présent dans ce contexte-là. Mais. Malheureusement, avec l'utilisation, enfin, les applications ont changé, il y a les modèles d'IA qui en sont venus tout faire basculer, donc maintenant on utilise beaucoup, beaucoup de données, et donc c'est absolument pas une hérésie de parler de I/O band, c'est même un facteur très limitant, donc ça devient très fréquent, même si ça le devrait pas, parce qu'on reste dans le calcul haute performance, donc il faut qu'on soit axé sur le calcul, donc qu'est-ce qu'on fait maintenant, on dit, bon, c'est une limitation, le I/O band, c'est une réalité, mais uniquement pour les nœuds d'entrées-sorties, pas pour les compute nodes, c'est juste les I/O nodes.
那么，当我们谈论高性能计算（HPC）中的I/O带宽时，直到最近几年，人们还会说：“我专注于高性能计算，关注计算性能，I/O带宽不应该成为问题，也不应该出现在这个环境中。”但是，不幸的是，随着应用的变化，尤其是人工智能模型的出现，一切都发生了改变。现在我们使用大量数据，因此谈论I/O带宽绝不是异端，反而是一个非常限制性能的因素，所以这种情况变得非常普遍，尽管本不应该如此，因为我们仍然专注于高性能计算，重点应放在计算上。那么我们现在怎么做呢？我们说，I/O带宽确实是一个限制，这是现实，但仅限于输入输出节点，而不是计算节点，仅仅是I/O节点的问题。

Donc sur les nœuds d'entrées-sorties, qui sont avec disque, parce qu'il y en a qui sont pas avec disque, qui font juste l'interface, qui servent d'accélérateur I/O, et qui font la connexion avec les systèmes de stockage, donc dans les nœuds de stockage avec disque, donc on peut faire beaucoup de tuning, et.
因此，在带有磁盘的输入输出节点上，因为有些节点没有磁盘，仅作为接口，作为I/O加速器，并与存储系统连接；所以在带有磁盘的存储节点上，我们可以进行大量的调优。

On a beaucoup de moyens pour avoir des informations sur les taux d'occupation, sur les profils d'accès, etc., et on peut faire beaucoup d'optimisation, sinon il faut beaucoup paralléliser aussi les entrées-sorties avec du striping qui est juste là, donc subdiviser les données, les disperser sur les éléments de stockage, les accéder en parallèle pour faire la lecture, l'écriture, etc., ou bien changer complètement d'architecture, si les disques sont devenus très— et puis on a des latences d'accès très, très importantes, donc on peut changer les disques, maintenant on a des supports de mémoire non volatiles qui sont très rapides, mais qui sont beaucoup moins fiables que les bons vieux disques magnétiques et les volumes magnéties.
我们有很多方法可以获取占用率、访问配置文件等信息，并且可以进行大量优化。否则，也需要通过条带化技术对输入输出进行高度并行处理，即将数据细分，分散到存储单元上，进行并行访问以实现读写操作等。或者完全改变架构，因为硬盘变得非常——而且访问延迟非常高，所以我们可以更换硬盘。现在有非常快速的非易失性存储介质，但它们的可靠性远不如传统的磁盘和磁盘卷。

Alors, est-ce que quand on fait une analyse et on dit, ah, je suis memory band, ou je suis CPU band, est-ce que je le suis pour toute la vie? Non. Parce qu'il suffit de changer de machine que les choses changent. Quand j'ai un code, je l'exécute sur une plateforme, et il est compute band, il suffit que je change de machine et là, il devient memory band, ou I/O band, donc il faut vraimentiment faire l'étude à chaque fois que je change de code, que je change de— surtout que je change de machine, on peut extrapoler, il y en a qui disent, je peux extrapoler, j'étais sur une machine qui avait un nœud, je passe sur la machine P nœud, et je passe sur— je passe sur une machine qui a P2 nœud, je peux extrapoler, non, on peut pas vraiment extrapoler, il faut vraiment faire attention parce que la performance n'est pas toujours linéaire, elle est même rarement linéaire, donc on a toujours des seuils où où il y a une saturation, où il y a un brusque changement de comportement, donc il faut faire attention à ça.
那么，当我们进行分析时，如果说“我是内存带宽受限”或者“我是CPU带宽受限”，这会是永久的吗？不是的。因为只要换了机器，情况就会改变。当我有一段代码，在某个平台上运行时是计算带宽受限的，只要换了机器，它可能变成内存带宽受限或者I/O带宽受限。因此，每次更换代码，尤其是更换机器时，都必须重新进行研究。有些人说可以外推，比如我之前在一台有一个节点的机器上，现在换到一台有P个节点的机器，再换到一台有2P个节点的机器，我可以进行外推。其实不行，不能真正外推，必须非常小心，因为性能并不总是线性的，甚至很少是线性的。总会有一些阈值，出现饱和或者行为突然变化的情况，所以必须注意这些问题。

Il faut pas extrapoler comme ça, donc comme je disais, un calcul complexe, qui peut être CPU band sur un processeur, je change, je l'optimise, et je peux en enlever cette limitation-là. Quand je change sur le même code, je change des données, el. Pas— elles vont me provoquer un taux de misss, incroyable, et je balance dans le memory band, je.
不要这样过度推断，所以正如我所说，这是一个复杂的计算，可能在处理器上受限于CPU带宽，我进行更改，进行优化，就能消除这个限制。当我在相同的代码上更改数据时，它们会导致极高的缺失率，然后我就会陷入内存带宽的瓶颈。

Peux être CPU band sur une famille de processeurs, je change— je passe de, je sais pas moi, de Intel à ARM, ou ou ma maintenant ça a été racheté par NVIDIA, je sais plus, donc je change complètement de— De limitation, donc il faut refaire à chaque fois les tests. Alors, les tests pour savoir si je suis memory band, CPU band, etc., souvent ça se fait sur des codes très, très basiques, qui ont été développés, donc il y a très longttemps, mais qui restent d'actualité, qui restent très utilisés, et même des références donc.
可能会在某个处理器系列上出现CPU瓶颈，比如我切换——我从英特尔换到ARM，或者现在ARM被英伟达收购了，我记不清了，所以我完全改变了限制条件，因此每次都需要重新进行测试。通常，为了判断我是内存瓶颈还是CPU瓶颈，测试会使用一些非常基础的代码，这些代码开发得很早，但至今仍然有效且被广泛使用，甚至成为了参考标准。

Vous avez, par exemple, le copy, donc qui qui vous permet juste de lire un vecteur et de le copier, ça, ça vous permet vraiment d'étudier votre— les capacités et les limites de votre accès et de vos— de vot votre mémoire, parce que vous accédez la mémoire pour lire un vecteur, et vous ré-accédez la mémoire pour écrire, et vous allez pousser donc les limites de la mémoire. Ensuite, vous avez introduire donc un peu le calcul, vous lisez, vous faites un calcul simple, c'est-à-dire vous multipliez toujours par le même scalaire, parce que si vous changez les valeurs, vous allez aller chercher ces valeurs-là en mémoire, donc vous forcez le— don donc vous avez un flux de lecture, un flux d'écriture, pareil ici, mais on crée deux flux, vous allez mettre une opération, donc qui va introduire un délai, qui est bien déterminé, on connaît la durée d'une multiplication, donc on a un délai déterministe, et on regarde qu'est-ce que ça fait au niveau de la mémoire, là, on va faire deux flux en lecture, séparés par le flux en é écriture, par une opération d'addition, le.
例如，你有复制操作，它允许你读取一个向量并将其复制，这样你就可以真正研究你访问和使用内存的能力和限制，因为你需要访问内存来读取向量，然后再次访问内存来写入，从而推动内存的极限。接下来，你引入了一些计算，你读取数据，进行简单的计算，也就是始终乘以相同的标量，因为如果你改变数值，你就需要从内存中获取这些数值，这样你就强制产生了一个读取流和一个写入流。同样地，这里创建了两个流，你会加入一个操作，这个操作会引入一个确定的延迟，我们知道一次乘法的时间，因此有一个确定性的延迟，然后观察这对内存的影响。在这里，我们将进行两个读取流，中间夹着一个写入流，通过一个加法操作。

Triad c'est la somme— je dirais le mix du scale et du add, donc deux flux en lecture, mais d' d'abord un premier flu flux avec une multiplication par un scalaire, le temps est déterministe, on lui ajoute donc le deuxième flux, et on va après nourrir le troisième flux en écriture, et aprèsès, donc faire le read pur, donc on a un seul flux en.
三元组是scale和add的结合——我会说是两者的混合，因此有两个读取流，但首先是第一个流与一个标量相乘，时间是确定性的，然后我们加入第二个流，接着将结果写入第三个流，然后进行纯读取，因此我们只有一个流。

Mre. En lecture, et on fait une somme cumulative, donc on fait que lire, on sature la mémoire avec la lecture, parce que l'addition ç ça coûte rien du tout, et on va voir quelle est la limite, quel est le débit que peut nous donner notre mémoire, quelle est la latence, etc. Et surtout que là, tout est— on peut facilement anter.
阅读时，我们进行累积求和，也就是说我们只是不断阅读，通过阅读让记忆达到饱和，因为加法运算几乎不花费资源。我们想看看记忆的极限是什么，记忆能给我们多大的吞吐量，延迟是多少，等等。尤其是这里，一切都——我们可以很容易地提前处理。

Eter. Sur des localités. Donc on peut lancer plusieurs tâches de copy-scale et add, etc., en parallèle pour saturer le débitbit de mémémoire,re, je vais voir jusqu'à quel niveau de scale je peux aller pour saturaturer la mémoire en lecture, en écriture, mais avant tout, attention, parce que l'écriture, ça dépend des architectures, elle peut être implémentée au bas niveau, c'est pour ça que quand je disais tout à l'heureure le HPC, on va même jusqu'au bas niveau, jusqu'à l'assembleur, pour voir sur le jeu d'instructions de la machine qu'on utilise, comment sont implémentées les instructions, par exemple l'écriture en mémoire, sur des— il y a des jeux d'instructions, des architectures où elle est implémentée en une seule fois, en une seule opération, et il y a des architectures dans le jeu d'instructions, on voit bien qu'elle a été— qu'elle double l'opération, qu'elle est implémentée en deux fois, c'est-à-dire qu'on va lire d'abord, et modifier après écrire, donc quand on veut faire une écriture, on lit d'abord l'ancienne version, et après on la modifie, donc ça fait deux accès mémoire, et là tout est— si vous ne savez pas comment, dans le jeu d'instructions de la machine, commentment l'écriture est implémentée, vous aurez deux flux en écriture, alors que vous pensez en avoir un seul.
以局部性为基础。因此，我们可以并行启动多个复制、缩放和添加等任务，以充分利用内存带宽。我将观察在多大规模下可以达到内存读写的饱和状态。但首先需要注意的是，写操作取决于架构，它可能在底层实现。这也是为什么我之前提到高性能计算（HPC）时，我们甚至会深入到底层，甚至汇编语言层面，查看所用机器的指令集，了解指令是如何实现的。例如，内存写操作在某些指令集和架构中可以一次性完成，而在另一些架构中，写操作会被拆分成两步：先读取，再修改后写入。也就是说，当执行写操作时，先读取旧数据，然后修改，导致两次内存访问。如果你不了解机器指令集中写操作的具体实现方式，可能会误以为只有一次写入操作，实际上却有两次写入流。

Donc c'est pour ça que vraiment c'est important, en HPC, il faut connaître sa machine, il faut connaître le jeu d'instructions, comment les opérérations sont implémentées, pour savoir— est-ce que j'ai un flux, j'ai deux, et c'est un facteur de deux quand même, donc ça peut tout fausser. Donc les métriques classiques, on utilise le MegaFlow, le.
所以这就是为什么在高性能计算中，了解自己的机器非常重要，必须了解指令集，了解操作是如何实现的，才能知道——我有一个数据流，还是两个，这可是一个两倍的差别，可能会完全影响结果。因此，我们使用经典的指标，比如MegaFlow。

MIPS pas vraiment, d'ailleurs il a été rebaptisé, donc voilà, et. Pour obtenir des des valeurs, parce qu'il n'y a que ça qui compte, l'évaluation quantitative des performances, on utilise des benchmarks ciblés pour avoir des mesures vraiment élémentaires, comme tout à l'heure, donc mesurer de manière très, très fine en séparant le flux, un flux, un seul flux en entrée, en lecture, un flux en lecture, un flux en écriture, regarder comment l'écriture est implémentée, doncc c'est des mesures élémentaires, il faut instrumenter votre matel.
MIPS 并不完全准确，实际上它已经被重新命名了。为了获得数值，因为只有数值才重要，即性能的定量评估，我们使用针对性的基准测试来获得非常基础的测量，就像刚才提到的那样。通过非常细致地测量，将数据流分开，一个单独的输入流，一个读取流，一个写入流，观察写入是如何实现的。这些都是基础的测量，必须对你的硬件进行插桩。

Des fois on n'en a pas besoin, parce que on a déjà des outils présents comme les compteurs de performance, les compteurs de performance sont un. Nombre de registres, qui sont matériellement existants sur les machines, donc qui sont là, le jeu d'instructions du. Constructeur permet de les avoir— il y a des fonctions implémentées en dur, donc qui enregistrent pas mal d'événements sur les composants, et vous, vous pouvez avoir accès à ces événements, à ces statistiques, et pouvoir les utiliser pour caractériser les performances de votre machine, surtout en en applications données, donc il y a des outils qui sont industriels, donc qui sont payants, et il y a des outils qui sont universitaires, qui sont gratuits notamment, papy, qui a été utilisé— enfin non, développé par l'université de Tennessee, et.
有时候我们不需要额外的工具，因为已经有现成的工具，比如性能计数器。性能计数器是一种硬件存在于机器上的多个寄存器，处理器的指令集允许访问它们——这些功能是硬编码实现的，可以记录组件上的许多事件。你可以访问这些事件和统计数据，并利用它们来描述你的机器性能，特别是在特定应用中。因此，有些工具是工业级的，需要付费；也有一些是大学开发的免费工具，比如由田纳西大学开发的papy。

Qui vous donne pas mal, je dirais même trop, de. Informations le nombre de cycles qu'a pris l'exécution de votre application, le nombre de cycles d'ente. Parce qu'on attend les entrées-sorties, ou les données qui ne sont pas là, le nombre d'instructions, le nombre d'instructionss. flottantes, le nombre de load store, le nombre de branchements, et même ceux qui sont pris et ceux qui ne sont pas pris, et qui ont été invalidés, le taux de miss, le taux de hit, le nombre de miss, par inst instruction, enfin vraiment beaucoup, beaucoup, be beaucoup de d'informations que vous pouvez utiliser, alors juste pour terminer, quand vous faites une évaluation, quantitative, il n'y a que ça qui compte, il faut toujours avoir une référence, vous ne pouvez pas venir dire « ma machine est très performante, elle exécute ce ce code en deux secondes », oui, mais moi je ne sais pas, deux secondes, c'est beaucoup ou pas beauco ?
这给了你不少信息，我甚至可以说是太多了，比如你的应用程序执行所花费的周期数，等待输入输出或缺失数据所花费的周期数，指令数量，浮点指令数量，加载存储指令数量，分支指令数量，甚至包括被采纳和未被采纳的分支，以及被无效的分支，缺失率，命中率，每条指令的缺失次数，真的有非常非常多的信息可以使用。最后总结一下，当你进行定量评估时，只有这些数据才是重要的，你必须始终有一个参考标准。你不能说“我的机器非常高效，它执行这段代码只用了两秒”，但我不知道两秒是多还是少。

Ça dépend par rapport à quoi, et je donne cet exemple, quand vous dites « la terre pèse 6 fois 10 puissance 24 kilos », moi je ne sais pas, si c'est beaucoup ou pas beaucoup par rapport aux autres planètes, parce que je ne m'y connais pas, moi en en planètes, en— donc ça ne veut rien dire, donc si vous n'avez pas de référence, le métrique même si vous avez passé be beaucoup de temps à instrumenter, à analyser, à— il ne veut rien dire, donc si vous dites « la terre pèèse t tant et elle est plus légère que Vénus, elle est légèrement plus légère que— plus légère, c'est 0,8 fois la taille de Vénus, », mais elle est 0 fois— une fois la taille de Mars », donc pour plus— comment dire, beaucoup plus lourde, mais.
这取决于相对于什么来说。举个例子，当你说“地球重6乘以10的24次方公斤”时，我不知道这相对于其他行星来说是多还是少，因为我对行星不太了解，所以这没有什么意义。如果你没有参考标准，即使你花了很多时间去测量、分析，这个数字也没有意义。比如说，如果你说“地球重这么多，它比金星轻一点，轻了大约0.8倍金星的大小”，但它是火星大小的多少倍呢？所以，怎么说呢，地球比火星重得多，但……

Jupiter c'est 315 fois la taille de la Terre, donc si si ç ça vous donne des références, et vous pouvez voir que oui, Jupiter est très, très grand, la Terre elle est minuscule à côté, mais attention, elle est presque— presque très proche de Vénus, donc vous avez des éléments de référence, si vous dites « j'ai tel benchmark », je l'utilise sur telle machine, il me donne deux secondes, et quand je l'utilise sur telle machine de référence, il me donne 1.
木星的体积是地球的315倍，所以如果这样说能给你一些参考，你可以看到，木星确实非常非常大，而地球相比之下非常渺小。但请注意，地球几乎——几乎非常接近金星，所以你有一些参考依据。如果你说“我有某个基准测试”，我在某台机器上运行它，结果是两秒钟，而在另一台参考机器上运行，结果是1秒钟。

Secondes, là vous pouvez dire « ah mais sur cette machine de référence j'ai 15, là j'ai 2, je vais 7 fois et demieie plus vite », donc vous pouvez dire que réellement vous êtes plus rapide, ou moins rapide, ça dépend, des fois on annonce des mauvaises nouvelles, que telle machine, et les gens peuvent comparer, donc voilà, vo.
稍等，您可以说“啊，但在这台参考机器上我得了15分，这里是2分，我快了7倍半”，所以您可以说实际上您更快，或者更慢，这取决于情况，有时候我们会传达一些坏消息，比如某台机器的表现，人们可以进行比较，就是这样。
